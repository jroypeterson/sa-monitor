# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.egg-info/

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# Project-specific
# logs/ are committed by ci_run.sh for audit; keep them tracked
# state/ is committed by ci_run.sh for cross-session dedup; keep tracked
# data/calendars/ holds Phase 2 enrichment calendars fetched per-session
# from sibling repos — transient cache, never commit
data/calendars/

# Edit-tool atomic-write leftovers (Windows + Dropbox race)
*.tmp.*
