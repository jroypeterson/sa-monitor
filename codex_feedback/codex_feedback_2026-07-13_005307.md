# Codex feedback — 2026-07-13_005307

Auto-generated by root `scripts/codex_review.sh`. The Codex banner below (version, model,
session id) is emitted by Codex itself — that is the verification it actually ran.

- generated_at: 2026-07-13T00:53:07-04:00
- codex_cli: codex-cli 0.141.0
- mode: plan
- reasoning_effort: xhigh
- cwd: /mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor
- git_head: 61f934f

-----------------------------------------------------------------------

Reading additional input from stdin...
OpenAI Codex v0.141.0
--------
workdir: /mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor
model: gpt-5.5
provider: openai
approval: never
sandbox: read-only
reasoning effort: xhigh
reasoning summaries: none
session id: 019f59d2-653c-73e0-89b5-50c92c58340d
--------
user
# Adversarial re-review (ROUND 2) — sa-monitor HC event classifier fixes

Round 1 found 5 classifier false-positives + 1 CTA wording issue; all FIXED: F1 clearance now gated by shared _reg_suppressed() (denial+commentary+pending); F2 _REG_DENIAL suppresses approval+clearance on 'did not/fails to/denies/withdraws/etc' (a bare rejection → None, not approval); F3 _REG_COMMENTARY suppresses 'comments on/congratulates/statement on/etc'; F4 _TRIAL_NEGATIVE now catches future-timeline 'expected/anticipated in|by|Q#|20xx / to read out / readout expected' but NOT bare 'expected' (so 'met primary, better than expected' still fires MET); F5 _CRL no longer matches bare 'CRL' (Charles River ticker) — requires 'complete response letter' or a receive/issue/from-FDA idiom; F6 biotech_triage_cta parameterized with context (halt path unchanged, HC events say 'readout/approval/CRL/clearance'). 250 tests pass (+22 negatives). I independently confirmed all 5 repros → None and the 4 real events still fire.

VERIFY the 6 fixes are complete and correct, and hunt for ANY REMAINING classifier false-positive or new regression: more denial/commentary phrasings that slip through, a real event now wrongly SUPPRESSED (false negative) by the new gates, direction inversions, the _reg_suppressed gate applied where it shouldn't be, CTA context regressions on the halt path. Concrete file:line + repro. IGNORE style/whitespace. If clean, say exactly: 'No Critical/High issues.'

## Diff (src + tests)
```diff
diff --git a/src/dedup.py b/src/dedup.py
index 94a7b65..8b36303 100644
--- a/src/dedup.py
+++ b/src/dedup.py
@@ -43,6 +43,12 @@ class HaltTracker:
     # ever. Persisted alongside seen_halts so a mid-session restart never
     # re-emits a follow-up.
     followed_up: set[HaltId] = field(default_factory=set)
+    # Delivery-dedup for the HC event-wire lane. Keys are strings of the form
+    # f"{news_id}|{symbol}|{event_type}" — one alert per (press release,
+    # covered ticker, event type), ever. Persisted like followed_up so a
+    # mid-session restart never re-emits an HC event. (Mirrors emitted_halts /
+    # followed_up exactly; unlike them, keyed by string not HaltId.)
+    hc_events_emitted: set[str] = field(default_factory=set)
 
     def ingest(self, events: list[HaltEvent]) -> Iterator[tuple[str, HaltEvent]]:
         """Diff `events` against state and yield (kind, event) for new ones.
@@ -85,3 +91,4 @@ class HaltTracker:
         self.resumes_emitted.clear()
         self.emitted_halts.clear()
         self.followed_up.clear()
+        self.hc_events_emitted.clear()
diff --git a/src/events/__init__.py b/src/events/__init__.py
new file mode 100644
index 0000000..8e06c6c
--- /dev/null
+++ b/src/events/__init__.py
@@ -0,0 +1,18 @@
+"""HC event-wire layer (v1 — PR-wire consumer).
+
+Consumes the PRN/BW/GlobeNewswire NewsItems that halt_monitor already fetches
+each news poll, classifies each into an HC event type (clinical-trial topline
+readout / FDA approval / CRL / medtech clearance), and — via the emit path in
+halt_monitor — fires an SA-faithful #street-account alert when the issuer's
+self-identified ticker is in the covered universe.
+
+Design of record: research/sa_monitor_hc_event_wire_design_2026-07-13.md.
+
+- `types.HCEvent`  — the classified-event dataclass.
+- `classify.classify(item) -> Optional[HCEvent]` — precision-biased keyword
+  classifier anchored to the locked headlines in template-library.md §15/§16/§19.
+
+The universe filter, per-ticker attribution, dedup, and delivery gating live in
+halt_monitor._emit_hc_events — this package only decides *what kind of event*
+a headline is (or None), never *whether to fire*.
+"""
diff --git a/src/events/classify.py b/src/events/classify.py
new file mode 100644
index 0000000..c0111ee
--- /dev/null
+++ b/src/events/classify.py
@@ -0,0 +1,267 @@
+"""Precision-biased keyword classifier: NewsItem -> Optional[HCEvent].
+
+Design of record: research/sa_monitor_hc_event_wire_design_2026-07-13.md §3.
+Rules are anchored to the LOCKED real headlines in template-library.md
+§15 (Phase-N primary MISSED), §16 (FDA approval), §19 (Phase-N primary MET).
+
+Philosophy — "miss over misfire" (design §7): a wrong "Phase 3 met" or "FDA
+approval" alert costs more trust than a missed one. Every rule requires the
+headline to CLEARLY match a completed action. Ambiguous, future-tense,
+procedural, or presentation headlines return None.
+
+classify() only proposes an event *type/direction/phase*; it never decides
+whether to fire. The universe filter + attribution + dedup live in
+halt_monitor._emit_hc_events. classify() returns an HCEvent with symbol="".
+"""
+from __future__ import annotations
+
+import re
+from typing import Optional
+
+from ..news.types import NewsItem
+from .types import (
+    CLEARANCE,
+    CRL,
+    DIR_MET,
+    DIR_MISSED,
+    DIR_NA,
+    FDA_APPROVAL,
+    TRIAL_READOUT,
+    HCEvent,
+)
+
+# --------------------------------------------------------------------------
+# Trial readout
+# --------------------------------------------------------------------------
+
+# "Phase 1/2/3" (also "Phase 2B", "Phase 1/2") — capture the leading digit.
+_PHASE = re.compile(r"\bphase\s*([123])", re.I)
+
+# Result attached to the PRIMARY (or co-/dual primary) endpoint. Tying the
+# verb to "primary" (rather than matching "met"/"missed" anywhere) keeps the
+# reported DIRECTION faithful, which is the harmful failure mode to avoid.
+_MET_PRIMARY = re.compile(
+    r"\b(met|meets|achieved|achieves|hit|reached)\b"
+    r"(?:\s+(?:its|the|all|both|all\s+of\s+its))?"
+    r"(?:\s+(?:co-|dual|two|three|multiple))?"
+    r"\s+(?:co-)?primary\b",
+    re.I,
+)
+# Anchored to primary/endpoint so "did not meet enrollment target / guidance"
+# (with a stray Phase in the title) can't read as a failed readout.
+_MISSED_PRIMARY = re.compile(
+    r"\b(did\s+not\s+meet|does\s+not\s+meet|failed\s+to\s+meet|fails\s+to\s+meet"
+    r"|missed|misses|did\s+not\s+achieve|does\s+not\s+achieve)\b"
+    r"(?:\s+(?:its|the|both|all|all\s+of\s+its))?"
+    r"(?:\s+(?:co-|dual))?"
+    r"\s+(?:primary|co-primary|endpoint)",
+    re.I,
+)
+# A topline-data announcement with no explicit met/missed in the headline is
+# still a genuine readout crossing the wire — fired at direction n/a.
+_TOPLINE = re.compile(r"\btop-?line\b", re.I)
+
+# Future / procedural / presentation headlines that mention a Phase but are NOT
+# a readout. If ANY of these match, the trial-readout path returns None. This
+# is the primary defense for the negative test set.
+_TRIAL_NEGATIVE = re.compile(
+    r"\bpresent"          # present / presents / presented / presentation / presenting
+    r"|\bto\s+report\b|\bwill\s+report\b"
+    r"|\bto\s+announce\b|\bto\s+host\b|\bto\s+provide\b|\bto\s+release\b"
+    r"|\bto\s+webcast\b|\bwebcast\b|\bconference\s+call\b|\binvestor\s+day\b"
+    r"|\bposter\b|\bat\s+the\s+\w+.*(?:conference|congress|meeting|symposium)"
+    r"|\binitiat"          # initiate / initiation / initiates
+    r"|\bcommenc"          # commence / commences / commencement
+    r"|\bfirst[\s-]patient\b|\bdosing\s+of\b|\bdoses\s+(?:the\s+)?first\b"
+    r"|\bfirst\s+patient\s+dosed\b"
+    r"|\benroll"           # enroll / enrolls / enrolled / enrollment
+    r"|\btrial\s+design\b|\bstudy\s+design\b"
+    r"|\bto\s+begin\b|\bplans?\s+to\b|\bexpects?\s+to\b"
+    r"|\bon\s+track\b|\bdesign(?:s|ed)?\s+to\s+evaluate\b"
+    r"|\bcompletes?\s+enrollment\b"
+    # F4: future-timeline readouts ("topline data expected in Q4") are NOT a
+    # readout that happened. Requires a timeline token after expected/anticipated
+    # so "met primary, better than expected" still fires.
+    r"|\bexpected\s+(?:in|by|during|for|later|this|next|early|mid|late|soon|shortly|imminent|upcoming|coming|q[1-4]|h[12]|first|second|third|fourth|20\d\d)"
+    r"|\banticipat\w*\s+(?:in|by|during|for|later|this|next|early|mid|late|soon|shortly|imminent|upcoming|coming|q[1-4]|h[12]|20\d\d)"
+    r"|\bto\s+read\s+out\b|\breadout\s+(?:expected|anticipated|on\s+track|in|by|soon|shortly)\b",
+    re.I,
+)
+
+
+def _classify_trial(title: str, item: NewsItem) -> Optional[HCEvent]:
+    m_phase = _PHASE.search(title)
+    if not m_phase:
+        return None
+    if _TRIAL_NEGATIVE.search(title):
+        return None  # future / procedural / presentation → not a readout
+
+    missed = _MISSED_PRIMARY.search(title)
+    met = _MET_PRIMARY.search(title)
+    topline = _TOPLINE.search(title)
+
+    if missed:
+        direction = DIR_MISSED
+    elif met:
+        direction = DIR_MET
+    elif topline:
+        direction = DIR_NA
+    else:
+        return None  # Phase present but no result grammar → not a readout
+
+    return _build(item, TRIAL_READOUT, direction, phase=m_phase.group(1))
+
+
+# --------------------------------------------------------------------------
+# FDA regulatory actions
+# --------------------------------------------------------------------------
+
+# Action-verb-anchored approval grammar. Deliberately does NOT match bare
+# "FDA approval" so pending/procedural headlines ("PDUFA date for FDA approval
+# decision", "seeking FDA approval") don't fire.
+_APPROVAL = re.compile(
+    r"\bfda\s+approves\b"
+    r"|\bfda\s+approved\b"
+    r"|\bfda\s+approval\s+of\b"
+    r"|\breceive[sd]?\s+fda\s+approval\b"
+    r"|\bgranted\s+fda\s+approval\b"
+    r"|\bwins?\s+fda\s+approval\b"
+    r"|\bsecure[sd]?\s+fda\s+approval\b"
+    r"|\bgains?\s+fda\s+approval\b"
+    r"|\bfda\s+grants?\s+(?:full\s+|accelerated\s+|traditional\s+|marketing\s+)?approval\b"
+    r"|\bfda\s+has\s+granted\s+approval\b"
+    r"|\bapproved\s+by\s+the\s+fda\b",
+    re.I,
+)
+# Pending / procedural regulatory milestones that are NOT a grant. Any hit
+# suppresses BOTH approval and clearance (belt-and-suspenders atop the
+# verb-anchored regexes). Shared by the approval + clearance paths.
+_APPROVAL_NEGATIVE = re.compile(
+    r"\baccept(?:s|ed)?\b|\bpriority\s+review\b|\bfast\s+track\b"
+    r"|\bbreakthrough\s+therapy\b|\borphan\s+drug\b|\brare\s+pediatric\b"
+    r"|\badvisory\s+committee\b|\badcomm\b|\bpdufa\b"
+    r"|\bresubmi|\bseeking\b|\bawait|\bapplication\s+for\b"
+    r"|\bfiles?\s+for\b|\bsubmit|\bsubmission\b|\bto\s+request\b"
+    r"|\bcomplete\s+response\s+letter\b|\bpre-?market\s+notification\b",
+    re.I,
+)
+
+# Denial / rejection grammar (F2). A rejection must NEVER read as a grant. v1
+# has no "rejection" event type, so a plain "did not receive approval" headline
+# → None (missing it is far safer than inverting it to an approval). Gates BOTH
+# approval and clearance. NOT applied to trial readouts (which have their own
+# missed-endpoint direction) or to CRL (the rejection event we DO fire).
+_REG_DENIAL = re.compile(
+    r"\bdid\s+not\b|\bdoes\s+not\b|\bdid\s*n[’']?t\b|\bdoes\s*n[’']?t\b"
+    r"|\bfailed\s+to\b|\bfails\s+to\b"
+    r"|\bnot\s+(?:receive|receiv\w+|win|won|gain\w*|secure\w*|grant\w*|approv\w+|clear\w+)\b"
+    r"|\bdenie[sd]\b|\bdenial\b|\bdeclin\w*\s+to\s+approve\b"
+    r"|\breject(?:s|ed|ion)?\b|\bnot\s+approved\b|\bunapproved\b"
+    r"|\bwithdraw\w*\b|\brescind\w*\b|\brevoke[sd]?\b",
+    re.I,
+)
+
+# Commentary / competitor framing (F3): an issuer reacting to SOMEONE ELSE's
+# FDA action. _emit_hc_events would mis-attribute it to the commenting issuer,
+# so suppress. Accept the rare miss of a self-approval PR that also contains one
+# of these words — precision-first.
+_REG_COMMENTARY = re.compile(
+    r"\bcomment(?:s|ary|ing|ed)?\s+on\b|\bcommentary\b"
+    r"|\bcongratulat\w+|\bapplaud\w*|\bwelcome[sd]?\b|\breact[s]?\s+to\b"
+    r"|\brespond[s]?\s+to\b|\bissues?\s+statement\b"
+    r"|\bstatement\s+(?:on|regarding|re)\b",
+    re.I,
+)
+
+# CRL (F5). Must NOT match bare \bcrl\b — CRL is also the ticker for Charles
+# River Laboratories, a COVERED name ("CRL Announces … Financial Results").
+# Require the full phrase or a regulatory-CRL idiom.
+_CRL = re.compile(
+    r"\bcomplete\s+response\s+letter\b"
+    r"|\breceiv\w*\s+(?:a\s+)?crl\b"
+    r"|\bcrl\s+from\s+(?:the\s+)?fda\b"
+    r"|\bissue[sd]?\s+(?:a\s+)?crl\b"
+    r"|\bfda\s+issue[sd]?\s+(?:a\s+)?crl\b",
+    re.I,
+)
+
+# Medtech clearance. Requires a completed clearance action — bare adjective
+# "FDA-cleared" (e.g. "operates an FDA-cleared facility") must NOT match.
+_CLEARANCE = re.compile(
+    r"\bfda\s+clears\b"
+    r"|\bfda\s+clearance\b"
+    r"|\breceive[sd]?\s+fda\s+clearance\b"
+    r"|\b510\(k\)\s+clearance\b"
+    r"|\bgranted\s+510\(k\)\b"
+    r"|\bde\s+novo\s+(?:clearance|authorization|grant)\b"
+    r"|\bpma\s+approval\b"
+    r"|\bfda\s+grants?\s+(?:de\s+novo|510)\b",
+    re.I,
+)
+
+
+def _reg_suppressed(title: str) -> bool:
+    """A grant (approval or clearance) is suppressed when the headline is
+    actually a pending milestone, a denial/rejection, or third-party commentary."""
+    return bool(
+        _APPROVAL_NEGATIVE.search(title)
+        or _REG_DENIAL.search(title)
+        or _REG_COMMENTARY.search(title)
+    )
+
+
+def _classify_regulatory(title: str, item: NewsItem) -> Optional[HCEvent]:
+    # CRL first — a Complete Response Letter is unambiguous and distinct from
+    # an approval; it must never be mis-read as one.
+    if _CRL.search(title):
+        return _build(item, CRL, DIR_NA)
+    # Approval + clearance share the same suppression gate: pending milestone,
+    # denial/rejection (F2), or third-party commentary (F3). Clearance was
+    # previously ungated (F1).
+    if _reg_suppressed(title):
+        return None
+    if _APPROVAL.search(title):
+        return _build(item, FDA_APPROVAL, DIR_NA)
+    if _CLEARANCE.search(title):
+        return _build(item, CLEARANCE, DIR_NA)
+    return None
+
+
+# --------------------------------------------------------------------------
+# Public entry point
+# --------------------------------------------------------------------------
+
+
+def _build(item: NewsItem, event_type: str, direction: str,
+           *, phase: str = "") -> HCEvent:
+    return HCEvent(
+        news_id=item.news_id,
+        symbol="",  # attribution decided by the emit path (tickers ∩ universe)
+        event_type=event_type,
+        direction=direction,
+        headline=item.title,
+        source=item.source,
+        url=item.url,
+        published_at=item.published_at,
+        phase=phase,
+        confidence="high",
+        raw_industries=tuple(item.industries),
+    )
+
+
+def classify(item: NewsItem) -> Optional[HCEvent]:
+    """Classify a NewsItem into an HCEvent, or None if it isn't one.
+
+    Precision-biased: returns None on any ambiguity. Regulatory actions
+    (approval / CRL / clearance) are checked before trial readouts — an
+    approval PR carries no Phase grammar, so the two families don't collide.
+    """
+    title = item.title or ""
+    if not title.strip():
+        return None
+
+    reg = _classify_regulatory(title, item)
+    if reg is not None:
+        return reg
+
+    return _classify_trial(title, item)
diff --git a/src/events/types.py b/src/events/types.py
new file mode 100644
index 0000000..82baeae
--- /dev/null
+++ b/src/events/types.py
@@ -0,0 +1,44 @@
+"""HCEvent — a classified health-care event from the PR wire.
+
+A pure dataclass with no upward dependencies (mirrors news.types.NewsItem):
+classify() builds one from a NewsItem, and halt_monitor._emit_hc_events stamps
+the covered `symbol` per-attribution before rendering.
+"""
+from __future__ import annotations
+
+from dataclasses import dataclass
+
+# Event-type constants — the four families v1 fires (design §1).
+TRIAL_READOUT = "trial_readout"
+FDA_APPROVAL = "fda_approval"
+CRL = "crl"
+CLEARANCE = "clearance"
+
+# Direction values for trial readouts.
+DIR_MET = "met"
+DIR_MISSED = "missed"
+DIR_NA = "n/a"
+
+
+@dataclass(frozen=True)
+class HCEvent:
+    """One classified HC event.
+
+    classify() returns this with `symbol=""` (attribution is decided by the emit
+    path from the item's already-extracted tickers ∩ universe); the emit path
+    then produces a per-symbol copy via dataclasses.replace before rendering.
+
+    Dedup key (built in the emit path): f"{news_id}|{symbol}|{event_type}".
+    """
+
+    news_id: str          # == NewsItem.url (dedup + reference)
+    symbol: str           # covered ticker this event is attributed to ("" until emit)
+    event_type: str       # TRIAL_READOUT | FDA_APPROVAL | CRL | CLEARANCE
+    direction: str        # DIR_MET | DIR_MISSED | DIR_NA
+    headline: str         # NewsItem.title, verbatim (SA reproduces issuer language)
+    source: str           # "prnewswire" | "businesswire" | "globenewswire"
+    url: str
+    published_at: str     # ISO-8601 UTC
+    phase: str = ""       # "1" | "2" | "3" | "" (trial readouts only)
+    confidence: str = "high"  # high (PR-wire self-identified ticker) | medium (resolver, session 2)
+    raw_industries: tuple[str, ...] = ()
diff --git a/src/halt_monitor.py b/src/halt_monitor.py
index c6e4992..6bfe459 100644
--- a/src/halt_monitor.py
+++ b/src/halt_monitor.py
@@ -22,7 +22,7 @@ import logging
 import signal
 import sys
 import time
-from dataclasses import asdict, dataclass, field
+from dataclasses import asdict, dataclass, field, replace
 from datetime import datetime, timezone
 from pathlib import Path
 from typing import Optional
@@ -32,12 +32,14 @@ from .calendars import AnalystDayCalendar, EarningsCalendar
 from .coverage import Universe
 from .dedup import HaltTracker
 from .enrichment import build_note_context, cross_ref_match, find_followup_news
+from .events.classify import classify
 from .feeds import nasdaq, nyse
 from .feeds.types import HaltEvent
 from .news import bw, gnw, prnewswire
 from .news.cache import NewsCache
+from .news.types import NewsItem
 from .reason_codes import is_phase1_emit_code
-from .template import render_followup, render_halt, render_resume
+from .template import render_followup, render_halt, render_hc_event, render_resume
 
 logging.basicConfig(
     level=logging.INFO,
@@ -74,6 +76,7 @@ class RunStats:
     halts_emitted: int = 0
     resumes_emitted: int = 0
     followups_emitted: int = 0
+    hc_events_emitted: int = 0
     halts_filtered_out_of_universe: int = 0
     halts_filtered_non_emit_code: int = 0
     halts_enriched_with_note: int = 0
@@ -337,6 +340,116 @@ def _emit_followups(
                             note_context=f"Follow-up: {item.title}")
 
 
+def _emit_hc_events(
+    news_items: list[NewsItem],
+    universe: Universe,
+    tracker: HaltTracker,
+    log_path: Optional[Path],
+    *,
+    stats: RunStats,
+    slack_mode: str = "off",
+    slack_webhook_url: Optional[str] = None,
+) -> None:
+    """Detect + emit HC event-wire alerts (FDA actions + clinical readouts).
+
+    v1 CONSUMES the PR-wire NewsItems already fetched this poll: classify each,
+    and — for items whose issuer-embedded tickers are in the covered universe —
+    post an SA-faithful alert (design of record §4).
+
+    Attribution is HIGH-confidence by construction: the ticker was written into
+    the issuer's own release and extracted by the news parsers, so there is no
+    name-matching / misattribution risk (the openFDA sponsor→ticker resolver is
+    deferred to session 2).
+
+    Dedup key: f"{news_id}|{symbol}|{event_type}" in tracker.hc_events_emitted —
+    one alert per (press release, covered ticker, event type), ever. Post-then-
+    mark, identical gating to _emit_followups:
+      off      → stdout emit is the delivery → mark
+      live     → mark only on a successful post
+      dry-run  → render only, never post, never mark (re-testable)
+    """
+    for item in news_items:
+        ev = classify(item)
+        if ev is None:
+            continue
+        # v1 universe filter = issuer-extracted tickers ∩ covered Universe.
+        # (session 2 adds the sponsor→ticker resolver fallback here.)
+        covered = [t for t in item.tickers if universe.get(t)]
+        if not covered:
+            continue
+        for symbol in covered:
+            dedup_key = f"{item.news_id}|{symbol}|{ev.event_type}"
+            if dedup_key in tracker.hc_events_emitted:
+                continue  # one alert per (PR, ticker, type), ever
+            meta = universe.get(symbol)
+            event = replace(ev, symbol=symbol)
+
+            rendered = render_hc_event(
+                event, sector=(meta.sector if meta else ""),
+                subsector=(meta.subsector if meta else ""),
+            )
+            print()
+            print("=" * 60)
+            print(rendered)
+            print("=" * 60)
+            sys.stdout.flush()
+
+            # dry-run: render only — never post, never mark (re-testable).
+            if slack_mode == "dry-run":
+                try:
+                    slack.post_hc_event(event, meta,
+                                        webhook_url=slack_webhook_url, dry_run=True)
+                except Exception as exc:
+                    log.error("hc-event dry-run render failed for %s: %s",
+                              symbol, exc)
+                continue
+
+            posted = True
+            if slack_mode == "live":
+                try:
+                    slack.post_hc_event(event, meta,
+                                        webhook_url=slack_webhook_url, dry_run=False)
+                    stats.slack_posts_succeeded += 1
+                except Exception as exc:
+                    posted = False
+                    stats.slack_posts_failed += 1
+                    log.error("slack hc-event post failed for %s: %s", symbol, exc)
+
+            # off: stdout emit is the delivery → mark. live: mark only on post ok.
+            if posted:
+                tracker.hc_events_emitted.add(dedup_key)
+                stats.hc_events_emitted += 1
+                log.info("hc-event emitted for %s [%s/%s]: %s",
+                         symbol, event.event_type, event.direction, event.headline)
+                if log_path:
+                    _append_hc_log(log_path, event, meta)
+
+
+def _append_hc_log(log_path: Path, event, meta) -> None:
+    """Structured JSONL audit record for a delivered HC event (mirrors
+    _append_log; HCEvent has a different shape than HaltEvent so it gets its
+    own writer rather than being coerced)."""
+    log_path.parent.mkdir(parents=True, exist_ok=True)
+    record = {
+        "ts_utc": datetime.now(timezone.utc).isoformat(timespec="seconds"),
+        "kind": "hc_event",
+        "emitted": True,
+        "symbol": event.symbol,
+        "event_type": event.event_type,
+        "direction": event.direction,
+        "phase": event.phase,
+        "headline": event.headline,
+        "source": event.source,
+        "url": event.url,
+        "published_at": event.published_at,
+        "confidence": event.confidence,
+        "sector": meta.sector if meta else None,
+        "subsector": meta.subsector if meta else None,
+    }
+    with log_path.open("a", encoding="utf-8") as f:
+        f.write(json.dumps(record, ensure_ascii=False) + "\n")
+
+
 def _append_log(log_path: Path, kind: str, event: HaltEvent, meta,
                 *, emitted: bool, note_context: Optional[str] = None) -> None:
     log_path.parent.mkdir(parents=True, exist_ok=True)
@@ -375,6 +488,7 @@ def _post_health_heartbeat(stats: RunStats, slack_mode: str,
         f"halts={stats.halts_emitted}",
         f"resumes={stats.resumes_emitted}",
         f"followups={stats.followups_emitted}",
+        f"hc_events={stats.hc_events_emitted}",
         f"slack_ok/fail={stats.slack_posts_succeeded}/{stats.slack_posts_failed}",
         f"fetch_errors={stats.fetch_errors}",
     ]
@@ -420,6 +534,7 @@ def run(
     analyst_days_calendar_path: Optional[Path] = None,
     enable_news_cross_ref: bool = False,
     news_window_minutes: int = 60,
+    hc_events_enabled: bool = False,
 ) -> RunStats:
     universe = Universe(universe_path) if universe_path else Universe()
     log.info("loaded universe: %d tickers (%s)", len(universe), universe.filter_rule)
@@ -443,6 +558,8 @@ def run(
     )
     if news_cache is not None:
         log.info("news cross-ref enabled (window=%dm, sources=PRN/BW/GNW)", news_window_minutes)
+    if hc_events_enabled:
+        log.info("HC event-wire enabled (FDA actions + clinical readouts → #street-account)")
 
     if persist_state:
         state.cleanup_old()
@@ -478,20 +595,31 @@ def run(
 
             # News-feed poll (slower cadence than halt feeds — every Nth tick).
             # Run BEFORE halt emit so a halt fired this tick can cross-ref news
-            # already in the cache from this poll.
-            if news_cache is not None and stats.polls_completed % NEWS_POLL_EVERY == 0:
+            # already in the cache from this poll. Fetched when EITHER the
+            # cross-ref cache OR the HC event-wire is enabled (both consume the
+            # same freshly-fetched news_items).
+            if ((news_cache is not None or hc_events_enabled)
+                    and stats.polls_completed % NEWS_POLL_EVERY == 0):
                 news_items: list = []
                 for label, mod in [("news_prnewswire", prnewswire),
                                     ("news_businesswire", bw),
                                     ("news_globenewswire", gnw)]:
                     news_items.extend(_safe_fetch(label, mod.fetch, stats, health, slack_mode))
-                added = news_cache.ingest(news_items)
-                stats.news_polls_completed += 1
-                stats.news_items_ingested += added
-                # tickers_indexed reflects items currently in the lookback
-                # window (i.e. cross-refable). news_items_ingested is the
-                # cumulative count for dedup auditing.
-                stats.news_cache_size = news_cache.tickers_indexed
+                if news_cache is not None:
+                    added = news_cache.ingest(news_items)
+                    stats.news_polls_completed += 1
+                    stats.news_items_ingested += added
+                    # tickers_indexed reflects items currently in the lookback
+                    # window (i.e. cross-refable). news_items_ingested is the
+                    # cumulative count for dedup auditing.
+                    stats.news_cache_size = news_cache.tickers_indexed
+                # HC event-wire: classify the freshly-fetched PR-wire items and
+                # emit alerts for covered names (additive; does not touch the
+                # halt/resume/follow-up paths above).
+                if hc_events_enabled:
+                    _emit_hc_events(news_items, universe, tracker, log_path,
+                                    stats=stats, slack_mode=slack_mode,
+                                    slack_webhook_url=slack_webhook_url)
 
             for kind, event in tracker.ingest(nasdaq_events + nyse_events):
                 _emit(kind, event, universe, log_path,
@@ -573,6 +701,9 @@ def main(argv: Optional[list[str]] = None) -> int:
                         help="enable PRN/BW/GNW news cross-ref enrichment (Phase 2 slice 2B)")
     parser.add_argument("--news-window-minutes", type=int, default=60,
                         help="lookback window for news cross-ref (default 60min)")
+    parser.add_argument("--hc-events", action="store_true",
+                        help="enable the HC event-wire lane (FDA approvals/CRLs + "
+                             "clinical-trial readouts on covered names → #street-account)")
     args = parser.parse_args(argv)
 
     stats = run(
@@ -584,6 +715,7 @@ def main(argv: Optional[list[str]] = None) -> int:
         analyst_days_calendar_path=args.analyst_days_calendar,
         enable_news_cross_ref=args.news_cross_ref,
         news_window_minutes=args.news_window_minutes,
+        hc_events_enabled=args.hc_events,
     )
     print(json.dumps(asdict(stats), indent=2))
     return 0
diff --git a/src/slack.py b/src/slack.py
index 03d22e5..5bf4f97 100644
--- a/src/slack.py
+++ b/src/slack.py
@@ -19,9 +19,19 @@ from typing import Optional
 import requests
 
 from .coverage import TickerMeta
+from .events.types import (
+    CLEARANCE,
+    CRL,
+    DIR_MET,
+    DIR_MISSED,
+    FDA_APPROVAL,
+    TRIAL_READOUT,
+    HCEvent,
+)
 from .feeds.types import HaltEvent
 from .news.types import NewsItem
 from .template import (
+    _NEWS_SOURCE_LABELS,
     _format_date_short,
     _format_price,
     _format_time_hhmm,
@@ -29,6 +39,9 @@ from .template import (
     followup_headline,
     followup_source_label,
     followup_when,
+    hc_event_cta_context,
+    hc_event_headline,
+    hc_event_when,
     is_biotech,
 )
 
@@ -182,6 +195,69 @@ def build_followup_blocks(halt: HaltEvent, item: NewsItem,
     }
 
 
+def _hc_event_emoji(event: HCEvent) -> str:
+    """Event-type emoji per design §5. Trial readouts split on direction so a
+    'met' and a 'missed' are visually distinct at a glance."""
+    if event.event_type == FDA_APPROVAL:
+        return ":pill:"
+    if event.event_type == CRL:
+        return ":x:"
+    if event.event_type == CLEARANCE:
+        return ":heavy_check_mark:"
+    if event.event_type == TRIAL_READOUT:
+        if event.direction == DIR_MISSED:
+            return ":small_red_triangle_down:"
+        if event.direction == DIR_MET:
+            return ":test_tube:"
+        return ":microscope:"  # topline, direction TBD
+    return ":pill:"
+
+
+def build_hc_event_blocks(event: HCEvent, meta: Optional[TickerMeta]) -> dict:
+    """Block Kit for an HC event-wire alert — an FDA action or clinical readout
+    on a covered name (design §5, templates §15/§16/§19). Single-section mrkdwn,
+    mirroring build_followup_blocks. Headline is the issuer's verbatim title;
+    the fact-dense efficacy body is a deferred LLM enhancement."""
+    symbol = event.symbol
+    headline = hc_event_headline(event.headline)
+    time_hhmm, date_short = hc_event_when(event.published_at)
+
+    header = f"{_hc_event_emoji(event)} *SA:* `{symbol}` — {headline}"
+
+    when = f"`{time_hhmm} ET {date_short}`" if time_hhmm else ""
+    detail_bits = [when] if when else []
+    if meta and meta.sector:
+        sector_str = meta.sector
+        if meta.subsector:
+            sector_str += f" / {meta.subsector}"
+        detail_bits.append(f"Sector: {sector_str}")
+    detail = "  ·  ".join(detail_bits)
+
+    label = _NEWS_SOURCE_LABELS.get(event.source, event.source)
+    source_line = f"_source: {label} press release"
+    if event.url:
+        source_line += f" · {event.url}"
+    source_line += "_"
+
+    lines = [header]
+    if detail:
+        lines.append(detail)
+    if meta and is_biotech(meta.subsector):
+        lines.append(biotech_triage_cta(symbol, context=hc_event_cta_context(event)))
+    lines.append(source_line)
+    text_block = "\n".join(lines)
+    fallback = f"SA: {symbol} — {headline}"
+    return {
+        "blocks": [
+            {
+                "type": "section",
+                "text": {"type": "mrkdwn", "text": text_block[:SLACK_SECTION_TEXT_LIMIT]},
+            }
+        ],
+        "text": fallback,
+    }
+
+
 def build_dm_blocks(message: str, *, level: str = "warning") -> dict:
     emoji = {"ok": ":white_check_mark:", "warning": ":warning:", "error": ":x:"}.get(
         level, ":warning:"
@@ -258,6 +334,18 @@ def post_followup(halt: HaltEvent, item: NewsItem, meta: Optional[TickerMeta], *
     return payload
 
 
+def post_hc_event(event: HCEvent, meta: Optional[TickerMeta], *,
+                  webhook_url: Optional[str] = None,
+                  dry_run: bool = False) -> Optional[dict]:
+    payload = build_hc_event_blocks(event, meta)
+    if dry_run:
+        log.info("slack[dry-run] hc_event payload for %s:\n%s", event.symbol,
+                 json.dumps(payload, indent=2))
+        return payload
+    post_payload(payload, webhook_url=webhook_url)
+    return payload
+
+
 def post_dm(message: str, *, level: str = "warning",
             webhook_url: Optional[str] = None,
             dry_run: bool = False) -> Optional[dict]:
diff --git a/src/state.py b/src/state.py
index baaa029..dd4b19f 100644
--- a/src/state.py
+++ b/src/state.py
@@ -64,6 +64,7 @@ def save(tracker: HaltTracker, *, state_dir: Path = DEFAULT_STATE_DIR,
         "resumes_emitted": [list(hid) for hid in tracker.resumes_emitted],
         "emitted_halts": [list(hid) for hid in tracker.emitted_halts],
         "followed_up": [list(hid) for hid in tracker.followed_up],
+        "hc_events_emitted": sorted(tracker.hc_events_emitted),
     }
 
     # Atomic write: tmp file → rename
@@ -106,6 +107,8 @@ def load(*, state_dir: Path = DEFAULT_STATE_DIR, day_key: str = "") -> HaltTrack
     # simply lack them and rehydrate to empty sets.
     emitted_halts = {tuple(e) for e in payload.get("emitted_halts", [])}
     followed_up = {tuple(f) for f in payload.get("followed_up", [])}
+    # HC event-wire delivery-dedup — string keys, not HaltId tuples.
+    hc_events_emitted = set(payload.get("hc_events_emitted", []))
 
     # Pre-fill seen_halts with placeholder HaltEvents — we only need the
     # halt_ids to be populated so dedup works; the full event history isn't
@@ -124,9 +127,12 @@ def load(*, state_dir: Path = DEFAULT_STATE_DIR, day_key: str = "") -> HaltTrack
     tracker.resumes_emitted = resumes
     tracker.emitted_halts = emitted_halts
     tracker.followed_up = followed_up
+    tracker.hc_events_emitted = hc_events_emitted
     log.info(
-        "state: rehydrated from %s (%d halts, %d resumes, %d emitted, %d followed_up)",
+        "state: rehydrated from %s (%d halts, %d resumes, %d emitted, "
+        "%d followed_up, %d hc_events)",
         path, len(halts), len(resumes), len(emitted_halts), len(followed_up),
+        len(hc_events_emitted),
     )
     return tracker
 
diff --git a/src/template.py b/src/template.py
index 3c22d2e..ca73c53 100644
--- a/src/template.py
+++ b/src/template.py
@@ -68,14 +68,18 @@ def is_biotech(subsector: str) -> bool:
     return (subsector or "").strip().casefold() == "biotech"
 
 
-def biotech_triage_cta(symbol: str) -> str:
-    """Lean halt->triage hand-off nudge for a biotech halt (option A, 2026-06-16).
+def biotech_triage_cta(symbol: str, context: str = "halt") -> str:
+    """Lean ->triage hand-off nudge for a biotech name (option A, 2026-06-16).
 
     sa-monitor can't auto-invoke the Claude-driven biotech_triage, so instead of
     pretending to trigger it we surface a copy-pasteable command. See the root
     biotech_catalyst_architecture_plan.md (§3) for why this is human-mediated.
+
+    `context` names what triggered the nudge — "halt" (default, preserves the
+    halt/resume wording verbatim) or an HC-event label ("readout"/"approval"/…)
+    so a non-halt HC event doesn't misleadingly read "Biotech halt".
     """
-    return f":dna: Biotech halt — triage this name?  ->  `triage {symbol}`"
+    return f":dna: Biotech {context} — triage this name?  ->  `triage {symbol}`"
 
 
 def render_halt(event: HaltEvent, *, sector: str = "", subsector: str = "",
@@ -208,6 +212,78 @@ def render_followup(halt: HaltEvent, item: NewsItem, *, sector: str = "",
     return "\n".join(parts)
 
 
+def hc_event_when(published_at: str) -> tuple[str, str]:
+    """(time_hhmm, date_short) in ET for an HC event, from its PR publish time.
+
+    Falls back to ("", "") if the timestamp is missing/malformed — the caller
+    then simply omits the stamp rather than printing garbage.
+    """
+    et = _iso_utc_to_et(published_at)
+    if et is None:
+        return ("", "")
+    return (f"{et.hour:02d}:{et.minute:02d}",
+            f"{et.month}/{et.day:02d}/{et.year % 100:02d}")
+
+
+def hc_event_headline(headline: str) -> str:
+    """The event headline, verbatim from the issuer, capped for readability.
+
+    SA reproduces issuer language; v1 delivers the issuer's own title (the
+    fact-dense §15/§19 efficacy body is a deferred LLM enhancement).
+    """
+    headline = (headline or "").strip()
+    if len(headline) > _FOLLOWUP_HEADLINE_LIMIT:
+        headline = headline[:_FOLLOWUP_HEADLINE_LIMIT].rsplit(" ", 1)[0] + "…"
+    return headline
+
+
+# Human label for an HC event type, used in the biotech triage CTA so it reads
+# "Biotech readout — triage this name?" rather than the halt path's "Biotech halt".
+_HC_CTA_CONTEXT = {
+    "trial_readout": "readout",
+    "fda_approval": "approval",
+    "crl": "CRL",
+    "clearance": "clearance",
+}
+
+
+def hc_event_cta_context(event) -> str:
+    """CTA context word for an HCEvent (falls back to a generic 'event')."""
+    return _HC_CTA_CONTEXT.get(event.event_type, "event")
+
+
+def render_hc_event(event, *, sector: str = "", subsector: str = "") -> str:
+    """Render an HC event-wire alert to plain text (stdout / --slack off).
+
+    event is an events.types.HCEvent. Mirrors render_followup's shape:
+
+        HH:MM ET M/DD/YY [StreetAccount] TICKER {verbatim issuer headline}
+        Sector: {sector} / {subsector}
+        [biotech triage CTA if biotech]
+        Source: {Wire} press release · {url}
+    """
+    time_hhmm, date_short = hc_event_when(event.published_at)
+    headline = hc_event_headline(event.headline)
+
+    when = f"{time_hhmm} ET {date_short} ".lstrip() if time_hhmm else ""
+    header = f"{when}[StreetAccount] {event.symbol} {headline}"
+
+    parts = [header]
+    if sector:
+        sector_line = f"Sector: {sector}"
+        if subsector:
+            sector_line += f" / {subsector}"
+        parts.append(sector_line)
+    if is_biotech(subsector):
+        parts.append(biotech_triage_cta(event.symbol, context=hc_event_cta_context(event)))
+    label = _NEWS_SOURCE_LABELS.get(event.source, event.source)
+    source_line = f"Source: {label} press release"
+    if event.url:
+        source_line += f" · {event.url}"
+    parts.append(source_line)
+    return "\n".join(parts)
+
+
 def render_resume(event: HaltEvent, *, sector: str = "", subsector: str = "",
                   name_override: str = "") -> str:
     """Render a resume notification — sa-monitor variant.
diff --git a/tests/test_hc_events.py b/tests/test_hc_events.py
new file mode 100644
index 0000000..b640d42
--- /dev/null
+++ b/tests/test_hc_events.py
@@ -0,0 +1,399 @@
+"""Tests for the HC event-wire lane — classify() + _emit_hc_events.
+
+Two halves:
+
+1. classify() precision — a POSITIVE set anchored to the locked real headlines
+   in template-library.md §15/§16/§19, and a NEGATIVE set of headlines that
+   must NOT fire (presentations, trial initiation/enrollment, earnings/
+   financing/personnel PRs, pending FDA milestones, boilerplate "FDA-cleared").
+   A wrong "Phase 3 met" / "FDA approval" alert is worse than a miss, so the
+   NEGATIVE set is the load-bearing guard.
+
+2. _emit_hc_events lifecycle — detection, covered-only filter, dedup, and the
+   slack-mode delivery gate (off marks, live marks only on post success,
+   dry-run marks nothing), mirroring tests/test_followup.py.
+"""
+from datetime import datetime, timezone
+from unittest.mock import patch
+
+import pytest
+
+from src import state
+from src.coverage import TickerMeta
+from src.dedup import HaltTracker
+from src.events.classify import classify
+from src.events.types import (
+    CLEARANCE,
+    CRL,
+    DIR_MET,
+    DIR_MISSED,
+    DIR_NA,
+    FDA_APPROVAL,
+    TRIAL_READOUT,
+)
+from src.halt_monitor import RunStats, _emit_hc_events
+from src.news.types import NewsItem
+
+
+# --------------------------------------------------------------------------
+# Fixtures / helpers
+# --------------------------------------------------------------------------
+
+class StubUniverse:
+    """Minimal Universe stand-in — _emit_hc_events only calls .get()."""
+
+    def __init__(self, mapping):
+        self._m = {k.upper(): v for k, v in mapping.items()}
+
+    def get(self, symbol):
+        return self._m.get(symbol.upper())
+
+
+def _meta(symbol="VRDN", subsector="Biotech"):
+    return TickerMeta(symbol=symbol, name=f"{symbol} Inc",
+                      sector="Biopharma", subsector=subsector)
+
+
+def _item(title, *, tickers=("VRDN",), source="prnewswire",
+          url=None, pub="2026-05-05T11:05:00+00:00"):
+    return NewsItem(
+        source=source, title=title, body="body text",
+        url=url or f"https://prn.test/{abs(hash(title)) % 10**8}",
+        published_at=pub, tickers=tickers,
+    )
+
+
+# --------------------------------------------------------------------------
+# 1a. classify() — POSITIVE set (anchored to §15 / §16 / §19)
+# --------------------------------------------------------------------------
+
+POSITIVE = [
+    # (title, event_type, direction, phase)
+    # §16 FDA approval — issuer-press-release variant (ARVN 2026-05-01 real capture)
+    ("Arvinas issues press release on FDA approval of VEPPANU (vepdegestrant) "
+     "for the treatment of ESR1M, ER+/HER2- advanced breast cancer",
+     FDA_APPROVAL, DIR_NA, ""),
+    # §16 FDA approval — headline variant
+    ("FDA approves Veppanu for ESR1M, ER+/HER2- advanced breast cancer",
+     FDA_APPROVAL, DIR_NA, ""),
+    ("Acme Pharma Receives FDA Approval for Acmezumab in metastatic melanoma",
+     FDA_APPROVAL, DIR_NA, ""),
+    ("FDA grants accelerated approval of Novadrug for rare genetic disorder",
+     FDA_APPROVAL, DIR_NA, ""),
+    # §19 trial readout — MET (VRDN 2026-05-05 real capture)
+    ("Viridian reports Phase 3 REVEAL-2 trial of elegrobart in chronic thyroid "
+     "eye disease met its primary endpoint",
+     TRIAL_READOUT, DIR_MET, "3"),
+    # §19 met dual primary
+    ("Cytokinetics reports ACACIA-HCM Phase 3 met dual primary endpoints",
+     TRIAL_READOUT, DIR_MET, "3"),
+    ("Newco achieves primary endpoint in Phase 2 study of newzumab",
+     TRIAL_READOUT, DIR_MET, "2"),
+    # §15 trial readout — MISSED (DAZALS / CEDAR real phrasings)
+    ("Corcept reports Phase 2 DAZALS study of dazucorilant does not meet "
+     "primary endpoint",
+     TRIAL_READOUT, DIR_MISSED, "2"),
+    ("Bigco reports Phase 2B CEDAR study of drugx did not meet its primary or "
+     "secondary efficacy endpoints",
+     TRIAL_READOUT, DIR_MISSED, "2"),
+    ("Smallco announces Phase 3 pivotal trial failed to meet primary endpoint",
+     TRIAL_READOUT, DIR_MISSED, "3"),
+    # topline, direction TBD (still a genuine readout crossing)
+    ("Genco announces topline results from Phase 3 SUNRISE trial of gencept",
+     TRIAL_READOUT, DIR_NA, "3"),
+    # CRL
+    ("Regenco receives Complete Response Letter from FDA for regzumab",
+     CRL, DIR_NA, ""),
+    # medtech clearance
+    ("Deviceco receives FDA clearance for its AI-powered cardiac monitor",
+     CLEARANCE, DIR_NA, ""),
+    ("Deviceco announces FDA 510(k) clearance of its next-gen catheter",
+     CLEARANCE, DIR_NA, ""),
+    # F5: idiomatic CRL still fires (only the BARE-token/ticker collision is fixed)
+    ("FDA issues CRL for acmezumab citing manufacturing deficiencies",
+     CRL, DIR_NA, ""),
+    ("Acme receives a CRL from the FDA for acmezumab",
+     CRL, DIR_NA, ""),
+    # F4 guard: a real MET readout that merely contains the word "expected"
+    # (not a future timeline) must STILL fire
+    ("Acme reports Phase 3 trial met primary endpoint, better than expected",
+     TRIAL_READOUT, DIR_MET, "3"),
+]
+
+
+@pytest.mark.parametrize("title,etype,direction,phase", POSITIVE)
+def test_classify_positive(title, etype, direction, phase):
+    ev = classify(_item(title))
+    assert ev is not None, f"expected an event, got None for: {title}"
+    assert ev.event_type == etype
+    assert ev.direction == direction
+    assert ev.phase == phase
+    assert ev.headline == title          # verbatim
+    assert ev.symbol == ""               # attribution decided in the emit path
+    assert ev.confidence == "high"
+
+
+# --------------------------------------------------------------------------
+# 1b. classify() — NEGATIVE set (must ALL return None)
+# --------------------------------------------------------------------------
+
+NEGATIVE = [
+    # presentations / future data disclosure (not a readout)
+    "Acme to present Phase 3 REVEAL data at the ASCO 2026 annual conference",
+    "Acme will present detailed Phase 2 results at an upcoming medical congress",
+    "Acme presents Phase 3 topline data in a late-breaking poster presentation",
+    "Acme to present topline Phase 2 data next week",
+    # trial lifecycle, not a readout
+    "Acme announces initiation of Phase 3 pivotal trial of acmezumab",
+    "Acme doses first patient in Phase 3 study of acmezumab",
+    "Acme completes enrollment in Phase 3 ELEVATE trial",
+    "Acme to host conference call to discuss Phase 3 trial design",
+    "Acme enrolls first patient in Phase 2 basket trial",
+    # non-clinical PRs (no readout, no approval)
+    "Acme reports first quarter 2026 financial results",
+    "Acme announces $150 million underwritten public offering",
+    "Acme appoints Jane Doe as Chief Executive Officer",
+    "Acme to participate in the Phase Capital healthcare investor conference",
+    # pending / procedural FDA milestones (NOT an approval)
+    "FDA accepts Acme's New Drug Application for acmezumab",
+    "FDA grants Priority Review to Acme's application for acmezumab",
+    "FDA grants Fast Track designation to acmezumab",
+    "FDA grants Breakthrough Therapy designation for acmezumab",
+    "Acme submits Biologics License Application to FDA seeking approval of acmezumab",
+    "Acme resubmits application for FDA approval of acmezumab following prior review",
+    # boilerplate adjective — must NOT read as a clearance event
+    "Acme opens new FDA-cleared manufacturing facility in North Carolina",
+    # ambiguous secondary-only readout — precision says skip
+    "Acme reports Phase 2 study met key secondary endpoint",
+
+    # --- Codex round-1 repros ------------------------------------------------
+    # F1: clearance path was UNGATED — pending / denial framings must not fire
+    "Acme submits 510(k) for FDA clearance of its cardiac monitor",
+    "Acme did not receive FDA clearance for its cardiac monitor",
+    "Acme fails to receive FDA clearance for its device",
+    "FDA denies 510(k) clearance for Acme's device",
+    "Acme withdraws 510(k) clearance application for its device",
+    # F2: negative-approval INVERSION — a rejection must never read as approval
+    "Acme did not receive FDA approval for acmezumab",
+    "Acme fails to win FDA approval for acmezumab",
+    "FDA rejects Acme's application; acmezumab not approved",
+    "Acme's acmezumab did not win FDA approval this cycle",
+    # F3: competitor / commentary approval attribution
+    "Acme comments on FDA approval of RivalDrug by a competitor",
+    "Acme issues statement on FDA approval of a rival therapy",
+    "Acme congratulates partner on FDA approval of RivalDrug",
+    # F4: future 'expected in' topline (adjectival, not caught by 'expects to')
+    "Acme announces topline Phase 3 data expected in Q4 2026",
+    "Acme Phase 3 SUNRISE topline readout expected in the second half of 2026",
+    "Acme on track to read out Phase 3 data later this year",
+    # F5: bare CRL collides with Charles River Labs (ticker CRL, covered)
+    "CRL Announces First-Quarter 2026 Financial Results",
+    "Charles River Laboratories (CRL) Announces New Facility in Massachusetts",
+]
+
+
+@pytest.mark.parametrize("title", NEGATIVE)
+def test_classify_negative(title):
+    assert classify(_item(title)) is None, f"MISFIRE: {title!r} should not classify"
+
+
+def test_classify_blank_title_is_none():
+    assert classify(_item("   ")) is None
+
+
+# --------------------------------------------------------------------------
+# F6 — biotech CTA wording: HC events must NOT say "Biotech halt"
+# --------------------------------------------------------------------------
+
+def test_hc_event_cta_does_not_say_halt(capsys):
+    from src import slack
+    from dataclasses import replace
+    item = _item("Viridian reports Phase 3 REVEAL-2 met its primary endpoint",
+                 tickers=("VRDN",))
+    universe = StubUniverse({"VRDN": _meta("VRDN")})
+    tracker = HaltTracker()
+    _run_emit([item], universe, tracker)
+    out = capsys.readouterr().out
+    assert "Biotech readout" in out            # event-appropriate wording
+    assert "Biotech halt" not in out           # F6: not the halt wording
+
+    # Slack block carries the same corrected wording.
+    ev = replace(classify(item), symbol="VRDN")
+    blocks = slack.build_hc_event_blocks(ev, _meta("VRDN"))
+    text = blocks["blocks"][0]["text"]["text"]
+    assert "Biotech readout" in text and "Biotech halt" not in text
+
+
+def test_biotech_triage_cta_halt_wording_unchanged():
+    """The halt/resume path must still read 'Biotech halt' (default context)."""
+    from src.template import biotech_triage_cta
+    assert biotech_triage_cta("VRDN") == \
+        ":dna: Biotech halt — triage this name?  ->  `triage VRDN`"
+    assert biotech_triage_cta("VRDN", context="approval").startswith(
+        ":dna: Biotech approval —")
+
+
+# --------------------------------------------------------------------------
+# 2. _emit_hc_events — lifecycle / gating
+# --------------------------------------------------------------------------
+
+def _run_emit(news_items, universe, tracker, *, slack_mode="off"):
+    stats = RunStats(started_at="t")
+    _emit_hc_events(news_items, universe, tracker, None,
+                    stats=stats, slack_mode=slack_mode)
+    return stats
+
+
+def test_covered_readout_emits_and_renders(capsys):
+    item = _item("Viridian reports Phase 3 REVEAL-2 trial of elegrobart in "
+                 "chronic thyroid eye disease met its primary endpoint",
+                 tickers=("VRDN",))
+    universe = StubUniverse({"VRDN": _meta("VRDN")})
+    tracker = HaltTracker()
+
+    stats = _run_emit([item], universe, tracker)
+    assert stats.hc_events_emitted == 1
+    key = f"{item.news_id}|VRDN|{TRIAL_READOUT}"
+    assert key in tracker.hc_events_emitted
+    out = capsys.readouterr().out
+    assert "VRDN" in out
+    assert "met its primary endpoint" in out
+    assert "[StreetAccount]" in out
+
+
+def test_negative_item_emits_nothing(capsys):
+    item = _item("Acme to present Phase 3 REVEAL data at the ASCO 2026 "
+                 "annual conference", tickers=("VRDN",))
+    universe = StubUniverse({"VRDN": _meta("VRDN")})
+    tracker = HaltTracker()
+
+    stats = _run_emit([item], universe, tracker)
+    assert stats.hc_events_emitted == 0
+    assert tracker.hc_events_emitted == set()
+    assert capsys.readouterr().out.strip() == ""
+
+
+def test_uncovered_ticker_never_fires():
+    item = _item("FDA approves Zzzdrug for advanced disease", tickers=("ZZZZ",))
+    universe = StubUniverse({"VRDN": _meta("VRDN")})  # ZZZZ not covered
+    tracker = HaltTracker()
+
+    stats = _run_emit([item], universe, tracker)
+    assert stats.hc_events_emitted == 0
+    assert tracker.hc_events_emitted == set()
+
+
+def test_item_with_no_tickers_never_fires():
+    item = _item("FDA approves Somedrug for advanced disease", tickers=())
+    universe = StubUniverse({"VRDN": _meta("VRDN")})
+    tracker = HaltTracker()
+
+    stats = _run_emit([item], universe, tracker)
+    assert stats.hc_events_emitted == 0
+
+
+def test_partner_ticker_covered_fires_for_partner():
+    # Small biotech's approval names its covered large-pharma partner — both
+    # tickers land in item.tickers; the covered one fires.
+    item = _item("Arvinas issues press release on FDA approval of VEPPANU for "
+                 "the treatment of ER+/HER2- advanced breast cancer",
+                 tickers=("ARVN", "PFE"))
+    universe = StubUniverse({"PFE": _meta("PFE", subsector="Pharma")})  # only PFE covered
+    tracker = HaltTracker()
+
+    stats = _run_emit([item], universe, tracker)
+    assert stats.hc_events_emitted == 1
+    assert f"{item.news_id}|PFE|{FDA_APPROVAL}" in tracker.hc_events_emitted
+
+
+def test_both_covered_tickers_each_fire_once():
+    item = _item("Arvinas issues press release on FDA approval of VEPPANU",
+                 tickers=("ARVN", "PFE"))
+    universe = StubUniverse({"ARVN": _meta("ARVN"), "PFE": _meta("PFE")})
+    tracker = HaltTracker()
+
+    stats = _run_emit([item], universe, tracker)
+    assert stats.hc_events_emitted == 2
+    assert f"{item.news_id}|ARVN|{FDA_APPROVAL}" in tracker.hc_events_emitted
+    assert f"{item.news_id}|PFE|{FDA_APPROVAL}" in tracker.hc_events_emitted
+
+
+def test_dedup_one_alert_per_pr_ticker_type_ever():
+    item = _item("FDA approves Veppanu for advanced breast cancer", tickers=("VRDN",))
+    universe = StubUniverse({"VRDN": _meta("VRDN")})
+    tracker = HaltTracker()
+
+    first = _run_emit([item], universe, tracker)
+    second = _run_emit([item], universe, tracker)  # same item re-fetched
+    assert first.hc_events_emitted == 1
+    assert second.hc_events_emitted == 0
+    assert len(tracker.hc_events_emitted) == 1
+
+
+def test_dry_run_renders_but_marks_nothing(capsys):
+    item = _item("FDA approves Veppanu for advanced breast cancer", tickers=("VRDN",))
+    universe = StubUniverse({"VRDN": _meta("VRDN")})
+    tracker = HaltTracker()
+
+    stats = _run_emit([item], universe, tracker, slack_mode="dry-run")
+    assert stats.hc_events_emitted == 0          # nothing delivered
+    assert tracker.hc_events_emitted == set()    # re-testable — no mark
+    assert "VRDN" in capsys.readouterr().out     # but it DID render
+
+
+def test_live_post_failure_does_not_mark():
+    item = _item("FDA approves Veppanu for advanced breast cancer", tickers=("VRDN",))
+    universe = StubUniverse({"VRDN": _meta("VRDN")})
+    tracker = HaltTracker()
+
+    with patch("src.slack.post_hc_event", side_effect=RuntimeError("slack down")):
+        stats = _run_emit([item], universe, tracker, slack_mode="live")
+    assert stats.hc_events_emitted == 0
+    assert stats.slack_posts_failed == 1
+    assert tracker.hc_events_emitted == set()   # post failed → not marked → retries next poll
+
+
+def test_live_post_success_marks():
+    item = _item("FDA approves Veppanu for advanced breast cancer", tickers=("VRDN",))
+    universe = StubUniverse({"VRDN": _meta("VRDN")})
+    tracker = HaltTracker()
+
+    with patch("src.slack.post_hc_event", return_value={"ok": True}) as m:
+        stats = _run_emit([item], universe, tracker, slack_mode="live")
+    assert m.called
+    assert stats.hc_events_emitted == 1
+    assert f"{item.news_id}|VRDN|{FDA_APPROVAL}" in tracker.hc_events_emitted
+
+
+# --------------------------------------------------------------------------
+# 3. dedup / state round-trip for hc_events_emitted
+# --------------------------------------------------------------------------
+
+def test_reset_clears_hc_events():
+    tracker = HaltTracker()
+    tracker.hc_events_emitted.add("u|VRDN|fda_approval")
+    tracker.reset()
+    assert tracker.hc_events_emitted == set()
+
+
+def test_hc_events_emitted_round_trip(tmp_path):
+    tracker = HaltTracker()
+    tracker.hc_events_emitted.add("https://prn.test/1|VRDN|trial_readout")
+    tracker.hc_events_emitted.add("https://prn.test/2|ARVN|fda_approval")
+    state.save(tracker, state_dir=tmp_path, day_key="2026-05-05")
+
+    fresh = state.load(state_dir=tmp_path, day_key="2026-05-05")
+    assert fresh.hc_events_emitted == {
+        "https://prn.test/1|VRDN|trial_readout",
+        "https://prn.test/2|ARVN|fda_approval",
+    }
+
+
+def test_old_state_without_hc_field_rehydrates_empty(tmp_path):
+    path = tmp_path / "dedup_state_2026-05-05.json"
+    path.write_text(
+        '{"schema_version": 1, "trading_day_et": "2026-05-05", '
+        '"halts": [], "resumes_emitted": []}'
+    )
+    fresh = state.load(state_dir=tmp_path, day_key="2026-05-05")
+    assert fresh.hc_events_emitted == set()
```
codex
I’ll re-review against the actual workspace, not just the pasted diff, then run the focused tests if the read-only sandbox allows it. I’m going to start by locating the classifier, template, Slack, and test files so line references are exact.
exec
/bin/bash -lc 'git status --short' in /mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor
exec
/bin/bash -lc 'git diff -- src/events/classify.py src/template.py src/slack.py src/halt_monitor.py src/dedup.py src/state.py tests/test_hc_events.py' in /mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor
exec
/bin/bash -lc 'rg --files' in /mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor
 succeeded in 0ms:
README.md
PROJECT_BRIEF.md
PHASE3_PLAN.md
phase1-data-sources.md
template-library.md
coverage-manager-issue-draft.md
SESSION_STATE.md
requirements.txt
src/halt_monitor.py
src/slack.py
src/reason_codes.py
src/template.py
src/state.py
src/__init__.py
src/dedup.py
src/coverage.py
src/calendars.py
src/enrichment.py
codex_feedback/codex_feedback_2026-07-13_001017.md
codex_feedback/codex_feedback_2026-07-13_000252.md
codex_feedback/codex_feedback_2026-07-12_235153.md
codex_feedback/codex_feedback_2026-07-13_004223.md
codex_feedback/codex_feedback_2026-07-13_005307.md
logs/halt_monitor_2026-06-11_am.jsonl
logs/halt_monitor_2026-07-01_am.jsonl
logs/halt_monitor_2026-06-10_am.jsonl
logs/halt_monitor_2026-06-02_pm.jsonl
logs/halt_monitor_2026-05-22_am.jsonl
logs/halt_monitor_2026-05-15_am.jsonl
logs/halt_monitor_2026-05-13_am.jsonl
logs/halt_monitor_2026-05-11_am.jsonl
logs/halt_monitor_2026-06-24_am.jsonl
logs/halt_monitor_2026-06-25_am.jsonl
logs/halt_monitor_2026-07-06_am.jsonl
logs/halt_monitor_2026-07-03_am.jsonl
logs/halt_monitor_20260623_225250.jsonl
logs/halt_monitor_2026-06-30_am.jsonl
tests/test_news_cache.py
tests/test_dedup.py
tests/test_hc_events.py
tests/test_followup.py
tests/test_feeds.py
tests/test_enrichment.py
tests/test_slack.py
tests/test_reason_codes.py
tests/test_news_parsers.py
tests/test_news_feeds.py
tests/test_state.py
tests/test_smoke.py
tests/test_template.py
tests/__init__.py
scripts/gh_issues.py
scripts/ci_run.sh
scripts/build_universe.py
scripts/one_shot_deploy.sh
scripts/_codex_hc_events_review.md
tests/test_calendars.py
tests/test_coverage.py
data/sa_monitor_universe.json
state/dedup_state_2026-07-07.json
state/dedup_state_2026-07-06.json
state/dedup_state_2026-07-03.json
state/dedup_state_2026-07-09.json
state/dedup_state_2026-07-08.json
state/dedup_state_2026-07-10.json
src/events/types.py
src/events/classify.py
src/events/__init__.py
src/news/parsers.py
src/news/gnw.py
src/news/cache.py
src/news/bw.py
src/news/types.py
src/news/prnewswire.py
src/news/__init__.py
src/feeds/nyse.py
src/feeds/nasdaq.py
src/feeds/__init__.py
src/feeds/types.py
tests/fixtures/nasdaq_rss_sample.xml
tests/fixtures/nyse_csv_sample.csv
tests/fixtures/news/gnw_health_sample.xml
tests/fixtures/news/bw_sample.xml
tests/fixtures/news/prn_health_sample.xml

 succeeded in 234ms:
diff --git a/src/dedup.py b/src/dedup.py
index 94a7b65..8b36303 100644
--- a/src/dedup.py
+++ b/src/dedup.py
@@ -43,6 +43,12 @@ class HaltTracker:
     # ever. Persisted alongside seen_halts so a mid-session restart never
     # re-emits a follow-up.
     followed_up: set[HaltId] = field(default_factory=set)
+    # Delivery-dedup for the HC event-wire lane. Keys are strings of the form
+    # f"{news_id}|{symbol}|{event_type}" — one alert per (press release,
+    # covered ticker, event type), ever. Persisted like followed_up so a
+    # mid-session restart never re-emits an HC event. (Mirrors emitted_halts /
+    # followed_up exactly; unlike them, keyed by string not HaltId.)
+    hc_events_emitted: set[str] = field(default_factory=set)
 
     def ingest(self, events: list[HaltEvent]) -> Iterator[tuple[str, HaltEvent]]:
         """Diff `events` against state and yield (kind, event) for new ones.
@@ -85,3 +91,4 @@ class HaltTracker:
         self.resumes_emitted.clear()
         self.emitted_halts.clear()
         self.followed_up.clear()
+        self.hc_events_emitted.clear()
diff --git a/src/events/classify.py b/src/events/classify.py
new file mode 100644
index 0000000..c0111ee
--- /dev/null
+++ b/src/events/classify.py
@@ -0,0 +1,267 @@
+"""Precision-biased keyword classifier: NewsItem -> Optional[HCEvent].
+
+Design of record: research/sa_monitor_hc_event_wire_design_2026-07-13.md §3.
+Rules are anchored to the LOCKED real headlines in template-library.md
+§15 (Phase-N primary MISSED), §16 (FDA approval), §19 (Phase-N primary MET).
+
+Philosophy — "miss over misfire" (design §7): a wrong "Phase 3 met" or "FDA
+approval" alert costs more trust than a missed one. Every rule requires the
+headline to CLEARLY match a completed action. Ambiguous, future-tense,
+procedural, or presentation headlines return None.
+
+classify() only proposes an event *type/direction/phase*; it never decides
+whether to fire. The universe filter + attribution + dedup live in
+halt_monitor._emit_hc_events. classify() returns an HCEvent with symbol="".
+"""
+from __future__ import annotations
+
+import re
+from typing import Optional
+
+from ..news.types import NewsItem
+from .types import (
+    CLEARANCE,
+    CRL,
+    DIR_MET,
+    DIR_MISSED,
+    DIR_NA,
+    FDA_APPROVAL,
+    TRIAL_READOUT,
+    HCEvent,
+)
+
+# --------------------------------------------------------------------------
+# Trial readout
+# --------------------------------------------------------------------------
+
+# "Phase 1/2/3" (also "Phase 2B", "Phase 1/2") — capture the leading digit.
+_PHASE = re.compile(r"\bphase\s*([123])", re.I)
+
+# Result attached to the PRIMARY (or co-/dual primary) endpoint. Tying the
+# verb to "primary" (rather than matching "met"/"missed" anywhere) keeps the
+# reported DIRECTION faithful, which is the harmful failure mode to avoid.
+_MET_PRIMARY = re.compile(
+    r"\b(met|meets|achieved|achieves|hit|reached)\b"
+    r"(?:\s+(?:its|the|all|both|all\s+of\s+its))?"
+    r"(?:\s+(?:co-|dual|two|three|multiple))?"
+    r"\s+(?:co-)?primary\b",
+    re.I,
+)
+# Anchored to primary/endpoint so "did not meet enrollment target / guidance"
+# (with a stray Phase in the title) can't read as a failed readout.
+_MISSED_PRIMARY = re.compile(
+    r"\b(did\s+not\s+meet|does\s+not\s+meet|failed\s+to\s+meet|fails\s+to\s+meet"
+    r"|missed|misses|did\s+not\s+achieve|does\s+not\s+achieve)\b"
+    r"(?:\s+(?:its|the|both|all|all\s+of\s+its))?"
+    r"(?:\s+(?:co-|dual))?"
+    r"\s+(?:primary|co-primary|endpoint)",
+    re.I,
+)
+# A topline-data announcement with no explicit met/missed in the headline is
+# still a genuine readout crossing the wire — fired at direction n/a.
+_TOPLINE = re.compile(r"\btop-?line\b", re.I)
+
+# Future / procedural / presentation headlines that mention a Phase but are NOT
+# a readout. If ANY of these match, the trial-readout path returns None. This
+# is the primary defense for the negative test set.
+_TRIAL_NEGATIVE = re.compile(
+    r"\bpresent"          # present / presents / presented / presentation / presenting
+    r"|\bto\s+report\b|\bwill\s+report\b"
+    r"|\bto\s+announce\b|\bto\s+host\b|\bto\s+provide\b|\bto\s+release\b"
+    r"|\bto\s+webcast\b|\bwebcast\b|\bconference\s+call\b|\binvestor\s+day\b"
+    r"|\bposter\b|\bat\s+the\s+\w+.*(?:conference|congress|meeting|symposium)"
+    r"|\binitiat"          # initiate / initiation / initiates
+    r"|\bcommenc"          # commence / commences / commencement
+    r"|\bfirst[\s-]patient\b|\bdosing\s+of\b|\bdoses\s+(?:the\s+)?first\b"
+    r"|\bfirst\s+patient\s+dosed\b"
+    r"|\benroll"           # enroll / enrolls / enrolled / enrollment
+    r"|\btrial\s+design\b|\bstudy\s+design\b"
+    r"|\bto\s+begin\b|\bplans?\s+to\b|\bexpects?\s+to\b"
+    r"|\bon\s+track\b|\bdesign(?:s|ed)?\s+to\s+evaluate\b"
+    r"|\bcompletes?\s+enrollment\b"
+    # F4: future-timeline readouts ("topline data expected in Q4") are NOT a
+    # readout that happened. Requires a timeline token after expected/anticipated
+    # so "met primary, better than expected" still fires.
+    r"|\bexpected\s+(?:in|by|during|for|later|this|next|early|mid|late|soon|shortly|imminent|upcoming|coming|q[1-4]|h[12]|first|second|third|fourth|20\d\d)"
+    r"|\banticipat\w*\s+(?:in|by|during|for|later|this|next|early|mid|late|soon|shortly|imminent|upcoming|coming|q[1-4]|h[12]|20\d\d)"
+    r"|\bto\s+read\s+out\b|\breadout\s+(?:expected|anticipated|on\s+track|in|by|soon|shortly)\b",
+    re.I,
+)
+
+
+def _classify_trial(title: str, item: NewsItem) -> Optional[HCEvent]:
+    m_phase = _PHASE.search(title)
+    if not m_phase:
+        return None
+    if _TRIAL_NEGATIVE.search(title):
+        return None  # future / procedural / presentation → not a readout
+
+    missed = _MISSED_PRIMARY.search(title)
+    met = _MET_PRIMARY.search(title)
+    topline = _TOPLINE.search(title)
+
+    if missed:
+        direction = DIR_MISSED
+    elif met:
+        direction = DIR_MET
+    elif topline:
+        direction = DIR_NA
+    else:
+        return None  # Phase present but no result grammar → not a readout
+
+    return _build(item, TRIAL_READOUT, direction, phase=m_phase.group(1))
+
+
+# --------------------------------------------------------------------------
+# FDA regulatory actions
+# --------------------------------------------------------------------------
+
+# Action-verb-anchored approval grammar. Deliberately does NOT match bare
+# "FDA approval" so pending/procedural headlines ("PDUFA date for FDA approval
+# decision", "seeking FDA approval") don't fire.
+_APPROVAL = re.compile(
+    r"\bfda\s+approves\b"
+    r"|\bfda\s+approved\b"
+    r"|\bfda\s+approval\s+of\b"
+    r"|\breceive[sd]?\s+fda\s+approval\b"
+    r"|\bgranted\s+fda\s+approval\b"
+    r"|\bwins?\s+fda\s+approval\b"
+    r"|\bsecure[sd]?\s+fda\s+approval\b"
+    r"|\bgains?\s+fda\s+approval\b"
+    r"|\bfda\s+grants?\s+(?:full\s+|accelerated\s+|traditional\s+|marketing\s+)?approval\b"
+    r"|\bfda\s+has\s+granted\s+approval\b"
+    r"|\bapproved\s+by\s+the\s+fda\b",
+    re.I,
+)
+# Pending / procedural regulatory milestones that are NOT a grant. Any hit
+# suppresses BOTH approval and clearance (belt-and-suspenders atop the
+# verb-anchored regexes). Shared by the approval + clearance paths.
+_APPROVAL_NEGATIVE = re.compile(
+    r"\baccept(?:s|ed)?\b|\bpriority\s+review\b|\bfast\s+track\b"
+    r"|\bbreakthrough\s+therapy\b|\borphan\s+drug\b|\brare\s+pediatric\b"
+    r"|\badvisory\s+committee\b|\badcomm\b|\bpdufa\b"
+    r"|\bresubmi|\bseeking\b|\bawait|\bapplication\s+for\b"
+    r"|\bfiles?\s+for\b|\bsubmit|\bsubmission\b|\bto\s+request\b"
+    r"|\bcomplete\s+response\s+letter\b|\bpre-?market\s+notification\b",
+    re.I,
+)
+
+# Denial / rejection grammar (F2). A rejection must NEVER read as a grant. v1
+# has no "rejection" event type, so a plain "did not receive approval" headline
+# → None (missing it is far safer than inverting it to an approval). Gates BOTH
+# approval and clearance. NOT applied to trial readouts (which have their own
+# missed-endpoint direction) or to CRL (the rejection event we DO fire).
+_REG_DENIAL = re.compile(
+    r"\bdid\s+not\b|\bdoes\s+not\b|\bdid\s*n[’']?t\b|\bdoes\s*n[’']?t\b"
+    r"|\bfailed\s+to\b|\bfails\s+to\b"
+    r"|\bnot\s+(?:receive|receiv\w+|win|won|gain\w*|secure\w*|grant\w*|approv\w+|clear\w+)\b"
+    r"|\bdenie[sd]\b|\bdenial\b|\bdeclin\w*\s+to\s+approve\b"
+    r"|\breject(?:s|ed|ion)?\b|\bnot\s+approved\b|\bunapproved\b"
+    r"|\bwithdraw\w*\b|\brescind\w*\b|\brevoke[sd]?\b",
+    re.I,
+)
+
+# Commentary / competitor framing (F3): an issuer reacting to SOMEONE ELSE's
+# FDA action. _emit_hc_events would mis-attribute it to the commenting issuer,
+# so suppress. Accept the rare miss of a self-approval PR that also contains one
+# of these words — precision-first.
+_REG_COMMENTARY = re.compile(
+    r"\bcomment(?:s|ary|ing|ed)?\s+on\b|\bcommentary\b"
+    r"|\bcongratulat\w+|\bapplaud\w*|\bwelcome[sd]?\b|\breact[s]?\s+to\b"
+    r"|\brespond[s]?\s+to\b|\bissues?\s+statement\b"
+    r"|\bstatement\s+(?:on|regarding|re)\b",
+    re.I,
+)
+
+# CRL (F5). Must NOT match bare \bcrl\b — CRL is also the ticker for Charles
+# River Laboratories, a COVERED name ("CRL Announces … Financial Results").
+# Require the full phrase or a regulatory-CRL idiom.
+_CRL = re.compile(
+    r"\bcomplete\s+response\s+letter\b"
+    r"|\breceiv\w*\s+(?:a\s+)?crl\b"
+    r"|\bcrl\s+from\s+(?:the\s+)?fda\b"
+    r"|\bissue[sd]?\s+(?:a\s+)?crl\b"
+    r"|\bfda\s+issue[sd]?\s+(?:a\s+)?crl\b",
+    re.I,
+)
+
+# Medtech clearance. Requires a completed clearance action — bare adjective
+# "FDA-cleared" (e.g. "operates an FDA-cleared facility") must NOT match.
+_CLEARANCE = re.compile(
+    r"\bfda\s+clears\b"
+    r"|\bfda\s+clearance\b"
+    r"|\breceive[sd]?\s+fda\s+clearance\b"
+    r"|\b510\(k\)\s+clearance\b"
+    r"|\bgranted\s+510\(k\)\b"
+    r"|\bde\s+novo\s+(?:clearance|authorization|grant)\b"
+    r"|\bpma\s+approval\b"
+    r"|\bfda\s+grants?\s+(?:de\s+novo|510)\b",
+    re.I,
+)
+
+
+def _reg_suppressed(title: str) -> bool:
+    """A grant (approval or clearance) is suppressed when the headline is
+    actually a pending milestone, a denial/rejection, or third-party commentary."""
+    return bool(
+        _APPROVAL_NEGATIVE.search(title)
+        or _REG_DENIAL.search(title)
+        or _REG_COMMENTARY.search(title)
+    )
+
+
+def _classify_regulatory(title: str, item: NewsItem) -> Optional[HCEvent]:
+    # CRL first — a Complete Response Letter is unambiguous and distinct from
+    # an approval; it must never be mis-read as one.
+    if _CRL.search(title):
+        return _build(item, CRL, DIR_NA)
+    # Approval + clearance share the same suppression gate: pending milestone,
+    # denial/rejection (F2), or third-party commentary (F3). Clearance was
+    # previously ungated (F1).
+    if _reg_suppressed(title):
+        return None
+    if _APPROVAL.search(title):
+        return _build(item, FDA_APPROVAL, DIR_NA)
+    if _CLEARANCE.search(title):
+        return _build(item, CLEARANCE, DIR_NA)
+    return None
+
+
+# --------------------------------------------------------------------------
+# Public entry point
+# --------------------------------------------------------------------------
+
+
+def _build(item: NewsItem, event_type: str, direction: str,
+           *, phase: str = "") -> HCEvent:
+    return HCEvent(
+        news_id=item.news_id,
+        symbol="",  # attribution decided by the emit path (tickers ∩ universe)
+        event_type=event_type,
+        direction=direction,
+        headline=item.title,
+        source=item.source,
+        url=item.url,
+        published_at=item.published_at,
+        phase=phase,
+        confidence="high",
+        raw_industries=tuple(item.industries),
+    )
+
+
+def classify(item: NewsItem) -> Optional[HCEvent]:
+    """Classify a NewsItem into an HCEvent, or None if it isn't one.
+
+    Precision-biased: returns None on any ambiguity. Regulatory actions
+    (approval / CRL / clearance) are checked before trial readouts — an
+    approval PR carries no Phase grammar, so the two families don't collide.
+    """
+    title = item.title or ""
+    if not title.strip():
+        return None
+
+    reg = _classify_regulatory(title, item)
+    if reg is not None:
+        return reg
+
+    return _classify_trial(title, item)
diff --git a/src/halt_monitor.py b/src/halt_monitor.py
index c6e4992..8e9a00b 100644
--- a/src/halt_monitor.py
+++ b/src/halt_monitor.py
@@ -1,593 +1,725 @@
-"""Phase 1 sa-monitor halt-feed runner (D4 + D5 + D6 wired).
-
-Polls NYSE LULD CSV and Nasdaq Trader RSS at 5-second cadence, filters halts
-to the sa-monitor coverage universe, dedupes by halt-id, renders to stdout
-in the sa-monitor template, and (when --slack live) posts to #street-account.
-
-D6 additions:
-- Persistent halt-id state (state/dedup_state_<YYYY-MM-DD>.json) so a runner
-  restart mid-session doesn't re-fire previously-seen halts.
-- Failure DMs to #street-account when a feed has N consecutive fetch errors.
-- Final health heartbeat on graceful shutdown (per HEALTH_REPORTING.md v1).
-- Structured JSONL event log of every emit decision.
-
-Run:
-    python -m src.halt_monitor --slack live --duration 3600 --log logs/run.jsonl
-"""
-from __future__ import annotations
-
-import argparse
-import json
-import logging
-import signal
-import sys
-import time
-from dataclasses import asdict, dataclass, field
-from datetime import datetime, timezone
-from pathlib import Path
-from typing import Optional
-
-from . import slack, state
-from .calendars import AnalystDayCalendar, EarningsCalendar
-from .coverage import Universe
-from .dedup import HaltTracker
-from .enrichment import build_note_context, cross_ref_match, find_followup_news
-from .feeds import nasdaq, nyse
-from .feeds.types import HaltEvent
-from .news import bw, gnw, prnewswire
-from .news.cache import NewsCache
-from .reason_codes import is_phase1_emit_code
-from .template import render_followup, render_halt, render_resume
-
-logging.basicConfig(
-    level=logging.INFO,
-    format="%(asctime)s %(levelname)s %(name)s: %(message)s",
-    datefmt="%Y-%m-%d %H:%M:%S",
-)
-log = logging.getLogger("halt_monitor")
-
-# After this many consecutive failures on a single feed we DM the user.
-# At 5s polling, 60 = ~5 minutes of a feed being unavailable.
-FAILURE_DM_THRESHOLD = 60
-
-# News feeds are slower-moving and rate-conscious — poll every Nth halt
-# tick rather than every tick. At 5s halt cadence, NEWS_POLL_EVERY=6 means
-# news refreshes every ~30s.
-NEWS_POLL_EVERY = 6
-
-
-@dataclass
-class FeedHealth:
-    """Per-feed runtime health for the failure-DM logic."""
-    consecutive_failures: int = 0
-    failure_dm_sent: bool = False  # don't spam — one DM per outage
-    last_error: str = ""
-
-
-@dataclass
-class RunStats:
-    started_at: str
-    polls_completed: int = 0
-    fetch_errors: int = 0
-    nasdaq_events_seen: int = 0
-    nyse_events_seen: int = 0
-    halts_emitted: int = 0
-    resumes_emitted: int = 0
-    followups_emitted: int = 0
-    halts_filtered_out_of_universe: int = 0
-    halts_filtered_non_emit_code: int = 0
-    halts_enriched_with_note: int = 0
-    halts_enriched_with_cross_ref: int = 0
-    slack_posts_succeeded: int = 0
-    slack_posts_failed: int = 0
-    state_loads: int = 0
-    state_saves: int = 0
-    earnings_calendar_loaded: int = 0
-    earnings_calendar_generated_at: Optional[str] = None
-    analyst_days_calendar_loaded: int = 0
-    analyst_days_calendar_generated_at: Optional[str] = None
-    news_polls_completed: int = 0
-    news_items_ingested: int = 0
-    news_cache_size: int = 0
-    feed_health: dict = field(default_factory=lambda: {
-        "nasdaq_rss": asdict(FeedHealth()),
-        "nyse_csv": asdict(FeedHealth()),
-        "news_prnewswire": asdict(FeedHealth()),
-        "news_businesswire": asdict(FeedHealth()),
-        "news_globenewswire": asdict(FeedHealth()),
-    })
-
-
-_should_stop = False
-
-
-def _handle_signal(signum, frame):  # noqa: ARG001
-    global _should_stop
-    log.warning("received signal %s; stopping after current poll", signum)
-    _should_stop = True
-
-
-def _safe_fetch(name: str, fetch_fn, stats: RunStats,
-                health: dict[str, FeedHealth],
-                slack_mode: str) -> list[HaltEvent]:
-    """Wrap a feed fetch so a single error doesn't kill the loop.
-    Tracks consecutive failures per feed; fires a Slack DM when threshold hit."""
-    try:
-        events = fetch_fn()
-        h = health[name]
-        if h.consecutive_failures > 0:
-            log.info("%s: recovered after %d consecutive failures",
-                     name, h.consecutive_failures)
-            h.consecutive_failures = 0
-            h.failure_dm_sent = False
-            h.last_error = ""
-        return events
-    except Exception as exc:
-        stats.fetch_errors += 1
-        h = health[name]
-        h.consecutive_failures += 1
-        h.last_error = str(exc)
-        log.error("%s fetch failed (%d consecutive): %s",
-                  name, h.consecutive_failures, exc)
-
-        if (h.consecutive_failures >= FAILURE_DM_THRESHOLD
-                and not h.failure_dm_sent
-                and slack_mode == "live"):
-            try:
-                slack.post_dm(
-                    f"feed `{name}` has failed {h.consecutive_failures} consecutive "
-                    f"polls (~{h.consecutive_failures * 5 // 60} min). Last error: "
-                    f"`{h.last_error[:200]}`",
-                    level="error",
-                )
-                h.failure_dm_sent = True
-                log.warning("%s: failure DM sent", name)
-            except Exception as dm_exc:
-                log.error("%s: failed to send failure DM: %s", name, dm_exc)
-        return []
-
-
-def _emit(
-    kind: str,
-    event: HaltEvent,
-    universe: Universe,
-    log_path: Optional[Path],
-    *,
-    include_non_emit: bool,
-    stats: RunStats,
-    slack_mode: str = "off",
-    slack_webhook_url: Optional[str] = None,
-    earnings_calendar: Optional[EarningsCalendar] = None,
-    analyst_days_calendar: Optional[AnalystDayCalendar] = None,
-    news_cache: Optional[NewsCache] = None,
-    tracker: Optional[HaltTracker] = None,
-) -> None:
-    """Filter, render, and emit one halt or resume event."""
-    meta = universe.get(event.symbol)
-    if meta is None:
-        stats.halts_filtered_out_of_universe += 1
-        log.debug("skip out-of-universe: %s", event)
-        return
-
-    if not include_non_emit and not is_phase1_emit_code(event.reason_code):
-        stats.halts_filtered_non_emit_code += 1
-        log.info("skip non-emit code %s for %s (logged only)",
-                 event.reason_code, event.symbol)
-        if log_path:
-            _append_log(log_path, kind, event, meta, emitted=False)
-        return
-
-    note_context: Optional[str] = None
-    if kind == "halt" and (earnings_calendar is not None
-                            or analyst_days_calendar is not None
-                            or news_cache is not None):
-        note_context = build_note_context(
-            event,
-            earnings=earnings_calendar,
-            analyst_days=analyst_days_calendar,
-            news_cache=news_cache,
-        )
-        if note_context:
-            stats.halts_enriched_with_note += 1
-            if note_context.startswith("Follows "):
-                stats.halts_enriched_with_cross_ref += 1
-            log.info("enriched %s with note: %s", event.symbol, note_context)
-
-    if kind == "halt":
-        stats.halts_emitted += 1
-        rendered = render_halt(event, sector=meta.sector,
-                               subsector=meta.subsector, name_override=meta.name,
-                               note_context=note_context)
-    elif kind == "resume":
-        stats.resumes_emitted += 1
-        rendered = render_resume(event, sector=meta.sector,
-                                 subsector=meta.subsector, name_override=meta.name)
-    else:
-        log.error("unknown emit kind: %s", kind)
-        return
-
-    print()
-    print("=" * 60)
-    print(rendered)
-    print("=" * 60)
-    sys.stdout.flush()
-
-    post_ok = False
-    if slack_mode != "off":
-        try:
-            if kind == "halt":
-                slack.post_halt(event, meta, webhook_url=slack_webhook_url,
-                                dry_run=(slack_mode == "dry-run"),
-                                note_context=note_context)
-            else:
-                slack.post_resume(event, meta, webhook_url=slack_webhook_url,
-                                  dry_run=(slack_mode == "dry-run"))
-            stats.slack_posts_succeeded += 1
-            post_ok = True
-        except Exception as exc:
-            stats.slack_posts_failed += 1
-            log.error("slack post failed for %s: %s", event.symbol, exc)
-
-    # Delivery-gating for §7 follow-up eligibility. seen_halts records mere
-    # OBSERVATIONS; a follow-up may only fire for a halt we actually DELIVERED.
-    # We reach here only past the non-emit-code filter, so:
-    #   off      → the stdout emit IS the delivery
-    #   live     → only a successful post_halt counts
-    #   dry-run  → nothing was delivered, never mark
-    if kind == "halt" and tracker is not None:
-        if slack_mode == "off":
-            delivered = True
-        elif slack_mode == "dry-run":
-            delivered = False
-        else:  # live
-            delivered = post_ok
-        if delivered:
-            tracker.emitted_halts.add(event.halt_id)
-            # Bug-3 guard: suppress a redundant standalone §7 follow-up ONLY
-            # when this halt's cross-ref NOTE actually fired — i.e. the resolving
-            # PR is within the note's window, so the news is already conveyed by
-            # the halt alert. Uses cross_ref_match (the note's own matcher), NOT
-            # find_followup_news: the latter's wider 60-min window would suppress
-            # follow-ups for PRs the note never included, silently dropping the
-            # news. A PR outside the note window (or crossing in a later poll)
-            # is left unmarked and still fires a real follow-up.
-            if (news_cache is not None
-                    and event.halt_id not in tracker.followed_up
-                    and cross_ref_match(event, news_cache) is not None):
-                tracker.followed_up.add(event.halt_id)
-
-    if log_path:
-        _append_log(log_path, kind, event, meta, emitted=True, note_context=note_context)
-
-
-def _emit_followups(
-    tracker: HaltTracker,
-    universe: Universe,
-    news_cache: Optional[NewsCache],
-    log_path: Optional[Path],
-    *,
-    stats: RunStats,
-    slack_mode: str = "off",
-    slack_webhook_url: Optional[str] = None,
-) -> None:
-    """Detect + emit §7 Follow-up alerts for previously-posted covered halts.
-
-    For each halt sa-monitor actually DELIVERED an alert for (tracker.emitted_
-    halts — NOT merely observed in seen_halts), if the ticker is in the covered
-    universe and a breaking PR has since crossed (matched by the shared
-    enrichment matcher find_followup_news), emit a standalone Follow-up alert —
-    the substantive news that resolves the "halted, news pending" alert.
-
-    One follow-up per halt, ever: tracker.followed_up is the persisted marker.
-    Post-then-mark — a halt is marked only AFTER a successful post so a transient
-    post failure retries next poll. Dry-run never posts and never marks.
-    """
-    if news_cache is None:
-        return
-
-    for hid, halt in list(tracker.seen_halts.items()):
-        if hid in tracker.followed_up:
-            continue  # one follow-up per halt, ever
-        if hid not in tracker.emitted_halts:
-            continue  # only follow-up halts we actually DELIVERED an alert for
-        meta = universe.get(halt.symbol)
-        if meta is None:
-            continue  # only follow-up covered names sa-monitor actually posted
-
-        item = find_followup_news(halt, news_cache)
-        if item is None:
-            continue  # no matching post-halt PR → emit nothing (no guessing)
-
-        rendered = render_followup(halt, item, sector=meta.sector,
-                                   subsector=meta.subsector)
-        print()
-        print("=" * 60)
-        print(rendered)
-        print("=" * 60)
-        sys.stdout.flush()
-
-        # dry-run: render only — never post, never mark (so it can be re-tested)
-        if slack_mode == "dry-run":
-            try:
-                slack.post_followup(halt, item, meta,
-                                    webhook_url=slack_webhook_url, dry_run=True)
-            except Exception as exc:
-                log.error("followup dry-run render failed for %s: %s", halt.symbol, exc)
-            continue
-
-        posted = True
-        if slack_mode == "live":
-            try:
-                slack.post_followup(halt, item, meta,
-                                    webhook_url=slack_webhook_url, dry_run=False)
-                stats.slack_posts_succeeded += 1
-            except Exception as exc:
-                posted = False
-                stats.slack_posts_failed += 1
-                log.error("slack followup post failed for %s: %s", halt.symbol, exc)
-
-        # slack_mode == "off": stdout emit is the delivery — mark so we don't
-        # reprint every poll. live: mark only on a successful post.
-        if posted:
-            tracker.followed_up.add(hid)
-            stats.followups_emitted += 1
-            log.info("followup emitted for %s: %s", halt.symbol, item.title)
-            if log_path:
-                _append_log(log_path, "followup", halt, meta, emitted=True,
-                            note_context=f"Follow-up: {item.title}")
-
-
-def _append_log(log_path: Path, kind: str, event: HaltEvent, meta,
-                *, emitted: bool, note_context: Optional[str] = None) -> None:
-    log_path.parent.mkdir(parents=True, exist_ok=True)
-    record = {
-        "ts_utc": datetime.now(timezone.utc).isoformat(timespec="seconds"),
-        "kind": kind,
-        "emitted": emitted,
-        "symbol": event.symbol,
-        "exchange": event.exchange,
-        "halt_date": event.halt_date,
-        "halt_time": event.halt_time,
-        "reason_code": event.reason_code,
-        "reason_description": event.reason_description,
-        "resume_trade_time": event.resume_trade_time,
-        "source": event.source,
-        "sector": meta.sector if meta else None,
-        "subsector": meta.subsector if meta else None,
-        "note_context": note_context,
-    }
-    with log_path.open("a", encoding="utf-8") as f:
-        f.write(json.dumps(record, ensure_ascii=False) + "\n")
-
-
-def _post_health_heartbeat(stats: RunStats, slack_mode: str,
-                            ended_with_error: bool = False) -> None:
-    """End-of-run heartbeat to #street-account per HEALTH_REPORTING.md.
-    Only fires in slack live mode."""
-    if slack_mode != "live":
-        return
-    status = "error" if ended_with_error else "ok"
-    if stats.fetch_errors > 0 and not ended_with_error:
-        status = "warning"
-
-    base_parts = [
-        f"halt-monitor run summary: polls={stats.polls_completed}",
-        f"halts={stats.halts_emitted}",
-        f"resumes={stats.resumes_emitted}",
-        f"followups={stats.followups_emitted}",
-        f"slack_ok/fail={stats.slack_posts_succeeded}/{stats.slack_posts_failed}",
-        f"fetch_errors={stats.fetch_errors}",
-    ]
-    enrich_part = ""
-    if (stats.earnings_calendar_loaded or stats.analyst_days_calendar_loaded
-            or stats.news_polls_completed):
-        enrich_bits = [f"enriched={stats.halts_enriched_with_note}"]
-        if stats.halts_enriched_with_cross_ref:
-            enrich_bits.append(f"cross_ref={stats.halts_enriched_with_cross_ref}")
-        sources = []
-        if stats.earnings_calendar_loaded:
-            sources.append(
-                f"earnings={stats.earnings_calendar_loaded}@{stats.earnings_calendar_generated_at or '?'}"
-            )
-        if stats.analyst_days_calendar_loaded:
-            sources.append(
-                f"analyst_days={stats.analyst_days_calendar_loaded}@{stats.analyst_days_calendar_generated_at or '?'}"
-            )
-        if stats.news_polls_completed:
-            sources.append(
-                f"news_cache={stats.news_cache_size} ({stats.news_polls_completed} polls)"
-            )
-        enrich_part = " ".join(enrich_bits) + " (" + ", ".join(sources) + ")"
-    summary = ", ".join(base_parts) + (" — " + enrich_part if enrich_part else "")
-    try:
-        slack.post_dm(summary, level=status if status in {"ok","warning","error"} else "warning")
-    except Exception as exc:
-        log.error("failed to post health heartbeat: %s", exc)
-
-
-def run(
-    *,
-    once: bool = False,
-    duration_sec: Optional[int] = None,
-    interval_sec: int = 5,
-    log_path: Optional[Path] = None,
-    include_non_emit: bool = False,
-    universe_path: Optional[Path] = None,
-    slack_mode: str = "off",
-    slack_webhook_url: Optional[str] = None,
-    persist_state: bool = True,
-    earnings_calendar_path: Optional[Path] = None,
-    analyst_days_calendar_path: Optional[Path] = None,
-    enable_news_cross_ref: bool = False,
-    news_window_minutes: int = 60,
-) -> RunStats:
-    universe = Universe(universe_path) if universe_path else Universe()
-    log.info("loaded universe: %d tickers (%s)", len(universe), universe.filter_rule)
-    log.info("slack_mode=%s persist_state=%s", slack_mode, persist_state)
-
-    earnings_calendar = (
-        EarningsCalendar(earnings_calendar_path) if earnings_calendar_path else None
-    )
-    analyst_days_calendar = (
-        AnalystDayCalendar(analyst_days_calendar_path) if analyst_days_calendar_path else None
-    )
-    if earnings_calendar is not None:
-        log.info("earnings calendar: %d events (generated_at=%s)",
-                 earnings_calendar.loaded_count, earnings_calendar.generated_at)
-    if analyst_days_calendar is not None:
-        log.info("analyst-days calendar: %d events (generated_at=%s)",
-                 analyst_days_calendar.loaded_count, analyst_days_calendar.generated_at)
-
-    news_cache: Optional[NewsCache] = (
-        NewsCache(window_minutes=news_window_minutes) if enable_news_cross_ref else None
-    )
-    if news_cache is not None:
-        log.info("news cross-ref enabled (window=%dm, sources=PRN/BW/GNW)", news_window_minutes)
-
-    if persist_state:
-        state.cleanup_old()
-        tracker = state.load()
-    else:
-        tracker = HaltTracker()
-
-    stats = RunStats(started_at=datetime.now(timezone.utc).isoformat(timespec="seconds"))
-    stats.state_loads = 1 if persist_state else 0
-    if earnings_calendar is not None:
-        stats.earnings_calendar_loaded = earnings_calendar.loaded_count
-        stats.earnings_calendar_generated_at = earnings_calendar.generated_at
-    if analyst_days_calendar is not None:
-        stats.analyst_days_calendar_loaded = analyst_days_calendar.loaded_count
-        stats.analyst_days_calendar_generated_at = analyst_days_calendar.generated_at
-    health = {name: FeedHealth(**stats.feed_health[name])
-              for name in stats.feed_health}
-
-    signal.signal(signal.SIGINT, _handle_signal)
-    signal.signal(signal.SIGTERM, _handle_signal)
-
-    deadline = (time.monotonic() + duration_sec) if duration_sec else None
-    ended_with_error = False
-
-    try:
-        while not _should_stop:
-            t0 = time.monotonic()
-
-            nasdaq_events = _safe_fetch("nasdaq_rss", nasdaq.fetch, stats, health, slack_mode)
-            nyse_events = _safe_fetch("nyse_csv", nyse.fetch, stats, health, slack_mode)
-            stats.nasdaq_events_seen = max(stats.nasdaq_events_seen, len(nasdaq_events))
-            stats.nyse_events_seen = max(stats.nyse_events_seen, len(nyse_events))
-
-            # News-feed poll (slower cadence than halt feeds — every Nth tick).
-            # Run BEFORE halt emit so a halt fired this tick can cross-ref news
-            # already in the cache from this poll.
-            if news_cache is not None and stats.polls_completed % NEWS_POLL_EVERY == 0:
-                news_items: list = []
-                for label, mod in [("news_prnewswire", prnewswire),
-                                    ("news_businesswire", bw),
-                                    ("news_globenewswire", gnw)]:
-                    news_items.extend(_safe_fetch(label, mod.fetch, stats, health, slack_mode))
-                added = news_cache.ingest(news_items)
-                stats.news_polls_completed += 1
-                stats.news_items_ingested += added
-                # tickers_indexed reflects items currently in the lookback
-                # window (i.e. cross-refable). news_items_ingested is the
-                # cumulative count for dedup auditing.
-                stats.news_cache_size = news_cache.tickers_indexed
-
-            for kind, event in tracker.ingest(nasdaq_events + nyse_events):
-                _emit(kind, event, universe, log_path,
-                      include_non_emit=include_non_emit, stats=stats,
-                      slack_mode=slack_mode, slack_webhook_url=slack_webhook_url,
-                      earnings_calendar=earnings_calendar,
-                      analyst_days_calendar=analyst_days_calendar,
-                      news_cache=news_cache, tracker=tracker)
-
-            # §7 Follow-up detection: a PR that crossed AFTER a previously-
-            # posted covered halt fired. Runs every poll (cheap — iterates
-            # seen_halts) so a follow-up fires promptly once its PR lands in
-            # the news cache. Obeys the same slack_mode gating as halt/resume.
-            _emit_followups(tracker, universe, news_cache, log_path,
-                            stats=stats, slack_mode=slack_mode,
-                            slack_webhook_url=slack_webhook_url)
-
-            if persist_state:
-                try:
-                    state.save(tracker)
-                    stats.state_saves += 1
-                except Exception as exc:
-                    log.error("state save failed: %s", exc)
-
-            stats.polls_completed += 1
-            if stats.polls_completed % 12 == 0:
-                log.info("poll %d: tracked=%d emit=%d/%d errors=%d",
-                         stats.polls_completed, len(tracker),
-                         stats.halts_emitted, stats.resumes_emitted, stats.fetch_errors)
-
-            if once:
-                break
-            if deadline and time.monotonic() >= deadline:
-                log.info("duration reached; stopping")
-                break
-
-            elapsed = time.monotonic() - t0
-            time.sleep(max(0.0, interval_sec - elapsed))
-    except Exception as exc:
-        ended_with_error = True
-        log.exception("run aborted: %s", exc)
-        if slack_mode == "live":
-            try:
-                slack.post_dm(f"halt-monitor crashed: `{exc}`", level="error")
-            except Exception:
-                pass
-        raise
-    finally:
-        # Persist health snapshot back into stats for the final report
-        for name, h in health.items():
-            stats.feed_health[name] = asdict(h)
-        log.info("run ended: polls=%d halts=%d resumes=%d errors=%d",
-                 stats.polls_completed, stats.halts_emitted,
-                 stats.resumes_emitted, stats.fetch_errors)
-        _post_health_heartbeat(stats, slack_mode, ended_with_error=ended_with_error)
-
-    return stats
-
-
-def main(argv: Optional[list[str]] = None) -> int:
-    parser = argparse.ArgumentParser(description="sa-monitor halt feed runner")
-    parser.add_argument("--once", action="store_true")
-    parser.add_argument("--duration", type=int, default=None)
-    parser.add_argument("--interval", type=int, default=5)
-    parser.add_argument(
-        "--log", type=Path,
-        default=Path("logs") / f"halt_monitor_{datetime.now().strftime('%Y%m%d_%H%M%S')}.jsonl",
-    )
-    parser.add_argument("--include-non-emit", action="store_true")
-    parser.add_argument("--universe", type=Path, default=None)
-    parser.add_argument("--slack", choices=["off", "dry-run", "live"], default="off")
-    parser.add_argument("--no-persist", action="store_true",
-                        help="disable dedup-state persistence (default: enabled)")
-    parser.add_argument("--earnings-calendar", type=Path, default=None,
-                        help="path to earnings-agent upcoming_events.json (Phase 2 enrichment)")
-    parser.add_argument("--analyst-days-calendar", type=Path, default=None,
-                        help="path to analyst-days upcoming_events.json (Phase 2 enrichment)")
-    parser.add_argument("--news-cross-ref", action="store_true",
-                        help="enable PRN/BW/GNW news cross-ref enrichment (Phase 2 slice 2B)")
-    parser.add_argument("--news-window-minutes", type=int, default=60,
-                        help="lookback window for news cross-ref (default 60min)")
-    args = parser.parse_args(argv)
-
-    stats = run(
-        once=args.once, duration_sec=args.duration, interval_sec=args.interval,
-        log_path=args.log, include_non_emit=args.include_non_emit,
-        universe_path=args.universe, slack_mode=args.slack,
-        persist_state=not args.no_persist,
-        earnings_calendar_path=args.earnings_calendar,
-        analyst_days_calendar_path=args.analyst_days_calendar,
-        enable_news_cross_ref=args.news_cross_ref,
-        news_window_minutes=args.news_window_minutes,
-    )
-    print(json.dumps(asdict(stats), indent=2))
-    return 0
-
-
-if __name__ == "__main__":
-    sys.exit(main())
+"""Phase 1 sa-monitor halt-feed runner (D4 + D5 + D6 wired).
+
+Polls NYSE LULD CSV and Nasdaq Trader RSS at 5-second cadence, filters halts
+to the sa-monitor coverage universe, dedupes by halt-id, renders to stdout
+in the sa-monitor template, and (when --slack live) posts to #street-account.
+
+D6 additions:
+- Persistent halt-id state (state/dedup_state_<YYYY-MM-DD>.json) so a runner
+  restart mid-session doesn't re-fire previously-seen halts.
+- Failure DMs to #street-account when a feed has N consecutive fetch errors.
+- Final health heartbeat on graceful shutdown (per HEALTH_REPORTING.md v1).
+- Structured JSONL event log of every emit decision.
+
+Run:
+    python -m src.halt_monitor --slack live --duration 3600 --log logs/run.jsonl
+"""
+from __future__ import annotations
+
+import argparse
+import json
+import logging
+import signal
+import sys
+import time
+from dataclasses import asdict, dataclass, field, replace
+from datetime import datetime, timezone
+from pathlib import Path
+from typing import Optional
+
+from . import slack, state
+from .calendars import AnalystDayCalendar, EarningsCalendar
+from .coverage import Universe
+from .dedup import HaltTracker
+from .enrichment import build_note_context, cross_ref_match, find_followup_news
+from .events.classify import classify
+from .feeds import nasdaq, nyse
+from .feeds.types import HaltEvent
+from .news import bw, gnw, prnewswire
+from .news.cache import NewsCache
+from .news.types import NewsItem
+from .reason_codes import is_phase1_emit_code
+from .template import render_followup, render_halt, render_hc_event, render_resume
+
+logging.basicConfig(
+    level=logging.INFO,
+    format="%(asctime)s %(levelname)s %(name)s: %(message)s",
+    datefmt="%Y-%m-%d %H:%M:%S",
+)
+log = logging.getLogger("halt_monitor")
+
+# After this many consecutive failures on a single feed we DM the user.
+# At 5s polling, 60 = ~5 minutes of a feed being unavailable.
+FAILURE_DM_THRESHOLD = 60
+
+# News feeds are slower-moving and rate-conscious — poll every Nth halt
+# tick rather than every tick. At 5s halt cadence, NEWS_POLL_EVERY=6 means
+# news refreshes every ~30s.
+NEWS_POLL_EVERY = 6
+
+
+@dataclass
+class FeedHealth:
+    """Per-feed runtime health for the failure-DM logic."""
+    consecutive_failures: int = 0
+    failure_dm_sent: bool = False  # don't spam — one DM per outage
+    last_error: str = ""
+
+
+@dataclass
+class RunStats:
+    started_at: str
+    polls_completed: int = 0
+    fetch_errors: int = 0
+    nasdaq_events_seen: int = 0
+    nyse_events_seen: int = 0
+    halts_emitted: int = 0
+    resumes_emitted: int = 0
+    followups_emitted: int = 0
+    hc_events_emitted: int = 0
+    halts_filtered_out_of_universe: int = 0
+    halts_filtered_non_emit_code: int = 0
+    halts_enriched_with_note: int = 0
+    halts_enriched_with_cross_ref: int = 0
+    slack_posts_succeeded: int = 0
+    slack_posts_failed: int = 0
+    state_loads: int = 0
+    state_saves: int = 0
+    earnings_calendar_loaded: int = 0
+    earnings_calendar_generated_at: Optional[str] = None
+    analyst_days_calendar_loaded: int = 0
+    analyst_days_calendar_generated_at: Optional[str] = None
+    news_polls_completed: int = 0
+    news_items_ingested: int = 0
+    news_cache_size: int = 0
+    feed_health: dict = field(default_factory=lambda: {
+        "nasdaq_rss": asdict(FeedHealth()),
+        "nyse_csv": asdict(FeedHealth()),
+        "news_prnewswire": asdict(FeedHealth()),
+        "news_businesswire": asdict(FeedHealth()),
+        "news_globenewswire": asdict(FeedHealth()),
+    })
+
+
+_should_stop = False
+
+
+def _handle_signal(signum, frame):  # noqa: ARG001
+    global _should_stop
+    log.warning("received signal %s; stopping after current poll", signum)
+    _should_stop = True
+
+
+def _safe_fetch(name: str, fetch_fn, stats: RunStats,
+                health: dict[str, FeedHealth],
+                slack_mode: str) -> list[HaltEvent]:
+    """Wrap a feed fetch so a single error doesn't kill the loop.
+    Tracks consecutive failures per feed; fires a Slack DM when threshold hit."""
+    try:
+        events = fetch_fn()
+        h = health[name]
+        if h.consecutive_failures > 0:
+            log.info("%s: recovered after %d consecutive failures",
+                     name, h.consecutive_failures)
+            h.consecutive_failures = 0
+            h.failure_dm_sent = False
+            h.last_error = ""
+        return events
+    except Exception as exc:
+        stats.fetch_errors += 1
+        h = health[name]
+        h.consecutive_failures += 1
+        h.last_error = str(exc)
+        log.error("%s fetch failed (%d consecutive): %s",
+                  name, h.consecutive_failures, exc)
+
+        if (h.consecutive_failures >= FAILURE_DM_THRESHOLD
+                and not h.failure_dm_sent
+                and slack_mode == "live"):
+            try:
+                slack.post_dm(
+                    f"feed `{name}` has failed {h.consecutive_failures} consecutive "
+                    f"polls (~{h.consecutive_failures * 5 // 60} min). Last error: "
+                    f"`{h.last_error[:200]}`",
+                    level="error",
+                )
+                h.failure_dm_sent = True
+                log.warning("%s: failure DM sent", name)
+            except Exception as dm_exc:
+                log.error("%s: failed to send failure DM: %s", name, dm_exc)
+        return []
+
+
+def _emit(
+    kind: str,
+    event: HaltEvent,
+    universe: Universe,
+    log_path: Optional[Path],
+    *,
+    include_non_emit: bool,
+    stats: RunStats,
+    slack_mode: str = "off",
+    slack_webhook_url: Optional[str] = None,
+    earnings_calendar: Optional[EarningsCalendar] = None,
+    analyst_days_calendar: Optional[AnalystDayCalendar] = None,
+    news_cache: Optional[NewsCache] = None,
+    tracker: Optional[HaltTracker] = None,
+) -> None:
+    """Filter, render, and emit one halt or resume event."""
+    meta = universe.get(event.symbol)
+    if meta is None:
+        stats.halts_filtered_out_of_universe += 1
+        log.debug("skip out-of-universe: %s", event)
+        return
+
+    if not include_non_emit and not is_phase1_emit_code(event.reason_code):
+        stats.halts_filtered_non_emit_code += 1
+        log.info("skip non-emit code %s for %s (logged only)",
+                 event.reason_code, event.symbol)
+        if log_path:
+            _append_log(log_path, kind, event, meta, emitted=False)
+        return
+
+    note_context: Optional[str] = None
+    if kind == "halt" and (earnings_calendar is not None
+                            or analyst_days_calendar is not None
+                            or news_cache is not None):
+        note_context = build_note_context(
+            event,
+            earnings=earnings_calendar,
+            analyst_days=analyst_days_calendar,
+            news_cache=news_cache,
+        )
+        if note_context:
+            stats.halts_enriched_with_note += 1
+            if note_context.startswith("Follows "):
+                stats.halts_enriched_with_cross_ref += 1
+            log.info("enriched %s with note: %s", event.symbol, note_context)
+
+    if kind == "halt":
+        stats.halts_emitted += 1
+        rendered = render_halt(event, sector=meta.sector,
+                               subsector=meta.subsector, name_override=meta.name,
+                               note_context=note_context)
+    elif kind == "resume":
+        stats.resumes_emitted += 1
+        rendered = render_resume(event, sector=meta.sector,
+                                 subsector=meta.subsector, name_override=meta.name)
+    else:
+        log.error("unknown emit kind: %s", kind)
+        return
+
+    print()
+    print("=" * 60)
+    print(rendered)
+    print("=" * 60)
+    sys.stdout.flush()
+
+    post_ok = False
+    if slack_mode != "off":
+        try:
+            if kind == "halt":
+                slack.post_halt(event, meta, webhook_url=slack_webhook_url,
+                                dry_run=(slack_mode == "dry-run"),
+                                note_context=note_context)
+            else:
+                slack.post_resume(event, meta, webhook_url=slack_webhook_url,
+                                  dry_run=(slack_mode == "dry-run"))
+            stats.slack_posts_succeeded += 1
+            post_ok = True
+        except Exception as exc:
+            stats.slack_posts_failed += 1
+            log.error("slack post failed for %s: %s", event.symbol, exc)
+
+    # Delivery-gating for §7 follow-up eligibility. seen_halts records mere
+    # OBSERVATIONS; a follow-up may only fire for a halt we actually DELIVERED.
+    # We reach here only past the non-emit-code filter, so:
+    #   off      → the stdout emit IS the delivery
+    #   live     → only a successful post_halt counts
+    #   dry-run  → nothing was delivered, never mark
+    if kind == "halt" and tracker is not None:
+        if slack_mode == "off":
+            delivered = True
+        elif slack_mode == "dry-run":
+            delivered = False
+        else:  # live
+            delivered = post_ok
+        if delivered:
+            tracker.emitted_halts.add(event.halt_id)
+            # Bug-3 guard: suppress a redundant standalone §7 follow-up ONLY
+            # when this halt's cross-ref NOTE actually fired — i.e. the resolving
+            # PR is within the note's window, so the news is already conveyed by
+            # the halt alert. Uses cross_ref_match (the note's own matcher), NOT
+            # find_followup_news: the latter's wider 60-min window would suppress
+            # follow-ups for PRs the note never included, silently dropping the
+            # news. A PR outside the note window (or crossing in a later poll)
+            # is left unmarked and still fires a real follow-up.
+            if (news_cache is not None
+                    and event.halt_id not in tracker.followed_up
+                    and cross_ref_match(event, news_cache) is not None):
+                tracker.followed_up.add(event.halt_id)
+
+    if log_path:
+        _append_log(log_path, kind, event, meta, emitted=True, note_context=note_context)
+
+
+def _emit_followups(
+    tracker: HaltTracker,
+    universe: Universe,
+    news_cache: Optional[NewsCache],
+    log_path: Optional[Path],
+    *,
+    stats: RunStats,
+    slack_mode: str = "off",
+    slack_webhook_url: Optional[str] = None,
+) -> None:
+    """Detect + emit §7 Follow-up alerts for previously-posted covered halts.
+
+    For each halt sa-monitor actually DELIVERED an alert for (tracker.emitted_
+    halts — NOT merely observed in seen_halts), if the ticker is in the covered
+    universe and a breaking PR has since crossed (matched by the shared
+    enrichment matcher find_followup_news), emit a standalone Follow-up alert —
+    the substantive news that resolves the "halted, news pending" alert.
+
+    One follow-up per halt, ever: tracker.followed_up is the persisted marker.
+    Post-then-mark — a halt is marked only AFTER a successful post so a transient
+    post failure retries next poll. Dry-run never posts and never marks.
+    """
+    if news_cache is None:
+        return
+
+    for hid, halt in list(tracker.seen_halts.items()):
+        if hid in tracker.followed_up:
+            continue  # one follow-up per halt, ever
+        if hid not in tracker.emitted_halts:
+            continue  # only follow-up halts we actually DELIVERED an alert for
+        meta = universe.get(halt.symbol)
+        if meta is None:
+            continue  # only follow-up covered names sa-monitor actually posted
+
+        item = find_followup_news(halt, news_cache)
+        if item is None:
+            continue  # no matching post-halt PR → emit nothing (no guessing)
+
+        rendered = render_followup(halt, item, sector=meta.sector,
+                                   subsector=meta.subsector)
+        print()
+        print("=" * 60)
+        print(rendered)
+        print("=" * 60)
+        sys.stdout.flush()
+
+        # dry-run: render only — never post, never mark (so it can be re-tested)
+        if slack_mode == "dry-run":
+            try:
+                slack.post_followup(halt, item, meta,
+                                    webhook_url=slack_webhook_url, dry_run=True)
+            except Exception as exc:
+                log.error("followup dry-run render failed for %s: %s", halt.symbol, exc)
+            continue
+
+        posted = True
+        if slack_mode == "live":
+            try:
+                slack.post_followup(halt, item, meta,
+                                    webhook_url=slack_webhook_url, dry_run=False)
+                stats.slack_posts_succeeded += 1
+            except Exception as exc:
+                posted = False
+                stats.slack_posts_failed += 1
+                log.error("slack followup post failed for %s: %s", halt.symbol, exc)
+
+        # slack_mode == "off": stdout emit is the delivery — mark so we don't
+        # reprint every poll. live: mark only on a successful post.
+        if posted:
+            tracker.followed_up.add(hid)
+            stats.followups_emitted += 1
+            log.info("followup emitted for %s: %s", halt.symbol, item.title)
+            if log_path:
+                _append_log(log_path, "followup", halt, meta, emitted=True,
+                            note_context=f"Follow-up: {item.title}")
+
+
+def _emit_hc_events(
+    news_items: list[NewsItem],
+    universe: Universe,
+    tracker: HaltTracker,
+    log_path: Optional[Path],
+    *,
+    stats: RunStats,
+    slack_mode: str = "off",
+    slack_webhook_url: Optional[str] = None,
+) -> None:
+    """Detect + emit HC event-wire alerts (FDA actions + clinical readouts).
+
+    v1 CONSUMES the PR-wire NewsItems already fetched this poll: classify each,
+    and — for items whose issuer-embedded tickers are in the covered universe —
+    post an SA-faithful alert (design of record §4).
+
+    Attribution is HIGH-confidence by construction: the ticker was written into
+    the issuer's own release and extracted by the news parsers, so there is no
+    name-matching / misattribution risk (the openFDA sponsor→ticker resolver is
+    deferred to session 2).
+
+    Dedup key: f"{news_id}|{symbol}|{event_type}" in tracker.hc_events_emitted —
+    one alert per (press release, covered ticker, event type), ever. Post-then-
+    mark, identical gating to _emit_followups:
+      off      → stdout emit is the delivery → mark
+      live     → mark only on a successful post
+      dry-run  → render only, never post, never mark (re-testable)
+    """
+    for item in news_items:
+        ev = classify(item)
+        if ev is None:
+            continue
+        # v1 universe filter = issuer-extracted tickers ∩ covered Universe.
+        # (session 2 adds the sponsor→ticker resolver fallback here.)
+        covered = [t for t in item.tickers if universe.get(t)]
+        if not covered:
+            continue
+        for symbol in covered:
+            dedup_key = f"{item.news_id}|{symbol}|{ev.event_type}"
+            if dedup_key in tracker.hc_events_emitted:
+                continue  # one alert per (PR, ticker, type), ever
+            meta = universe.get(symbol)
+            event = replace(ev, symbol=symbol)
+
+            rendered = render_hc_event(
+                event, sector=(meta.sector if meta else ""),
+                subsector=(meta.subsector if meta else ""),
+            )
+            print()
+            print("=" * 60)
+            print(rendered)
+            print("=" * 60)
+            sys.stdout.flush()
+
+            # dry-run: render only — never post, never mark (re-testable).
+            if slack_mode == "dry-run":
+                try:
+                    slack.post_hc_event(event, meta,
+                                        webhook_url=slack_webhook_url, dry_run=True)
+                except Exception as exc:
+                    log.error("hc-event dry-run render failed for %s: %s",
+                              symbol, exc)
+                continue
+
+            posted = True
+            if slack_mode == "live":
+                try:
+                    slack.post_hc_event(event, meta,
+                                        webhook_url=slack_webhook_url, dry_run=False)
+                    stats.slack_posts_succeeded += 1
+                except Exception as exc:
+                    posted = False
+                    stats.slack_posts_failed += 1
+                    log.error("slack hc-event post failed for %s: %s", symbol, exc)
+
+            # off: stdout emit is the delivery → mark. live: mark only on post ok.
+            if posted:
+                tracker.hc_events_emitted.add(dedup_key)
+                stats.hc_events_emitted += 1
+                log.info("hc-event emitted for %s [%s/%s]: %s",
+                         symbol, event.event_type, event.direction, event.headline)
+                if log_path:
+                    _append_hc_log(log_path, event, meta)
+
+
+def _append_hc_log(log_path: Path, event, meta) -> None:
+    """Structured JSONL audit record for a delivered HC event (mirrors
+    _append_log; HCEvent has a different shape than HaltEvent so it gets its
+    own writer rather than being coerced)."""
+    log_path.parent.mkdir(parents=True, exist_ok=True)
+    record = {
+        "ts_utc": datetime.now(timezone.utc).isoformat(timespec="seconds"),
+        "kind": "hc_event",
+        "emitted": True,
+        "symbol": event.symbol,
+        "event_type": event.event_type,
+        "direction": event.direction,
+        "phase": event.phase,
+        "headline": event.headline,
+        "source": event.source,
+        "url": event.url,
+        "published_at": event.published_at,
+        "confidence": event.confidence,
+        "sector": meta.sector if meta else None,
+        "subsector": meta.subsector if meta else None,
+    }
+    with log_path.open("a", encoding="utf-8") as f:
+        f.write(json.dumps(record, ensure_ascii=False) + "\n")
+
+
+def _append_log(log_path: Path, kind: str, event: HaltEvent, meta,
+                *, emitted: bool, note_context: Optional[str] = None) -> None:
+    log_path.parent.mkdir(parents=True, exist_ok=True)
+    record = {
+        "ts_utc": datetime.now(timezone.utc).isoformat(timespec="seconds"),
+        "kind": kind,
+        "emitted": emitted,
+        "symbol": event.symbol,
+        "exchange": event.exchange,
+        "halt_date": event.halt_date,
+        "halt_time": event.halt_time,
+        "reason_code": event.reason_code,
+        "reason_description": event.reason_description,
+        "resume_trade_time": event.resume_trade_time,
+        "source": event.source,
+        "sector": meta.sector if meta else None,
+        "subsector": meta.subsector if meta else None,
+        "note_context": note_context,
+    }
+    with log_path.open("a", encoding="utf-8") as f:
+        f.write(json.dumps(record, ensure_ascii=False) + "\n")
+
+
+def _post_health_heartbeat(stats: RunStats, slack_mode: str,
+                            ended_with_error: bool = False) -> None:
+    """End-of-run heartbeat to #street-account per HEALTH_REPORTING.md.
+    Only fires in slack live mode."""
+    if slack_mode != "live":
+        return
+    status = "error" if ended_with_error else "ok"
+    if stats.fetch_errors > 0 and not ended_with_error:
+        status = "warning"
+
+    base_parts = [
+        f"halt-monitor run summary: polls={stats.polls_completed}",
+        f"halts={stats.halts_emitted}",
+        f"resumes={stats.resumes_emitted}",
+        f"followups={stats.followups_emitted}",
+        f"hc_events={stats.hc_events_emitted}",
+        f"slack_ok/fail={stats.slack_posts_succeeded}/{stats.slack_posts_failed}",
+        f"fetch_errors={stats.fetch_errors}",
+    ]
+    enrich_part = ""
+    if (stats.earnings_calendar_loaded or stats.analyst_days_calendar_loaded
+            or stats.news_polls_completed):
+        enrich_bits = [f"enriched={stats.halts_enriched_with_note}"]
+        if stats.halts_enriched_with_cross_ref:
+            enrich_bits.append(f"cross_ref={stats.halts_enriched_with_cross_ref}")
+        sources = []
+        if stats.earnings_calendar_loaded:
+            sources.append(
+                f"earnings={stats.earnings_calendar_loaded}@{stats.earnings_calendar_generated_at or '?'}"
+            )
+        if stats.analyst_days_calendar_loaded:
+            sources.append(
+                f"analyst_days={stats.analyst_days_calendar_loaded}@{stats.analyst_days_calendar_generated_at or '?'}"
+            )
+        if stats.news_polls_completed:
+            sources.append(
+                f"news_cache={stats.news_cache_size} ({stats.news_polls_completed} polls)"
+            )
+        enrich_part = " ".join(enrich_bits) + " (" + ", ".join(sources) + ")"
+    summary = ", ".join(base_parts) + (" — " + enrich_part if enrich_part else "")
+    try:
+        slack.post_dm(summary, level=status if status in {"ok","warning","error"} else "warning")
+    except Exception as exc:
+        log.error("failed to post health heartbeat: %s", exc)
+
+
+def run(
+    *,
+    once: bool = False,
+    duration_sec: Optional[int] = None,
+    interval_sec: int = 5,
+    log_path: Optional[Path] = None,
+    include_non_emit: bool = False,
+    universe_path: Optional[Path] = None,
+    slack_mode: str = "off",
+    slack_webhook_url: Optional[str] = None,
+    persist_state: bool = True,
+    earnings_calendar_path: Optional[Path] = None,
+    analyst_days_calendar_path: Optional[Path] = None,
+    enable_news_cross_ref: bool = False,
+    news_window_minutes: int = 60,
+    hc_events_enabled: bool = False,
+) -> RunStats:
+    universe = Universe(universe_path) if universe_path else Universe()
+    log.info("loaded universe: %d tickers (%s)", len(universe), universe.filter_rule)
+    log.info("slack_mode=%s persist_state=%s", slack_mode, persist_state)
+
+    earnings_calendar = (
+        EarningsCalendar(earnings_calendar_path) if earnings_calendar_path else None
+    )
+    analyst_days_calendar = (
+        AnalystDayCalendar(analyst_days_calendar_path) if analyst_days_calendar_path else None
+    )
+    if earnings_calendar is not None:
+        log.info("earnings calendar: %d events (generated_at=%s)",
+                 earnings_calendar.loaded_count, earnings_calendar.generated_at)
+    if analyst_days_calendar is not None:
+        log.info("analyst-days calendar: %d events (generated_at=%s)",
+                 analyst_days_calendar.loaded_count, analyst_days_calendar.generated_at)
+
+    news_cache: Optional[NewsCache] = (
+        NewsCache(window_minutes=news_window_minutes) if enable_news_cross_ref else None
+    )
+    if news_cache is not None:
+        log.info("news cross-ref enabled (window=%dm, sources=PRN/BW/GNW)", news_window_minutes)
+    if hc_events_enabled:
+        log.info("HC event-wire enabled (FDA actions + clinical readouts → #street-account)")
+
+    if persist_state:
+        state.cleanup_old()
+        tracker = state.load()
+    else:
+        tracker = HaltTracker()
+
+    stats = RunStats(started_at=datetime.now(timezone.utc).isoformat(timespec="seconds"))
+    stats.state_loads = 1 if persist_state else 0
+    if earnings_calendar is not None:
+        stats.earnings_calendar_loaded = earnings_calendar.loaded_count
+        stats.earnings_calendar_generated_at = earnings_calendar.generated_at
+    if analyst_days_calendar is not None:
+        stats.analyst_days_calendar_loaded = analyst_days_calendar.loaded_count
+        stats.analyst_days_calendar_generated_at = analyst_days_calendar.generated_at
+    health = {name: FeedHealth(**stats.feed_health[name])
+              for name in stats.feed_health}
+
+    signal.signal(signal.SIGINT, _handle_signal)
+    signal.signal(signal.SIGTERM, _handle_signal)
+
+    deadline = (time.monotonic() + duration_sec) if duration_sec else None
+    ended_with_error = False
+
+    try:
+        while not _should_stop:
+            t0 = time.monotonic()
+
+            nasdaq_events = _safe_fetch("nasdaq_rss", nasdaq.fetch, stats, health, slack_mode)
+            nyse_events = _safe_fetch("nyse_csv", nyse.fetch, stats, health, slack_mode)
+            stats.nasdaq_events_seen = max(stats.nasdaq_events_seen, len(nasdaq_events))
+            stats.nyse_events_seen = max(stats.nyse_events_seen, len(nyse_events))
+
+            # News-feed poll (slower cadence than halt feeds — every Nth tick).
+            # Run BEFORE halt emit so a halt fired this tick can cross-ref news
+            # already in the cache from this poll. Fetched when EITHER the
+            # cross-ref cache OR the HC event-wire is enabled (both consume the
+            # same freshly-fetched news_items).
+            if ((news_cache is not None or hc_events_enabled)
+                    and stats.polls_completed % NEWS_POLL_EVERY == 0):
+                news_items: list = []
+                for label, mod in [("news_prnewswire", prnewswire),
+                                    ("news_businesswire", bw),
+                                    ("news_globenewswire", gnw)]:
+                    news_items.extend(_safe_fetch(label, mod.fetch, stats, health, slack_mode))
+                if news_cache is not None:
+                    added = news_cache.ingest(news_items)
+                    stats.news_polls_completed += 1
+                    stats.news_items_ingested += added
+                    # tickers_indexed reflects items currently in the lookback
+                    # window (i.e. cross-refable). news_items_ingested is the
+                    # cumulative count for dedup auditing.
+                    stats.news_cache_size = news_cache.tickers_indexed
+                # HC event-wire: classify the freshly-fetched PR-wire items and
+                # emit alerts for covered names (additive; does not touch the
+                # halt/resume/follow-up paths above).
+                if hc_events_enabled:
+                    _emit_hc_events(news_items, universe, tracker, log_path,
+                                    stats=stats, slack_mode=slack_mode,
+                                    slack_webhook_url=slack_webhook_url)
+
+            for kind, event in tracker.ingest(nasdaq_events + nyse_events):
+                _emit(kind, event, universe, log_path,
+                      include_non_emit=include_non_emit, stats=stats,
+                      slack_mode=slack_mode, slack_webhook_url=slack_webhook_url,
+                      earnings_calendar=earnings_calendar,
+                      analyst_days_calendar=analyst_days_calendar,
+                      news_cache=news_cache, tracker=tracker)
+
+            # §7 Follow-up detection: a PR that crossed AFTER a previously-
+            # posted covered halt fired. Runs every poll (cheap — iterates
+            # seen_halts) so a follow-up fires promptly once its PR lands in
+            # the news cache. Obeys the same slack_mode gating as halt/resume.
+            _emit_followups(tracker, universe, news_cache, log_path,
+                            stats=stats, slack_mode=slack_mode,
+                            slack_webhook_url=slack_webhook_url)
+
+            if persist_state:
+                try:
+                    state.save(tracker)
+                    stats.state_saves += 1
+                except Exception as exc:
+                    log.error("state save failed: %s", exc)
+
+            stats.polls_completed += 1
+            if stats.polls_completed % 12 == 0:
+                log.info("poll %d: tracked=%d emit=%d/%d errors=%d",
+                         stats.polls_completed, len(tracker),
+                         stats.halts_emitted, stats.resumes_emitted, stats.fetch_errors)
+
+            if once:
+                break
+            if deadline and time.monotonic() >= deadline:
+                log.info("duration reached; stopping")
+                break
+
+            elapsed = time.monotonic() - t0
+            time.sleep(max(0.0, interval_sec - elapsed))
+    except Exception as exc:
+        ended_with_error = True
+        log.exception("run aborted: %s", exc)
+        if slack_mode == "live":
+            try:
+                slack.post_dm(f"halt-monitor crashed: `{exc}`", level="error")
+            except Exception:
+                pass
+        raise
+    finally:
+        # Persist health snapshot back into stats for the final report
+        for name, h in health.items():
+            stats.feed_health[name] = asdict(h)
+        log.info("run ended: polls=%d halts=%d resumes=%d errors=%d",
+                 stats.polls_completed, stats.halts_emitted,
+                 stats.resumes_emitted, stats.fetch_errors)
+        _post_health_heartbeat(stats, slack_mode, ended_with_error=ended_with_error)
+
+    return stats
+
+
+def main(argv: Optional[list[str]] = None) -> int:
+    parser = argparse.ArgumentParser(description="sa-monitor halt feed runner")
+    parser.add_argument("--once", action="store_true")
+    parser.add_argument("--duration", type=int, default=None)
+    parser.add_argument("--interval", type=int, default=5)
+    parser.add_argument(
+        "--log", type=Path,
+        default=Path("logs") / f"halt_monitor_{datetime.now().strftime('%Y%m%d_%H%M%S')}.jsonl",
+    )
+    parser.add_argument("--include-non-emit", action="store_true")
+    parser.add_argument("--universe", type=Path, default=None)
+    parser.add_argument("--slack", choices=["off", "dry-run", "live"], default="off")
+    parser.add_argument("--no-persist", action="store_true",
+                        help="disable dedup-state persistence (default: enabled)")
+    parser.add_argument("--earnings-calendar", type=Path, default=None,
+                        help="path to earnings-agent upcoming_events.json (Phase 2 enrichment)")
+    parser.add_argument("--analyst-days-calendar", type=Path, default=None,
+                        help="path to analyst-days upcoming_events.json (Phase 2 enrichment)")
+    parser.add_argument("--news-cross-ref", action="store_true",
+                        help="enable PRN/BW/GNW news cross-ref enrichment (Phase 2 slice 2B)")
+    parser.add_argument("--news-window-minutes", type=int, default=60,
+                        help="lookback window for news cross-ref (default 60min)")
+    parser.add_argument("--hc-events", action="store_true",
+                        help="enable the HC event-wire lane (FDA approvals/CRLs + "
+                             "clinical-trial readouts on covered names → #street-account)")
+    args = parser.parse_args(argv)
+
+    stats = run(
+        once=args.once, duration_sec=args.duration, interval_sec=args.interval,
+        log_path=args.log, include_non_emit=args.include_non_emit,
+        universe_path=args.universe, slack_mode=args.slack,
+        persist_state=not args.no_persist,
+        earnings_calendar_path=args.earnings_calendar,
+        analyst_days_calendar_path=args.analyst_days_calendar,
+        enable_news_cross_ref=args.news_cross_ref,
+        news_window_minutes=args.news_window_minutes,
+        hc_events_enabled=args.hc_events,
+    )
+    print(json.dumps(asdict(stats), indent=2))
+    return 0
+
+
+if __name__ == "__main__":
+    sys.exit(main())
diff --git a/src/slack.py b/src/slack.py
index 03d22e5..50a5f5b 100644
--- a/src/slack.py
+++ b/src/slack.py
@@ -1,269 +1,357 @@
-"""Slack delivery for sa-monitor halt + resume notifications.
-
-Posts to the consolidated #street-account channel for ALL sa-monitor alerts
-across Phases 1-5. Block Kit format per HEALTH_REPORTING.md §4.1.
-
-Webhook source-of-truth:
-- SLACK_WEBHOOK_STREET_ACCOUNT env var, OR
-- Claude Folder/.secrets/slack_webhook_street_account.txt (one line)
-"""
-from __future__ import annotations
-
-import json
-import logging
-import os
-import time
-from pathlib import Path
-from typing import Optional
-
-import requests
-
-from .coverage import TickerMeta
-from .feeds.types import HaltEvent
-from .news.types import NewsItem
-from .template import (
-    _format_date_short,
-    _format_price,
-    _format_time_hhmm,
-    biotech_triage_cta,
-    followup_headline,
-    followup_source_label,
-    followup_when,
-    is_biotech,
-)
-
-log = logging.getLogger(__name__)
-
-REPO_ROOT = Path(__file__).resolve().parents[1]
-CLAUDE_FOLDER = REPO_ROOT.parent
-WEBHOOK_FILE = CLAUDE_FOLDER / ".secrets" / "slack_webhook_street_account.txt"
-ENV_VAR = "SLACK_WEBHOOK_STREET_ACCOUNT"
-
-POST_TIMEOUT_SEC = 10
-SLACK_SECTION_TEXT_LIMIT = 3000
-
-# Backoff for the wake-race: a scheduled task that catches up on wake
-# (StartWhenAvailable) can fire before DNS/WiFi is up, so the first POST dies
-# with a transient transport error. Retry rides through it. See CONVENTIONS s3.
-_RETRY_BACKOFF = (5, 15, 30)  # seconds to wait BEFORE retry attempts 2..N
-
-
-def resolve_webhook_url(env_var: str = ENV_VAR,
-                        file_path: Path = WEBHOOK_FILE) -> str:
-    env = os.environ.get(env_var)
-    if env:
-        return env.strip()
-    if file_path.exists():
-        return file_path.read_text(encoding="utf-8").strip()
-    raise SystemExit(
-        f"No Slack webhook configured. Set the {env_var} env var or write the "
-        f"webhook URL (one line) to {file_path}."
-    )
-
-
-def build_halt_blocks(event: HaltEvent, meta: Optional[TickerMeta],
-                      *, note_context: Optional[str] = None) -> dict:
-    name = (meta.name if meta else "") or event.name or event.symbol
-    date_short = _format_date_short(event.halt_date)
-    time_hhmm = _format_time_hhmm(event.halt_time)
-
-    headline = f":no_entry: *SA:* `{event.symbol}` — {name} halted, news pending"
-    if event.last_price is not None:
-        headline += f"  ·  {_format_price(event.last_price)}"
-
-    reason_line = (
-        f"`{time_hhmm} ET {date_short}`  ·  reason `{event.reason_code} - "
-        f"{event.reason_description}`"
-    )
-    if meta and meta.sector:
-        sector_str = meta.sector
-        if meta.subsector:
-            sector_str += f" / {meta.subsector}"
-        reason_line += f"  ·  Sector: {sector_str}"
-
-    source_line = f"_source: {event.source} ({event.exchange})_"
-    lines = [headline, reason_line]
-    if note_context:
-        lines.append(f"_{note_context}_")
-    # Lean halt->triage hand-off (option A): biotech halts carry a copy-pasteable
-    # triage nudge. See root biotech_catalyst_architecture_plan.md §3.
-    if meta and is_biotech(meta.subsector):
-        lines.append(biotech_triage_cta(event.symbol))
-    lines.append(source_line)
-    text_block = "\n".join(lines)
-    fallback = (
-        f"SA: {event.symbol} {name} halted, news pending "
-        f"({event.reason_code} - {event.reason_description})"
-    )
-    return {
-        "blocks": [
-            {
-                "type": "section",
-                "text": {"type": "mrkdwn", "text": text_block[:SLACK_SECTION_TEXT_LIMIT]},
-            }
-        ],
-        "text": fallback,
-    }
-
-
-def build_resume_blocks(event: HaltEvent, meta: Optional[TickerMeta]) -> dict:
-    name = (meta.name if meta else "") or event.name or event.symbol
-    date_short = _format_date_short(event.halt_date)
-    halt_hhmm = _format_time_hhmm(event.halt_time)
-    resume_hhmm = _format_time_hhmm(event.resume_trade_time or "")
-
-    headline = (
-        f":white_check_mark: *SA:* `{event.symbol}` — shares to resume trading "
-        f"at {resume_hhmm} ET"
-    )
-    detail = (
-        f"`{date_short}`  ·  resume `{resume_hhmm} ET`  ·  originally halted "
-        f"`{halt_hhmm} ET`  ·  reason `{event.reason_code} - "
-        f"{event.reason_description}`"
-    )
-    if meta and meta.sector:
-        sector_str = meta.sector
-        if meta.subsector:
-            sector_str += f" / {meta.subsector}"
-        detail += f"  ·  Sector: {sector_str}"
-
-    source_line = f"_source: {event.source} ({event.exchange})_"
-    text_block = "\n".join([headline, detail, source_line])
-    fallback = (
-        f"SA: {event.symbol} resume at {resume_hhmm} ET (originally halted "
-        f"{halt_hhmm} ET, {event.reason_code})"
-    )
-    return {
-        "blocks": [
-            {
-                "type": "section",
-                "text": {"type": "mrkdwn", "text": text_block[:SLACK_SECTION_TEXT_LIMIT]},
-            }
-        ],
-        "text": fallback,
-    }
-
-
-def build_followup_blocks(halt: HaltEvent, item: NewsItem,
-                          meta: Optional[TickerMeta], *,
-                          last_price: Optional[float] = None) -> dict:
-    """Block Kit for a §7 Follow-up alert — the substantive news that broke on a
-    previously-halted covered name. Mirrors build_halt_blocks; references the
-    original halt in-body (no Slack threading over a webhook)."""
-    symbol = halt.symbol
-    name = (meta.name if meta else "") or halt.name or symbol
-    time_hhmm, date_short = followup_when(halt, item)
-    headline = followup_headline(item)
-
-    header = f":newspaper: *SA:* `{symbol}` — Follow-up: {headline}"
-    price = last_price if last_price is not None else halt.last_price
-    if price is not None:
-        header += f"  ·  {_format_price(price)}"
-
-    halt_hhmm = _format_time_hhmm(halt.halt_time)
-    detail = f"`{time_hhmm} ET {date_short}`  ·  Follows the {halt_hhmm} ET halt on `{symbol}`"
-    if meta and meta.sector:
-        sector_str = meta.sector
-        if meta.subsector:
-            sector_str += f" / {meta.subsector}"
-        detail += f"  ·  Sector: {sector_str}"
-
-    source_line = f"_source: {followup_source_label(item)} press release_"
-    text_block = "\n".join([header, detail, source_line])
-    fallback = f"SA: {symbol} Follow-up: {headline}"
-    return {
-        "blocks": [
-            {
-                "type": "section",
-                "text": {"type": "mrkdwn", "text": text_block[:SLACK_SECTION_TEXT_LIMIT]},
-            }
-        ],
-        "text": fallback,
-    }
-
-
-def build_dm_blocks(message: str, *, level: str = "warning") -> dict:
-    emoji = {"ok": ":white_check_mark:", "warning": ":warning:", "error": ":x:"}.get(
-        level, ":warning:"
-    )
-    text = f"{emoji} *sa-monitor*: {message}"
-    return {
-        "blocks": [{"type": "section", "text": {"type": "mrkdwn", "text": text}}],
-        "text": f"sa-monitor: {message}",
-    }
-
-
-def post_payload(payload: dict, *, webhook_url: Optional[str] = None,
-                 timeout: int = POST_TIMEOUT_SEC) -> requests.Response:
-    url = webhook_url or resolve_webhook_url()
-    headers = {"Content-Type": "application/json"}
-    log.debug("slack: POST (url redacted)")
-    body = json.dumps(payload)
-    # Retry only transient transport errors (DNS-not-ready on wake, etc.); a
-    # successful-but-bad-status response is left to raise_for_status as before.
-    attempts = len(_RETRY_BACKOFF) + 1
-    last_exc = None
-    for i in range(attempts):
-        try:
-            resp = requests.post(url, headers=headers, data=body, timeout=timeout)
-            break
-        except requests.exceptions.RequestException as exc:
-            last_exc = exc
-            if i == attempts - 1:
-                raise
-            delay = _RETRY_BACKOFF[i]
-            log.warning("slack: POST attempt %d/%d failed (%s); retrying in %ds",
-                        i + 1, attempts, exc, delay)
-            if not os.environ.get("PYTEST_CURRENT_TEST"):
-                time.sleep(delay)
-    resp.raise_for_status()
-    return resp
-
-
-def post_halt(event: HaltEvent, meta: Optional[TickerMeta], *,
-              webhook_url: Optional[str] = None,
-              dry_run: bool = False,
-              note_context: Optional[str] = None) -> Optional[dict]:
-    payload = build_halt_blocks(event, meta, note_context=note_context)
-    if dry_run:
-        log.info("slack[dry-run] halt payload for %s:\n%s", event.symbol,
-                 json.dumps(payload, indent=2))
-        return payload
-    post_payload(payload, webhook_url=webhook_url)
-    return payload
-
-
-def post_resume(event: HaltEvent, meta: Optional[TickerMeta], *,
-                webhook_url: Optional[str] = None,
-                dry_run: bool = False) -> Optional[dict]:
-    payload = build_resume_blocks(event, meta)
-    if dry_run:
-        log.info("slack[dry-run] resume payload for %s:\n%s", event.symbol,
-                 json.dumps(payload, indent=2))
-        return payload
-    post_payload(payload, webhook_url=webhook_url)
-    return payload
-
-
-def post_followup(halt: HaltEvent, item: NewsItem, meta: Optional[TickerMeta], *,
-                  webhook_url: Optional[str] = None,
-                  dry_run: bool = False,
-                  last_price: Optional[float] = None) -> Optional[dict]:
-    payload = build_followup_blocks(halt, item, meta, last_price=last_price)
-    if dry_run:
-        log.info("slack[dry-run] followup payload for %s:\n%s", halt.symbol,
-                 json.dumps(payload, indent=2))
-        return payload
-    post_payload(payload, webhook_url=webhook_url)
-    return payload
-
-
-def post_dm(message: str, *, level: str = "warning",
-            webhook_url: Optional[str] = None,
-            dry_run: bool = False) -> Optional[dict]:
-    payload = build_dm_blocks(message, level=level)
-    if dry_run:
-        log.info("slack[dry-run] dm payload:\n%s", json.dumps(payload, indent=2))
-        return payload
-    post_payload(payload, webhook_url=webhook_url)
-    return payload
+"""Slack delivery for sa-monitor halt + resume notifications.
+
+Posts to the consolidated #street-account channel for ALL sa-monitor alerts
+across Phases 1-5. Block Kit format per HEALTH_REPORTING.md §4.1.
+
+Webhook source-of-truth:
+- SLACK_WEBHOOK_STREET_ACCOUNT env var, OR
+- Claude Folder/.secrets/slack_webhook_street_account.txt (one line)
+"""
+from __future__ import annotations
+
+import json
+import logging
+import os
+import time
+from pathlib import Path
+from typing import Optional
+
+import requests
+
+from .coverage import TickerMeta
+from .events.types import (
+    CLEARANCE,
+    CRL,
+    DIR_MET,
+    DIR_MISSED,
+    FDA_APPROVAL,
+    TRIAL_READOUT,
+    HCEvent,
+)
+from .feeds.types import HaltEvent
+from .news.types import NewsItem
+from .template import (
+    _NEWS_SOURCE_LABELS,
+    _format_date_short,
+    _format_price,
+    _format_time_hhmm,
+    biotech_triage_cta,
+    followup_headline,
+    followup_source_label,
+    followup_when,
+    hc_event_cta_context,
+    hc_event_headline,
+    hc_event_when,
+    is_biotech,
+)
+
+log = logging.getLogger(__name__)
+
+REPO_ROOT = Path(__file__).resolve().parents[1]
+CLAUDE_FOLDER = REPO_ROOT.parent
+WEBHOOK_FILE = CLAUDE_FOLDER / ".secrets" / "slack_webhook_street_account.txt"
+ENV_VAR = "SLACK_WEBHOOK_STREET_ACCOUNT"
+
+POST_TIMEOUT_SEC = 10
+SLACK_SECTION_TEXT_LIMIT = 3000
+
+# Backoff for the wake-race: a scheduled task that catches up on wake
+# (StartWhenAvailable) can fire before DNS/WiFi is up, so the first POST dies
+# with a transient transport error. Retry rides through it. See CONVENTIONS s3.
+_RETRY_BACKOFF = (5, 15, 30)  # seconds to wait BEFORE retry attempts 2..N
+
+
+def resolve_webhook_url(env_var: str = ENV_VAR,
+                        file_path: Path = WEBHOOK_FILE) -> str:
+    env = os.environ.get(env_var)
+    if env:
+        return env.strip()
+    if file_path.exists():
+        return file_path.read_text(encoding="utf-8").strip()
+    raise SystemExit(
+        f"No Slack webhook configured. Set the {env_var} env var or write the "
+        f"webhook URL (one line) to {file_path}."
+    )
+
+
+def build_halt_blocks(event: HaltEvent, meta: Optional[TickerMeta],
+                      *, note_context: Optional[str] = None) -> dict:
+    name = (meta.name if meta else "") or event.name or event.symbol
+    date_short = _format_date_short(event.halt_date)
+    time_hhmm = _format_time_hhmm(event.halt_time)
+
+    headline = f":no_entry: *SA:* `{event.symbol}` — {name} halted, news pending"
+    if event.last_price is not None:
+        headline += f"  ·  {_format_price(event.last_price)}"
+
+    reason_line = (
+        f"`{time_hhmm} ET {date_short}`  ·  reason `{event.reason_code} - "
+        f"{event.reason_description}`"
+    )
+    if meta and meta.sector:
+        sector_str = meta.sector
+        if meta.subsector:
+            sector_str += f" / {meta.subsector}"
+        reason_line += f"  ·  Sector: {sector_str}"
+
+    source_line = f"_source: {event.source} ({event.exchange})_"
+    lines = [headline, reason_line]
+    if note_context:
+        lines.append(f"_{note_context}_")
+    # Lean halt->triage hand-off (option A): biotech halts carry a copy-pasteable
+    # triage nudge. See root biotech_catalyst_architecture_plan.md §3.
+    if meta and is_biotech(meta.subsector):
+        lines.append(biotech_triage_cta(event.symbol))
+    lines.append(source_line)
+    text_block = "\n".join(lines)
+    fallback = (
+        f"SA: {event.symbol} {name} halted, news pending "
+        f"({event.reason_code} - {event.reason_description})"
+    )
+    return {
+        "blocks": [
+            {
+                "type": "section",
+                "text": {"type": "mrkdwn", "text": text_block[:SLACK_SECTION_TEXT_LIMIT]},
+            }
+        ],
+        "text": fallback,
+    }
+
+
+def build_resume_blocks(event: HaltEvent, meta: Optional[TickerMeta]) -> dict:
+    name = (meta.name if meta else "") or event.name or event.symbol
+    date_short = _format_date_short(event.halt_date)
+    halt_hhmm = _format_time_hhmm(event.halt_time)
+    resume_hhmm = _format_time_hhmm(event.resume_trade_time or "")
+
+    headline = (
+        f":white_check_mark: *SA:* `{event.symbol}` — shares to resume trading "
+        f"at {resume_hhmm} ET"
+    )
+    detail = (
+        f"`{date_short}`  ·  resume `{resume_hhmm} ET`  ·  originally halted "
+        f"`{halt_hhmm} ET`  ·  reason `{event.reason_code} - "
+        f"{event.reason_description}`"
+    )
+    if meta and meta.sector:
+        sector_str = meta.sector
+        if meta.subsector:
+            sector_str += f" / {meta.subsector}"
+        detail += f"  ·  Sector: {sector_str}"
+
+    source_line = f"_source: {event.source} ({event.exchange})_"
+    text_block = "\n".join([headline, detail, source_line])
+    fallback = (
+        f"SA: {event.symbol} resume at {resume_hhmm} ET (originally halted "
+        f"{halt_hhmm} ET, {event.reason_code})"
+    )
+    return {
+        "blocks": [
+            {
+                "type": "section",
+                "text": {"type": "mrkdwn", "text": text_block[:SLACK_SECTION_TEXT_LIMIT]},
+            }
+        ],
+        "text": fallback,
+    }
+
+
+def build_followup_blocks(halt: HaltEvent, item: NewsItem,
+                          meta: Optional[TickerMeta], *,
+                          last_price: Optional[float] = None) -> dict:
+    """Block Kit for a §7 Follow-up alert — the substantive news that broke on a
+    previously-halted covered name. Mirrors build_halt_blocks; references the
+    original halt in-body (no Slack threading over a webhook)."""
+    symbol = halt.symbol
+    name = (meta.name if meta else "") or halt.name or symbol
+    time_hhmm, date_short = followup_when(halt, item)
+    headline = followup_headline(item)
+
+    header = f":newspaper: *SA:* `{symbol}` — Follow-up: {headline}"
+    price = last_price if last_price is not None else halt.last_price
+    if price is not None:
+        header += f"  ·  {_format_price(price)}"
+
+    halt_hhmm = _format_time_hhmm(halt.halt_time)
+    detail = f"`{time_hhmm} ET {date_short}`  ·  Follows the {halt_hhmm} ET halt on `{symbol}`"
+    if meta and meta.sector:
+        sector_str = meta.sector
+        if meta.subsector:
+            sector_str += f" / {meta.subsector}"
+        detail += f"  ·  Sector: {sector_str}"
+
+    source_line = f"_source: {followup_source_label(item)} press release_"
+    text_block = "\n".join([header, detail, source_line])
+    fallback = f"SA: {symbol} Follow-up: {headline}"
+    return {
+        "blocks": [
+            {
+                "type": "section",
+                "text": {"type": "mrkdwn", "text": text_block[:SLACK_SECTION_TEXT_LIMIT]},
+            }
+        ],
+        "text": fallback,
+    }
+
+
+def _hc_event_emoji(event: HCEvent) -> str:
+    """Event-type emoji per design §5. Trial readouts split on direction so a
+    'met' and a 'missed' are visually distinct at a glance."""
+    if event.event_type == FDA_APPROVAL:
+        return ":pill:"
+    if event.event_type == CRL:
+        return ":x:"
+    if event.event_type == CLEARANCE:
+        return ":heavy_check_mark:"
+    if event.event_type == TRIAL_READOUT:
+        if event.direction == DIR_MISSED:
+            return ":small_red_triangle_down:"
+        if event.direction == DIR_MET:
+            return ":test_tube:"
+        return ":microscope:"  # topline, direction TBD
+    return ":pill:"
+
+
+def build_hc_event_blocks(event: HCEvent, meta: Optional[TickerMeta]) -> dict:
+    """Block Kit for an HC event-wire alert — an FDA action or clinical readout
+    on a covered name (design §5, templates §15/§16/§19). Single-section mrkdwn,
+    mirroring build_followup_blocks. Headline is the issuer's verbatim title;
+    the fact-dense efficacy body is a deferred LLM enhancement."""
+    symbol = event.symbol
+    headline = hc_event_headline(event.headline)
+    time_hhmm, date_short = hc_event_when(event.published_at)
+
+    header = f"{_hc_event_emoji(event)} *SA:* `{symbol}` — {headline}"
+
+    when = f"`{time_hhmm} ET {date_short}`" if time_hhmm else ""
+    detail_bits = [when] if when else []
+    if meta and meta.sector:
+        sector_str = meta.sector
+        if meta.subsector:
+            sector_str += f" / {meta.subsector}"
+        detail_bits.append(f"Sector: {sector_str}")
+    detail = "  ·  ".join(detail_bits)
+
+    label = _NEWS_SOURCE_LABELS.get(event.source, event.source)
+    source_line = f"_source: {label} press release"
+    if event.url:
+        source_line += f" · {event.url}"
+    source_line += "_"
+
+    lines = [header]
+    if detail:
+        lines.append(detail)
+    if meta and is_biotech(meta.subsector):
+        lines.append(biotech_triage_cta(symbol, context=hc_event_cta_context(event)))
+    lines.append(source_line)
+    text_block = "\n".join(lines)
+    fallback = f"SA: {symbol} — {headline}"
+    return {
+        "blocks": [
+            {
+                "type": "section",
+                "text": {"type": "mrkdwn", "text": text_block[:SLACK_SECTION_TEXT_LIMIT]},
+            }
+        ],
+        "text": fallback,
+    }
+
+
+def build_dm_blocks(message: str, *, level: str = "warning") -> dict:
+    emoji = {"ok": ":white_check_mark:", "warning": ":warning:", "error": ":x:"}.get(
+        level, ":warning:"
+    )
+    text = f"{emoji} *sa-monitor*: {message}"
+    return {
+        "blocks": [{"type": "section", "text": {"type": "mrkdwn", "text": text}}],
+        "text": f"sa-monitor: {message}",
+    }
+
+
+def post_payload(payload: dict, *, webhook_url: Optional[str] = None,
+                 timeout: int = POST_TIMEOUT_SEC) -> requests.Response:
+    url = webhook_url or resolve_webhook_url()
+    headers = {"Content-Type": "application/json"}
+    log.debug("slack: POST (url redacted)")
+    body = json.dumps(payload)
+    # Retry only transient transport errors (DNS-not-ready on wake, etc.); a
+    # successful-but-bad-status response is left to raise_for_status as before.
+    attempts = len(_RETRY_BACKOFF) + 1
+    last_exc = None
+    for i in range(attempts):
+        try:
+            resp = requests.post(url, headers=headers, data=body, timeout=timeout)
+            break
+        except requests.exceptions.RequestException as exc:
+            last_exc = exc
+            if i == attempts - 1:
+                raise
+            delay = _RETRY_BACKOFF[i]
+            log.warning("slack: POST attempt %d/%d failed (%s); retrying in %ds",
+                        i + 1, attempts, exc, delay)
+            if not os.environ.get("PYTEST_CURRENT_TEST"):
+                time.sleep(delay)
+    resp.raise_for_status()
+    return resp
+
+
+def post_halt(event: HaltEvent, meta: Optional[TickerMeta], *,
+              webhook_url: Optional[str] = None,
+              dry_run: bool = False,
+              note_context: Optional[str] = None) -> Optional[dict]:
+    payload = build_halt_blocks(event, meta, note_context=note_context)
+    if dry_run:
+        log.info("slack[dry-run] halt payload for %s:\n%s", event.symbol,
+                 json.dumps(payload, indent=2))
+        return payload
+    post_payload(payload, webhook_url=webhook_url)
+    return payload
+
+
+def post_resume(event: HaltEvent, meta: Optional[TickerMeta], *,
+                webhook_url: Optional[str] = None,
+                dry_run: bool = False) -> Optional[dict]:
+    payload = build_resume_blocks(event, meta)
+    if dry_run:
+        log.info("slack[dry-run] resume payload for %s:\n%s", event.symbol,
+                 json.dumps(payload, indent=2))
+        return payload
+    post_payload(payload, webhook_url=webhook_url)
+    return payload
+
+
+def post_followup(halt: HaltEvent, item: NewsItem, meta: Optional[TickerMeta], *,
+                  webhook_url: Optional[str] = None,
+                  dry_run: bool = False,
+                  last_price: Optional[float] = None) -> Optional[dict]:
+    payload = build_followup_blocks(halt, item, meta, last_price=last_price)
+    if dry_run:
+        log.info("slack[dry-run] followup payload for %s:\n%s", halt.symbol,
+                 json.dumps(payload, indent=2))
+        return payload
+    post_payload(payload, webhook_url=webhook_url)
+    return payload
+
+
+def post_hc_event(event: HCEvent, meta: Optional[TickerMeta], *,
+                  webhook_url: Optional[str] = None,
+                  dry_run: bool = False) -> Optional[dict]:
+    payload = build_hc_event_blocks(event, meta)
+    if dry_run:
+        log.info("slack[dry-run] hc_event payload for %s:\n%s", event.symbol,
+                 json.dumps(payload, indent=2))
+        return payload
+    post_payload(payload, webhook_url=webhook_url)
+    return payload
+
+
+def post_dm(message: str, *, level: str = "warning",
+            webhook_url: Optional[str] = None,
+            dry_run: bool = False) -> Optional[dict]:
+    payload = build_dm_blocks(message, level=level)
+    if dry_run:
+        log.info("slack[dry-run] dm payload:\n%s", json.dumps(payload, indent=2))
+        return payload
+    post_payload(payload, webhook_url=webhook_url)
+    return payload
diff --git a/src/state.py b/src/state.py
index baaa029..dd4b19f 100644
--- a/src/state.py
+++ b/src/state.py
@@ -64,6 +64,7 @@ def save(tracker: HaltTracker, *, state_dir: Path = DEFAULT_STATE_DIR,
         "resumes_emitted": [list(hid) for hid in tracker.resumes_emitted],
         "emitted_halts": [list(hid) for hid in tracker.emitted_halts],
         "followed_up": [list(hid) for hid in tracker.followed_up],
+        "hc_events_emitted": sorted(tracker.hc_events_emitted),
     }
 
     # Atomic write: tmp file → rename
@@ -106,6 +107,8 @@ def load(*, state_dir: Path = DEFAULT_STATE_DIR, day_key: str = "") -> HaltTrack
     # simply lack them and rehydrate to empty sets.
     emitted_halts = {tuple(e) for e in payload.get("emitted_halts", [])}
     followed_up = {tuple(f) for f in payload.get("followed_up", [])}
+    # HC event-wire delivery-dedup — string keys, not HaltId tuples.
+    hc_events_emitted = set(payload.get("hc_events_emitted", []))
 
     # Pre-fill seen_halts with placeholder HaltEvents — we only need the
     # halt_ids to be populated so dedup works; the full event history isn't
@@ -124,9 +127,12 @@ def load(*, state_dir: Path = DEFAULT_STATE_DIR, day_key: str = "") -> HaltTrack
     tracker.resumes_emitted = resumes
     tracker.emitted_halts = emitted_halts
     tracker.followed_up = followed_up
+    tracker.hc_events_emitted = hc_events_emitted
     log.info(
-        "state: rehydrated from %s (%d halts, %d resumes, %d emitted, %d followed_up)",
+        "state: rehydrated from %s (%d halts, %d resumes, %d emitted, "
+        "%d followed_up, %d hc_events)",
         path, len(halts), len(resumes), len(emitted_halts), len(followed_up),
+        len(hc_events_emitted),
     )
     return tracker
 
diff --git a/src/template.py b/src/template.py
index 3c22d2e..ca73c53 100644
--- a/src/template.py
+++ b/src/template.py
@@ -68,14 +68,18 @@ def is_biotech(subsector: str) -> bool:
     return (subsector or "").strip().casefold() == "biotech"
 
 
-def biotech_triage_cta(symbol: str) -> str:
-    """Lean halt->triage hand-off nudge for a biotech halt (option A, 2026-06-16).
+def biotech_triage_cta(symbol: str, context: str = "halt") -> str:
+    """Lean ->triage hand-off nudge for a biotech name (option A, 2026-06-16).
 
     sa-monitor can't auto-invoke the Claude-driven biotech_triage, so instead of
     pretending to trigger it we surface a copy-pasteable command. See the root
     biotech_catalyst_architecture_plan.md (§3) for why this is human-mediated.
+
+    `context` names what triggered the nudge — "halt" (default, preserves the
+    halt/resume wording verbatim) or an HC-event label ("readout"/"approval"/…)
+    so a non-halt HC event doesn't misleadingly read "Biotech halt".
     """
-    return f":dna: Biotech halt — triage this name?  ->  `triage {symbol}`"
+    return f":dna: Biotech {context} — triage this name?  ->  `triage {symbol}`"
 
 
 def render_halt(event: HaltEvent, *, sector: str = "", subsector: str = "",
@@ -208,6 +212,78 @@ def render_followup(halt: HaltEvent, item: NewsItem, *, sector: str = "",
     return "\n".join(parts)
 
 
+def hc_event_when(published_at: str) -> tuple[str, str]:
+    """(time_hhmm, date_short) in ET for an HC event, from its PR publish time.
+
+    Falls back to ("", "") if the timestamp is missing/malformed — the caller
+    then simply omits the stamp rather than printing garbage.
+    """
+    et = _iso_utc_to_et(published_at)
+    if et is None:
+        return ("", "")
+    return (f"{et.hour:02d}:{et.minute:02d}",
+            f"{et.month}/{et.day:02d}/{et.year % 100:02d}")
+
+
+def hc_event_headline(headline: str) -> str:
+    """The event headline, verbatim from the issuer, capped for readability.
+
+    SA reproduces issuer language; v1 delivers the issuer's own title (the
+    fact-dense §15/§19 efficacy body is a deferred LLM enhancement).
+    """
+    headline = (headline or "").strip()
+    if len(headline) > _FOLLOWUP_HEADLINE_LIMIT:
+        headline = headline[:_FOLLOWUP_HEADLINE_LIMIT].rsplit(" ", 1)[0] + "…"
+    return headline
+
+
+# Human label for an HC event type, used in the biotech triage CTA so it reads
+# "Biotech readout — triage this name?" rather than the halt path's "Biotech halt".
+_HC_CTA_CONTEXT = {
+    "trial_readout": "readout",
+    "fda_approval": "approval",
+    "crl": "CRL",
+    "clearance": "clearance",
+}
+
+
+def hc_event_cta_context(event) -> str:
+    """CTA context word for an HCEvent (falls back to a generic 'event')."""
+    return _HC_CTA_CONTEXT.get(event.event_type, "event")
+
+
+def render_hc_event(event, *, sector: str = "", subsector: str = "") -> str:
+    """Render an HC event-wire alert to plain text (stdout / --slack off).
+
+    event is an events.types.HCEvent. Mirrors render_followup's shape:
+
+        HH:MM ET M/DD/YY [StreetAccount] TICKER {verbatim issuer headline}
+        Sector: {sector} / {subsector}
+        [biotech triage CTA if biotech]
+        Source: {Wire} press release · {url}
+    """
+    time_hhmm, date_short = hc_event_when(event.published_at)
+    headline = hc_event_headline(event.headline)
+
+    when = f"{time_hhmm} ET {date_short} ".lstrip() if time_hhmm else ""
+    header = f"{when}[StreetAccount] {event.symbol} {headline}"
+
+    parts = [header]
+    if sector:
+        sector_line = f"Sector: {sector}"
+        if subsector:
+            sector_line += f" / {subsector}"
+        parts.append(sector_line)
+    if is_biotech(subsector):
+        parts.append(biotech_triage_cta(event.symbol, context=hc_event_cta_context(event)))
+    label = _NEWS_SOURCE_LABELS.get(event.source, event.source)
+    source_line = f"Source: {label} press release"
+    if event.url:
+        source_line += f" · {event.url}"
+    parts.append(source_line)
+    return "\n".join(parts)
+
+
 def render_resume(event: HaltEvent, *, sector: str = "", subsector: str = "",
                   name_override: str = "") -> str:
     """Render a resume notification — sa-monitor variant.
diff --git a/tests/test_hc_events.py b/tests/test_hc_events.py
new file mode 100644
index 0000000..b640d42
--- /dev/null
+++ b/tests/test_hc_events.py
@@ -0,0 +1,399 @@
+"""Tests for the HC event-wire lane — classify() + _emit_hc_events.
+
+Two halves:
+
+1. classify() precision — a POSITIVE set anchored to the locked real headlines
+   in template-library.md §15/§16/§19, and a NEGATIVE set of headlines that
+   must NOT fire (presentations, trial initiation/enrollment, earnings/
+   financing/personnel PRs, pending FDA milestones, boilerplate "FDA-cleared").
+   A wrong "Phase 3 met" / "FDA approval" alert is worse than a miss, so the
+   NEGATIVE set is the load-bearing guard.
+
+2. _emit_hc_events lifecycle — detection, covered-only filter, dedup, and the
+   slack-mode delivery gate (off marks, live marks only on post success,
+   dry-run marks nothing), mirroring tests/test_followup.py.
+"""
+from datetime import datetime, timezone
+from unittest.mock import patch
+
+import pytest
+
+from src import state
+from src.coverage import TickerMeta
+from src.dedup import HaltTracker
+from src.events.classify import classify
+from src.events.types import (
+    CLEARANCE,
+    CRL,
+    DIR_MET,
+    DIR_MISSED,
+    DIR_NA,
+    FDA_APPROVAL,
+    TRIAL_READOUT,
+)
+from src.halt_monitor import RunStats, _emit_hc_events
+from src.news.types import NewsItem
+
+
+# --------------------------------------------------------------------------
+# Fixtures / helpers
+# --------------------------------------------------------------------------
+
+class StubUniverse:
+    """Minimal Universe stand-in — _emit_hc_events only calls .get()."""
+
+    def __init__(self, mapping):
+        self._m = {k.upper(): v for k, v in mapping.items()}
+
+    def get(self, symbol):
+        return self._m.get(symbol.upper())
+
+
+def _meta(symbol="VRDN", subsector="Biotech"):
+    return TickerMeta(symbol=symbol, name=f"{symbol} Inc",
+                      sector="Biopharma", subsector=subsector)
+
+
+def _item(title, *, tickers=("VRDN",), source="prnewswire",
+          url=None, pub="2026-05-05T11:05:00+00:00"):
+    return NewsItem(
+        source=source, title=title, body="body text",
+        url=url or f"https://prn.test/{abs(hash(title)) % 10**8}",
+        published_at=pub, tickers=tickers,
+    )
+
+
+# --------------------------------------------------------------------------
+# 1a. classify() — POSITIVE set (anchored to §15 / §16 / §19)
+# --------------------------------------------------------------------------
+
+POSITIVE = [
+    # (title, event_type, direction, phase)
+    # §16 FDA approval — issuer-press-release variant (ARVN 2026-05-01 real capture)
+    ("Arvinas issues press release on FDA approval of VEPPANU (vepdegestrant) "
+     "for the treatment of ESR1M, ER+/HER2- advanced breast cancer",
+     FDA_APPROVAL, DIR_NA, ""),
+    # §16 FDA approval — headline variant
+    ("FDA approves Veppanu for ESR1M, ER+/HER2- advanced breast cancer",
+     FDA_APPROVAL, DIR_NA, ""),
+    ("Acme Pharma Receives FDA Approval for Acmezumab in metastatic melanoma",
+     FDA_APPROVAL, DIR_NA, ""),
+    ("FDA grants accelerated approval of Novadrug for rare genetic disorder",
+     FDA_APPROVAL, DIR_NA, ""),
+    # §19 trial readout — MET (VRDN 2026-05-05 real capture)
+    ("Viridian reports Phase 3 REVEAL-2 trial of elegrobart in chronic thyroid "
+     "eye disease met its primary endpoint",
+     TRIAL_READOUT, DIR_MET, "3"),
+    # §19 met dual primary
+    ("Cytokinetics reports ACACIA-HCM Phase 3 met dual primary endpoints",
+     TRIAL_READOUT, DIR_MET, "3"),
+    ("Newco achieves primary endpoint in Phase 2 study of newzumab",
+     TRIAL_READOUT, DIR_MET, "2"),
+    # §15 trial readout — MISSED (DAZALS / CEDAR real phrasings)
+    ("Corcept reports Phase 2 DAZALS study of dazucorilant does not meet "
+     "primary endpoint",
+     TRIAL_READOUT, DIR_MISSED, "2"),
+    ("Bigco reports Phase 2B CEDAR study of drugx did not meet its primary or "
+     "secondary efficacy endpoints",
+     TRIAL_READOUT, DIR_MISSED, "2"),
+    ("Smallco announces Phase 3 pivotal trial failed to meet primary endpoint",
+     TRIAL_READOUT, DIR_MISSED, "3"),
+    # topline, direction TBD (still a genuine readout crossing)
+    ("Genco announces topline results from Phase 3 SUNRISE trial of gencept",
+     TRIAL_READOUT, DIR_NA, "3"),
+    # CRL
+    ("Regenco receives Complete Response Letter from FDA for regzumab",
+     CRL, DIR_NA, ""),
+    # medtech clearance
+    ("Deviceco receives FDA clearance for its AI-powered cardiac monitor",
+     CLEARANCE, DIR_NA, ""),
+    ("Deviceco announces FDA 510(k) clearance of its next-gen catheter",
+     CLEARANCE, DIR_NA, ""),
+    # F5: idiomatic CRL still fires (only the BARE-token/ticker collision is fixed)
+    ("FDA issues CRL for acmezumab citing manufacturing deficiencies",
+     CRL, DIR_NA, ""),
+    ("Acme receives a CRL from the FDA for acmezumab",
+     CRL, DIR_NA, ""),
+    # F4 guard: a real MET readout that merely contains the word "expected"
+    # (not a future timeline) must STILL fire
+    ("Acme reports Phase 3 trial met primary endpoint, better than expected",
+     TRIAL_READOUT, DIR_MET, "3"),
+]
+
+
+@pytest.mark.parametrize("title,etype,direction,phase", POSITIVE)
+def test_classify_positive(title, etype, direction, phase):
+    ev = classify(_item(title))
+    assert ev is not None, f"expected an event, got None for: {title}"
+    assert ev.event_type == etype
+    assert ev.direction == direction
+    assert ev.phase == phase
+    assert ev.headline == title          # verbatim
+    assert ev.symbol == ""               # attribution decided in the emit path
+    assert ev.confidence == "high"
+
+
+# --------------------------------------------------------------------------
+# 1b. classify() — NEGATIVE set (must ALL return None)
+# --------------------------------------------------------------------------
+
+NEGATIVE = [
+    # presentations / future data disclosure (not a readout)
+    "Acme to present Phase 3 REVEAL data at the ASCO 2026 annual conference",
+    "Acme will present detailed Phase 2 results at an upcoming medical congress",
+    "Acme presents Phase 3 topline data in a late-breaking poster presentation",
+    "Acme to present topline Phase 2 data next week",
+    # trial lifecycle, not a readout
+    "Acme announces initiation of Phase 3 pivotal trial of acmezumab",
+    "Acme doses first patient in Phase 3 study of acmezumab",
+    "Acme completes enrollment in Phase 3 ELEVATE trial",
+    "Acme to host conference call to discuss Phase 3 trial design",
+    "Acme enrolls first patient in Phase 2 basket trial",
+    # non-clinical PRs (no readout, no approval)
+    "Acme reports first quarter 2026 financial results",
+    "Acme announces $150 million underwritten public offering",
+    "Acme appoints Jane Doe as Chief Executive Officer",
+    "Acme to participate in the Phase Capital healthcare investor conference",
+    # pending / procedural FDA milestones (NOT an approval)
+    "FDA accepts Acme's New Drug Application for acmezumab",
+    "FDA grants Priority Review to Acme's application for acmezumab",
+    "FDA grants Fast Track designation to acmezumab",
+    "FDA grants Breakthrough Therapy designation for acmezumab",
+    "Acme submits Biologics License Application to FDA seeking approval of acmezumab",
+    "Acme resubmits application for FDA approval of acmezumab following prior review",
+    # boilerplate adjective — must NOT read as a clearance event
+    "Acme opens new FDA-cleared manufacturing facility in North Carolina",
+    # ambiguous secondary-only readout — precision says skip
+    "Acme reports Phase 2 study met key secondary endpoint",
+
+    # --- Codex round-1 repros ------------------------------------------------
+    # F1: clearance path was UNGATED — pending / denial framings must not fire
+    "Acme submits 510(k) for FDA clearance of its cardiac monitor",
+    "Acme did not receive FDA clearance for its cardiac monitor",
+    "Acme fails to receive FDA clearance for its device",
+    "FDA denies 510(k) clearance for Acme's device",
+    "Acme withdraws 510(k) clearance application for its device",
+    # F2: negative-approval INVERSION — a rejection must never read as approval
+    "Acme did not receive FDA approval for acmezumab",
+    "Acme fails to win FDA approval for acmezumab",
+    "FDA rejects Acme's application; acmezumab not approved",
+    "Acme's acmezumab did not win FDA approval this cycle",
+    # F3: competitor / commentary approval attribution
+    "Acme comments on FDA approval of RivalDrug by a competitor",
+    "Acme issues statement on FDA approval of a rival therapy",
+    "Acme congratulates partner on FDA approval of RivalDrug",
+    # F4: future 'expected in' topline (adjectival, not caught by 'expects to')
+    "Acme announces topline Phase 3 data expected in Q4 2026",
+    "Acme Phase 3 SUNRISE topline readout expected in the second half of 2026",
+    "Acme on track to read out Phase 3 data later this year",
+    # F5: bare CRL collides with Charles River Labs (ticker CRL, covered)
+    "CRL Announces First-Quarter 2026 Financial Results",
+    "Charles River Laboratories (CRL) Announces New Facility in Massachusetts",
+]
+
+
+@pytest.mark.parametrize("title", NEGATIVE)
+def test_classify_negative(title):
+    assert classify(_item(title)) is None, f"MISFIRE: {title!r} should not classify"
+
+
+def test_classify_blank_title_is_none():
+    assert classify(_item("   ")) is None
+
+
+# --------------------------------------------------------------------------
+# F6 — biotech CTA wording: HC events must NOT say "Biotech halt"
+# --------------------------------------------------------------------------
+
+def test_hc_event_cta_does_not_say_halt(capsys):
+    from src import slack
+    from dataclasses import replace
+    item = _item("Viridian reports Phase 3 REVEAL-2 met its primary endpoint",
+                 tickers=("VRDN",))
+    universe = StubUniverse({"VRDN": _meta("VRDN")})
+    tracker = HaltTracker()
+    _run_emit([item], universe, tracker)
+    out = capsys.readouterr().out
+    assert "Biotech readout" in out            # event-appropriate wording
+    assert "Biotech halt" not in out           # F6: not the halt wording
+
+    # Slack block carries the same corrected wording.
+    ev = replace(classify(item), symbol="VRDN")
+    blocks = slack.build_hc_event_blocks(ev, _meta("VRDN"))
+    text = blocks["blocks"][0]["text"]["text"]
+    assert "Biotech readout" in text and "Biotech halt" not in text
+
+
+def test_biotech_triage_cta_halt_wording_unchanged():
+    """The halt/resume path must still read 'Biotech halt' (default context)."""
+    from src.template import biotech_triage_cta
+    assert biotech_triage_cta("VRDN") == \
+        ":dna: Biotech halt — triage this name?  ->  `triage VRDN`"
+    assert biotech_triage_cta("VRDN", context="approval").startswith(
+        ":dna: Biotech approval —")
+
+
+# --------------------------------------------------------------------------
+# 2. _emit_hc_events — lifecycle / gating
+# --------------------------------------------------------------------------
+
+def _run_emit(news_items, universe, tracker, *, slack_mode="off"):
+    stats = RunStats(started_at="t")
+    _emit_hc_events(news_items, universe, tracker, None,
+                    stats=stats, slack_mode=slack_mode)
+    return stats
+
+
+def test_covered_readout_emits_and_renders(capsys):
+    item = _item("Viridian reports Phase 3 REVEAL-2 trial of elegrobart in "
+                 "chronic thyroid eye disease met its primary endpoint",
+                 tickers=("VRDN",))
+    universe = StubUniverse({"VRDN": _meta("VRDN")})
+    tracker = HaltTracker()
+
+    stats = _run_emit([item], universe, tracker)
+    assert stats.hc_events_emitted == 1
+    key = f"{item.news_id}|VRDN|{TRIAL_READOUT}"
+    assert key in tracker.hc_events_emitted
+    out = capsys.readouterr().out
+    assert "VRDN" in out
+    assert "met its primary endpoint" in out
+    assert "[StreetAccount]" in out
+
+
+def test_negative_item_emits_nothing(capsys):
+    item = _item("Acme to present Phase 3 REVEAL data at the ASCO 2026 "
+                 "annual conference", tickers=("VRDN",))
+    universe = StubUniverse({"VRDN": _meta("VRDN")})
+    tracker = HaltTracker()
+
+    stats = _run_emit([item], universe, tracker)
+    assert stats.hc_events_emitted == 0
+    assert tracker.hc_events_emitted == set()
+    assert capsys.readouterr().out.strip() == ""
+
+
+def test_uncovered_ticker_never_fires():
+    item = _item("FDA approves Zzzdrug for advanced disease", tickers=("ZZZZ",))
+    universe = StubUniverse({"VRDN": _meta("VRDN")})  # ZZZZ not covered
+    tracker = HaltTracker()
+
+    stats = _run_emit([item], universe, tracker)
+    assert stats.hc_events_emitted == 0
+    assert tracker.hc_events_emitted == set()
+
+
+def test_item_with_no_tickers_never_fires():
+    item = _item("FDA approves Somedrug for advanced disease", tickers=())
+    universe = StubUniverse({"VRDN": _meta("VRDN")})
+    tracker = HaltTracker()
+
+    stats = _run_emit([item], universe, tracker)
+    assert stats.hc_events_emitted == 0
+
+
+def test_partner_ticker_covered_fires_for_partner():
+    # Small biotech's approval names its covered large-pharma partner — both
+    # tickers land in item.tickers; the covered one fires.
+    item = _item("Arvinas issues press release on FDA approval of VEPPANU for "
+                 "the treatment of ER+/HER2- advanced breast cancer",
+                 tickers=("ARVN", "PFE"))
+    universe = StubUniverse({"PFE": _meta("PFE", subsector="Pharma")})  # only PFE covered
+    tracker = HaltTracker()
+
+    stats = _run_emit([item], universe, tracker)
+    assert stats.hc_events_emitted == 1
+    assert f"{item.news_id}|PFE|{FDA_APPROVAL}" in tracker.hc_events_emitted
+
+
+def test_both_covered_tickers_each_fire_once():
+    item = _item("Arvinas issues press release on FDA approval of VEPPANU",
+                 tickers=("ARVN", "PFE"))
+    universe = StubUniverse({"ARVN": _meta("ARVN"), "PFE": _meta("PFE")})
+    tracker = HaltTracker()
+
+    stats = _run_emit([item], universe, tracker)
+    assert stats.hc_events_emitted == 2
+    assert f"{item.news_id}|ARVN|{FDA_APPROVAL}" in tracker.hc_events_emitted
+    assert f"{item.news_id}|PFE|{FDA_APPROVAL}" in tracker.hc_events_emitted
+
+
+def test_dedup_one_alert_per_pr_ticker_type_ever():
+    item = _item("FDA approves Veppanu for advanced breast cancer", tickers=("VRDN",))
+    universe = StubUniverse({"VRDN": _meta("VRDN")})
+    tracker = HaltTracker()
+
+    first = _run_emit([item], universe, tracker)
+    second = _run_emit([item], universe, tracker)  # same item re-fetched
+    assert first.hc_events_emitted == 1
+    assert second.hc_events_emitted == 0
+    assert len(tracker.hc_events_emitted) == 1
+
+
+def test_dry_run_renders_but_marks_nothing(capsys):
+    item = _item("FDA approves Veppanu for advanced breast cancer", tickers=("VRDN",))
+    universe = StubUniverse({"VRDN": _meta("VRDN")})
+    tracker = HaltTracker()
+
+    stats = _run_emit([item], universe, tracker, slack_mode="dry-run")
+    assert stats.hc_events_emitted == 0          # nothing delivered
+    assert tracker.hc_events_emitted == set()    # re-testable — no mark
+    assert "VRDN" in capsys.readouterr().out     # but it DID render
+
+
+def test_live_post_failure_does_not_mark():
+    item = _item("FDA approves Veppanu for advanced breast cancer", tickers=("VRDN",))
+    universe = StubUniverse({"VRDN": _meta("VRDN")})
+    tracker = HaltTracker()
+
+    with patch("src.slack.post_hc_event", side_effect=RuntimeError("slack down")):
+        stats = _run_emit([item], universe, tracker, slack_mode="live")
+    assert stats.hc_events_emitted == 0
+    assert stats.slack_posts_failed == 1
+    assert tracker.hc_events_emitted == set()   # post failed → not marked → retries next poll
+
+
+def test_live_post_success_marks():
+    item = _item("FDA approves Veppanu for advanced breast cancer", tickers=("VRDN",))
+    universe = StubUniverse({"VRDN": _meta("VRDN")})
+    tracker = HaltTracker()
+
+    with patch("src.slack.post_hc_event", return_value={"ok": True}) as m:
+        stats = _run_emit([item], universe, tracker, slack_mode="live")
+    assert m.called
+    assert stats.hc_events_emitted == 1
+    assert f"{item.news_id}|VRDN|{FDA_APPROVAL}" in tracker.hc_events_emitted
+
+
+# --------------------------------------------------------------------------
+# 3. dedup / state round-trip for hc_events_emitted
+# --------------------------------------------------------------------------
+
+def test_reset_clears_hc_events():
+    tracker = HaltTracker()
+    tracker.hc_events_emitted.add("u|VRDN|fda_approval")
+    tracker.reset()
+    assert tracker.hc_events_emitted == set()
+
+
+def test_hc_events_emitted_round_trip(tmp_path):
+    tracker = HaltTracker()
+    tracker.hc_events_emitted.add("https://prn.test/1|VRDN|trial_readout")
+    tracker.hc_events_emitted.add("https://prn.test/2|ARVN|fda_approval")
+    state.save(tracker, state_dir=tmp_path, day_key="2026-05-05")
+
+    fresh = state.load(state_dir=tmp_path, day_key="2026-05-05")
+    assert fresh.hc_events_emitted == {
+        "https://prn.test/1|VRDN|trial_readout",
+        "https://prn.test/2|ARVN|fda_approval",
+    }
+
+
+def test_old_state_without_hc_field_rehydrates_empty(tmp_path):
+    path = tmp_path / "dedup_state_2026-05-05.json"
+    path.write_text(
+        '{"schema_version": 1, "trading_day_et": "2026-05-05", '
+        '"halts": [], "resumes_emitted": []}'
+    )
+    fresh = state.load(state_dir=tmp_path, day_key="2026-05-05")
+    assert fresh.hc_events_emitted == set()

 succeeded in 414ms:
 M .github/workflows/halt-monitor-am.yml
 M .github/workflows/halt-monitor-pm.yml
 M .gitignore
 M PROJECT_BRIEF.md
 M README.md
 M codex_feedback/codex_feedback_2026-07-12_235153.md
 M codex_feedback/codex_feedback_2026-07-13_000252.md
 M codex_feedback/codex_feedback_2026-07-13_001017.md
 M data/sa_monitor_universe.json
 M logs/halt_monitor_2026-05-11_am.jsonl
 M logs/halt_monitor_2026-05-13_am.jsonl
 M logs/halt_monitor_2026-05-15_am.jsonl
 M logs/halt_monitor_2026-05-22_am.jsonl
 M logs/halt_monitor_2026-06-02_pm.jsonl
 M logs/halt_monitor_2026-06-10_am.jsonl
 M logs/halt_monitor_2026-06-11_am.jsonl
 M logs/halt_monitor_2026-06-24_am.jsonl
 M logs/halt_monitor_2026-06-25_am.jsonl
 M logs/halt_monitor_2026-06-30_am.jsonl
 M logs/halt_monitor_2026-07-01_am.jsonl
 M logs/halt_monitor_2026-07-03_am.jsonl
 M logs/halt_monitor_2026-07-06_am.jsonl
 M scripts/build_universe.py
 M scripts/ci_run.sh
 M src/dedup.py
 A src/events/__init__.py
 A src/events/classify.py
 A src/events/types.py
 M src/halt_monitor.py
 M src/slack.py
 M src/state.py
 M src/template.py
 M state/dedup_state_2026-07-03.json
 M state/dedup_state_2026-07-06.json
 M state/dedup_state_2026-07-07.json
 M state/dedup_state_2026-07-08.json
 M state/dedup_state_2026-07-09.json
 M state/dedup_state_2026-07-10.json
 M template-library.md
 M tests/fixtures/news/gnw_health_sample.xml
 A tests/test_hc_events.py
 M tests/test_slack.py
?? codex_feedback/codex_feedback_2026-07-13_004223.md
?? codex_feedback/codex_feedback_2026-07-13_005307.md
?? logs/halt_monitor_20260623_225250.jsonl
?? scripts/_codex_hc_events_review.md

codex
The workspace matches the pasted changes, with additional unrelated dirty files present. I’m narrowing now to executable behavior around regex ordering, suppression scope, and render/Slack CTA paths so any repros are grounded in current line numbers.
exec
/bin/bash -lc "nl -ba src/template.py | sed -n '55,240p'" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor
 succeeded in 0ms:
    55	    """Format price as '$X.XX' or empty if None."""
    56	    if price is None:
    57	        return ""
    58	    return f"${price:.2f}"
    59	
    60	
    61	def _ticker_display_name(event: HaltEvent, name_override: str = "") -> str:
    62	    """Prefer name from sa_monitor_universe.json (override) over feed name."""
    63	    return (name_override or event.name or event.symbol).strip()
    64	
    65	
    66	def is_biotech(subsector: str) -> bool:
    67	    """True when the name's CM subsector is Biotech (case/space-insensitive)."""
    68	    return (subsector or "").strip().casefold() == "biotech"
    69	
    70	
    71	def biotech_triage_cta(symbol: str, context: str = "halt") -> str:
    72	    """Lean ->triage hand-off nudge for a biotech name (option A, 2026-06-16).
    73	
    74	    sa-monitor can't auto-invoke the Claude-driven biotech_triage, so instead of
    75	    pretending to trigger it we surface a copy-pasteable command. See the root
    76	    biotech_catalyst_architecture_plan.md (§3) for why this is human-mediated.
    77	
    78	    `context` names what triggered the nudge — "halt" (default, preserves the
    79	    halt/resume wording verbatim) or an HC-event label ("readout"/"approval"/…)
    80	    so a non-halt HC event doesn't misleadingly read "Biotech halt".
    81	    """
    82	    return f":dna: Biotech {context} — triage this name?  ->  `triage {symbol}`"
    83	
    84	
    85	def render_halt(event: HaltEvent, *, sector: str = "", subsector: str = "",
    86	                name_override: str = "", note_context: Optional[str] = None) -> str:
    87	    """Render a halt notification — sa-monitor variant.
    88	
    89	    Phase 1 v0 template (per template-library.md §3):
    90	
    91	        SA: TICKER [Company halted, news pending]
    92	        HH:MM ET M/DD/YY [TICKER] halted at $X.XX, reason code [CODE - description]
    93	        Note TICKER is scheduled to report earnings this morning   <-- Phase 2 enrichment
    94	        Sector: {sector} / {subsector}
    95	    """
    96	    name = _ticker_display_name(event, name_override)
    97	    date_short = _format_date_short(event.halt_date)
    98	    time_hhmm = _format_time_hhmm(event.halt_time)
    99	    price_str = _format_price(event.last_price)
   100	
   101	    headline = f"SA: {event.symbol} [{name} halted, news pending]"
   102	
   103	    code_block = f"[{event.reason_code} - {event.reason_description}]"
   104	
   105	    if price_str:
   106	        body_line = (
   107	            f"{time_hhmm} ET {date_short} [{event.symbol}] halted at {price_str}, "
   108	            f"reason code {code_block}"
   109	        )
   110	    else:
   111	        body_line = (
   112	            f"{time_hhmm} ET {date_short} [{event.symbol}] halted, reason code "
   113	            f"{code_block}"
   114	        )
   115	
   116	    sector_line = ""
   117	    if sector:
   118	        sector_line = f"Sector: {sector}"
   119	        if subsector:
   120	            sector_line += f" / {subsector}"
   121	
   122	    parts = [headline, body_line]
   123	    if note_context:
   124	        parts.append(note_context)
   125	    if sector_line:
   126	        parts.append(sector_line)
   127	    if is_biotech(subsector):
   128	        parts.append(biotech_triage_cta(event.symbol))
   129	    parts.append(f"Source: {event.source} ({event.exchange})")
   130	    return "\n".join(parts)
   131	
   132	
   133	def _iso_utc_to_et(iso_ts: str) -> Optional[datetime]:
   134	    """ISO-8601 UTC timestamp -> ET datetime; None on malformed input."""
   135	    if not iso_ts:
   136	        return None
   137	    try:
   138	        dt = datetime.fromisoformat(iso_ts.replace("Z", "+00:00"))
   139	    except ValueError:
   140	        return None
   141	    if dt.tzinfo is None:
   142	        dt = dt.replace(tzinfo=timezone.utc)
   143	    return dt.astimezone(_ET)
   144	
   145	
   146	def followup_when(halt: HaltEvent, item: NewsItem) -> tuple[str, str]:
   147	    """(time_hhmm, date_short) for a §7 Follow-up header.
   148	
   149	    The follow-up crosses when the news breaks, so the header stamps the news
   150	    item's publish time in ET. Falls back to the halt's own timestamp if the
   151	    item has no parseable publish time. Shared by render_followup + Slack so
   152	    both render the identical header.
   153	    """
   154	    et = _iso_utc_to_et(item.published_at)
   155	    if et is not None:
   156	        return (f"{et.hour:02d}:{et.minute:02d}",
   157	                f"{et.month}/{et.day:02d}/{et.year % 100:02d}")
   158	    return (_format_time_hhmm(halt.halt_time), _format_date_short(halt.halt_date))
   159	
   160	
   161	def followup_headline(item: NewsItem) -> str:
   162	    """The substantive headline for a §7 Follow-up, capped for readability."""
   163	    headline = (item.title or "").strip()
   164	    if len(headline) > _FOLLOWUP_HEADLINE_LIMIT:
   165	        headline = headline[:_FOLLOWUP_HEADLINE_LIMIT].rsplit(" ", 1)[0] + "…"
   166	    return headline
   167	
   168	
   169	def followup_source_label(item: NewsItem) -> str:
   170	    """Pretty wire-source label (e.g. 'PR Newswire') for a §7 Follow-up."""
   171	    return _NEWS_SOURCE_LABELS.get(item.source, item.source)
   172	
   173	
   174	def render_followup(halt: HaltEvent, item: NewsItem, *, sector: str = "",
   175	                    subsector: str = "", last_price: Optional[float] = None) -> str:
   176	    """Render a §7 Follow-up alert — the substantive news that broke on a
   177	    previously-halted covered name (template-library.md §7, webhook variant).
   178	
   179	        HH:MM ET M/DD/YY [StreetAccount] TICKER Follow-up: {headline} ($X.XX)
   180	        Follows the HH:MM ET halt on TICKER
   181	        Sector: {sector} / {subsector}
   182	        Source: {Wire} press release
   183	
   184	    sa-monitor posts via webhook (no Slack threading), so the original halt is
   185	    referenced in-body rather than via SA's archive block. Price is the halt's
   186	    last_price unless a fresher one is supplied.
   187	    """
   188	    symbol = halt.symbol
   189	    time_hhmm, date_short = followup_when(halt, item)
   190	    headline = followup_headline(item)
   191	
   192	    price = last_price if last_price is not None else halt.last_price
   193	    price_str = _format_price(price)
   194	
   195	    header = (
   196	        f"{time_hhmm} ET {date_short} [StreetAccount] {symbol} "
   197	        f"Follow-up: {headline}"
   198	    )
   199	    if price_str:
   200	        header += f" ({price_str})"
   201	
   202	    halt_hhmm = _format_time_hhmm(halt.halt_time)
   203	    back_ref = f"Follows the {halt_hhmm} ET halt on {symbol}"
   204	
   205	    parts = [header, back_ref]
   206	    if sector:
   207	        sector_line = f"Sector: {sector}"
   208	        if subsector:
   209	            sector_line += f" / {subsector}"
   210	        parts.append(sector_line)
   211	    parts.append(f"Source: {followup_source_label(item)} press release")
   212	    return "\n".join(parts)
   213	
   214	
   215	def hc_event_when(published_at: str) -> tuple[str, str]:
   216	    """(time_hhmm, date_short) in ET for an HC event, from its PR publish time.
   217	
   218	    Falls back to ("", "") if the timestamp is missing/malformed — the caller
   219	    then simply omits the stamp rather than printing garbage.
   220	    """
   221	    et = _iso_utc_to_et(published_at)
   222	    if et is None:
   223	        return ("", "")
   224	    return (f"{et.hour:02d}:{et.minute:02d}",
   225	            f"{et.month}/{et.day:02d}/{et.year % 100:02d}")
   226	
   227	
   228	def hc_event_headline(headline: str) -> str:
   229	    """The event headline, verbatim from the issuer, capped for readability.
   230	
   231	    SA reproduces issuer language; v1 delivers the issuer's own title (the
   232	    fact-dense §15/§19 efficacy body is a deferred LLM enhancement).
   233	    """
   234	    headline = (headline or "").strip()
   235	    if len(headline) > _FOLLOWUP_HEADLINE_LIMIT:
   236	        headline = headline[:_FOLLOWUP_HEADLINE_LIMIT].rsplit(" ", 1)[0] + "…"
   237	    return headline
   238	
   239	
   240	# Human label for an HC event type, used in the biotech triage CTA so it reads

exec
/bin/bash -lc "nl -ba src/events/classify.py | sed -n '1,280p'" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor
 succeeded in 0ms:
     1	"""Precision-biased keyword classifier: NewsItem -> Optional[HCEvent].
     2	
     3	Design of record: research/sa_monitor_hc_event_wire_design_2026-07-13.md §3.
     4	Rules are anchored to the LOCKED real headlines in template-library.md
     5	§15 (Phase-N primary MISSED), §16 (FDA approval), §19 (Phase-N primary MET).
     6	
     7	Philosophy — "miss over misfire" (design §7): a wrong "Phase 3 met" or "FDA
     8	approval" alert costs more trust than a missed one. Every rule requires the
     9	headline to CLEARLY match a completed action. Ambiguous, future-tense,
    10	procedural, or presentation headlines return None.
    11	
    12	classify() only proposes an event *type/direction/phase*; it never decides
    13	whether to fire. The universe filter + attribution + dedup live in
    14	halt_monitor._emit_hc_events. classify() returns an HCEvent with symbol="".
    15	"""
    16	from __future__ import annotations
    17	
    18	import re
    19	from typing import Optional
    20	
    21	from ..news.types import NewsItem
    22	from .types import (
    23	    CLEARANCE,
    24	    CRL,
    25	    DIR_MET,
    26	    DIR_MISSED,
    27	    DIR_NA,
    28	    FDA_APPROVAL,
    29	    TRIAL_READOUT,
    30	    HCEvent,
    31	)
    32	
    33	# --------------------------------------------------------------------------
    34	# Trial readout
    35	# --------------------------------------------------------------------------
    36	
    37	# "Phase 1/2/3" (also "Phase 2B", "Phase 1/2") — capture the leading digit.
    38	_PHASE = re.compile(r"\bphase\s*([123])", re.I)
    39	
    40	# Result attached to the PRIMARY (or co-/dual primary) endpoint. Tying the
    41	# verb to "primary" (rather than matching "met"/"missed" anywhere) keeps the
    42	# reported DIRECTION faithful, which is the harmful failure mode to avoid.
    43	_MET_PRIMARY = re.compile(
    44	    r"\b(met|meets|achieved|achieves|hit|reached)\b"
    45	    r"(?:\s+(?:its|the|all|both|all\s+of\s+its))?"
    46	    r"(?:\s+(?:co-|dual|two|three|multiple))?"
    47	    r"\s+(?:co-)?primary\b",
    48	    re.I,
    49	)
    50	# Anchored to primary/endpoint so "did not meet enrollment target / guidance"
    51	# (with a stray Phase in the title) can't read as a failed readout.
    52	_MISSED_PRIMARY = re.compile(
    53	    r"\b(did\s+not\s+meet|does\s+not\s+meet|failed\s+to\s+meet|fails\s+to\s+meet"
    54	    r"|missed|misses|did\s+not\s+achieve|does\s+not\s+achieve)\b"
    55	    r"(?:\s+(?:its|the|both|all|all\s+of\s+its))?"
    56	    r"(?:\s+(?:co-|dual))?"
    57	    r"\s+(?:primary|co-primary|endpoint)",
    58	    re.I,
    59	)
    60	# A topline-data announcement with no explicit met/missed in the headline is
    61	# still a genuine readout crossing the wire — fired at direction n/a.
    62	_TOPLINE = re.compile(r"\btop-?line\b", re.I)
    63	
    64	# Future / procedural / presentation headlines that mention a Phase but are NOT
    65	# a readout. If ANY of these match, the trial-readout path returns None. This
    66	# is the primary defense for the negative test set.
    67	_TRIAL_NEGATIVE = re.compile(
    68	    r"\bpresent"          # present / presents / presented / presentation / presenting
    69	    r"|\bto\s+report\b|\bwill\s+report\b"
    70	    r"|\bto\s+announce\b|\bto\s+host\b|\bto\s+provide\b|\bto\s+release\b"
    71	    r"|\bto\s+webcast\b|\bwebcast\b|\bconference\s+call\b|\binvestor\s+day\b"
    72	    r"|\bposter\b|\bat\s+the\s+\w+.*(?:conference|congress|meeting|symposium)"
    73	    r"|\binitiat"          # initiate / initiation / initiates
    74	    r"|\bcommenc"          # commence / commences / commencement
    75	    r"|\bfirst[\s-]patient\b|\bdosing\s+of\b|\bdoses\s+(?:the\s+)?first\b"
    76	    r"|\bfirst\s+patient\s+dosed\b"
    77	    r"|\benroll"           # enroll / enrolls / enrolled / enrollment
    78	    r"|\btrial\s+design\b|\bstudy\s+design\b"
    79	    r"|\bto\s+begin\b|\bplans?\s+to\b|\bexpects?\s+to\b"
    80	    r"|\bon\s+track\b|\bdesign(?:s|ed)?\s+to\s+evaluate\b"
    81	    r"|\bcompletes?\s+enrollment\b"
    82	    # F4: future-timeline readouts ("topline data expected in Q4") are NOT a
    83	    # readout that happened. Requires a timeline token after expected/anticipated
    84	    # so "met primary, better than expected" still fires.
    85	    r"|\bexpected\s+(?:in|by|during|for|later|this|next|early|mid|late|soon|shortly|imminent|upcoming|coming|q[1-4]|h[12]|first|second|third|fourth|20\d\d)"
    86	    r"|\banticipat\w*\s+(?:in|by|during|for|later|this|next|early|mid|late|soon|shortly|imminent|upcoming|coming|q[1-4]|h[12]|20\d\d)"
    87	    r"|\bto\s+read\s+out\b|\breadout\s+(?:expected|anticipated|on\s+track|in|by|soon|shortly)\b",
    88	    re.I,
    89	)
    90	
    91	
    92	def _classify_trial(title: str, item: NewsItem) -> Optional[HCEvent]:
    93	    m_phase = _PHASE.search(title)
    94	    if not m_phase:
    95	        return None
    96	    if _TRIAL_NEGATIVE.search(title):
    97	        return None  # future / procedural / presentation → not a readout
    98	
    99	    missed = _MISSED_PRIMARY.search(title)
   100	    met = _MET_PRIMARY.search(title)
   101	    topline = _TOPLINE.search(title)
   102	
   103	    if missed:
   104	        direction = DIR_MISSED
   105	    elif met:
   106	        direction = DIR_MET
   107	    elif topline:
   108	        direction = DIR_NA
   109	    else:
   110	        return None  # Phase present but no result grammar → not a readout
   111	
   112	    return _build(item, TRIAL_READOUT, direction, phase=m_phase.group(1))
   113	
   114	
   115	# --------------------------------------------------------------------------
   116	# FDA regulatory actions
   117	# --------------------------------------------------------------------------
   118	
   119	# Action-verb-anchored approval grammar. Deliberately does NOT match bare
   120	# "FDA approval" so pending/procedural headlines ("PDUFA date for FDA approval
   121	# decision", "seeking FDA approval") don't fire.
   122	_APPROVAL = re.compile(
   123	    r"\bfda\s+approves\b"
   124	    r"|\bfda\s+approved\b"
   125	    r"|\bfda\s+approval\s+of\b"
   126	    r"|\breceive[sd]?\s+fda\s+approval\b"
   127	    r"|\bgranted\s+fda\s+approval\b"
   128	    r"|\bwins?\s+fda\s+approval\b"
   129	    r"|\bsecure[sd]?\s+fda\s+approval\b"
   130	    r"|\bgains?\s+fda\s+approval\b"
   131	    r"|\bfda\s+grants?\s+(?:full\s+|accelerated\s+|traditional\s+|marketing\s+)?approval\b"
   132	    r"|\bfda\s+has\s+granted\s+approval\b"
   133	    r"|\bapproved\s+by\s+the\s+fda\b",
   134	    re.I,
   135	)
   136	# Pending / procedural regulatory milestones that are NOT a grant. Any hit
   137	# suppresses BOTH approval and clearance (belt-and-suspenders atop the
   138	# verb-anchored regexes). Shared by the approval + clearance paths.
   139	_APPROVAL_NEGATIVE = re.compile(
   140	    r"\baccept(?:s|ed)?\b|\bpriority\s+review\b|\bfast\s+track\b"
   141	    r"|\bbreakthrough\s+therapy\b|\borphan\s+drug\b|\brare\s+pediatric\b"
   142	    r"|\badvisory\s+committee\b|\badcomm\b|\bpdufa\b"
   143	    r"|\bresubmi|\bseeking\b|\bawait|\bapplication\s+for\b"
   144	    r"|\bfiles?\s+for\b|\bsubmit|\bsubmission\b|\bto\s+request\b"
   145	    r"|\bcomplete\s+response\s+letter\b|\bpre-?market\s+notification\b",
   146	    re.I,
   147	)
   148	
   149	# Denial / rejection grammar (F2). A rejection must NEVER read as a grant. v1
   150	# has no "rejection" event type, so a plain "did not receive approval" headline
   151	# → None (missing it is far safer than inverting it to an approval). Gates BOTH
   152	# approval and clearance. NOT applied to trial readouts (which have their own
   153	# missed-endpoint direction) or to CRL (the rejection event we DO fire).
   154	_REG_DENIAL = re.compile(
   155	    r"\bdid\s+not\b|\bdoes\s+not\b|\bdid\s*n[’']?t\b|\bdoes\s*n[’']?t\b"
   156	    r"|\bfailed\s+to\b|\bfails\s+to\b"
   157	    r"|\bnot\s+(?:receive|receiv\w+|win|won|gain\w*|secure\w*|grant\w*|approv\w+|clear\w+)\b"
   158	    r"|\bdenie[sd]\b|\bdenial\b|\bdeclin\w*\s+to\s+approve\b"
   159	    r"|\breject(?:s|ed|ion)?\b|\bnot\s+approved\b|\bunapproved\b"
   160	    r"|\bwithdraw\w*\b|\brescind\w*\b|\brevoke[sd]?\b",
   161	    re.I,
   162	)
   163	
   164	# Commentary / competitor framing (F3): an issuer reacting to SOMEONE ELSE's
   165	# FDA action. _emit_hc_events would mis-attribute it to the commenting issuer,
   166	# so suppress. Accept the rare miss of a self-approval PR that also contains one
   167	# of these words — precision-first.
   168	_REG_COMMENTARY = re.compile(
   169	    r"\bcomment(?:s|ary|ing|ed)?\s+on\b|\bcommentary\b"
   170	    r"|\bcongratulat\w+|\bapplaud\w*|\bwelcome[sd]?\b|\breact[s]?\s+to\b"
   171	    r"|\brespond[s]?\s+to\b|\bissues?\s+statement\b"
   172	    r"|\bstatement\s+(?:on|regarding|re)\b",
   173	    re.I,
   174	)
   175	
   176	# CRL (F5). Must NOT match bare \bcrl\b — CRL is also the ticker for Charles
   177	# River Laboratories, a COVERED name ("CRL Announces … Financial Results").
   178	# Require the full phrase or a regulatory-CRL idiom.
   179	_CRL = re.compile(
   180	    r"\bcomplete\s+response\s+letter\b"
   181	    r"|\breceiv\w*\s+(?:a\s+)?crl\b"
   182	    r"|\bcrl\s+from\s+(?:the\s+)?fda\b"
   183	    r"|\bissue[sd]?\s+(?:a\s+)?crl\b"
   184	    r"|\bfda\s+issue[sd]?\s+(?:a\s+)?crl\b",
   185	    re.I,
   186	)
   187	
   188	# Medtech clearance. Requires a completed clearance action — bare adjective
   189	# "FDA-cleared" (e.g. "operates an FDA-cleared facility") must NOT match.
   190	_CLEARANCE = re.compile(
   191	    r"\bfda\s+clears\b"
   192	    r"|\bfda\s+clearance\b"
   193	    r"|\breceive[sd]?\s+fda\s+clearance\b"
   194	    r"|\b510\(k\)\s+clearance\b"
   195	    r"|\bgranted\s+510\(k\)\b"
   196	    r"|\bde\s+novo\s+(?:clearance|authorization|grant)\b"
   197	    r"|\bpma\s+approval\b"
   198	    r"|\bfda\s+grants?\s+(?:de\s+novo|510)\b",
   199	    re.I,
   200	)
   201	
   202	
   203	def _reg_suppressed(title: str) -> bool:
   204	    """A grant (approval or clearance) is suppressed when the headline is
   205	    actually a pending milestone, a denial/rejection, or third-party commentary."""
   206	    return bool(
   207	        _APPROVAL_NEGATIVE.search(title)
   208	        or _REG_DENIAL.search(title)
   209	        or _REG_COMMENTARY.search(title)
   210	    )
   211	
   212	
   213	def _classify_regulatory(title: str, item: NewsItem) -> Optional[HCEvent]:
   214	    # CRL first — a Complete Response Letter is unambiguous and distinct from
   215	    # an approval; it must never be mis-read as one.
   216	    if _CRL.search(title):
   217	        return _build(item, CRL, DIR_NA)
   218	    # Approval + clearance share the same suppression gate: pending milestone,
   219	    # denial/rejection (F2), or third-party commentary (F3). Clearance was
   220	    # previously ungated (F1).
   221	    if _reg_suppressed(title):
   222	        return None
   223	    if _APPROVAL.search(title):
   224	        return _build(item, FDA_APPROVAL, DIR_NA)
   225	    if _CLEARANCE.search(title):
   226	        return _build(item, CLEARANCE, DIR_NA)
   227	    return None
   228	
   229	
   230	# --------------------------------------------------------------------------
   231	# Public entry point
   232	# --------------------------------------------------------------------------
   233	
   234	
   235	def _build(item: NewsItem, event_type: str, direction: str,
   236	           *, phase: str = "") -> HCEvent:
   237	    return HCEvent(
   238	        news_id=item.news_id,
   239	        symbol="",  # attribution decided by the emit path (tickers ∩ universe)
   240	        event_type=event_type,
   241	        direction=direction,
   242	        headline=item.title,
   243	        source=item.source,
   244	        url=item.url,
   245	        published_at=item.published_at,
   246	        phase=phase,
   247	        confidence="high",
   248	        raw_industries=tuple(item.industries),
   249	    )
   250	
   251	
   252	def classify(item: NewsItem) -> Optional[HCEvent]:
   253	    """Classify a NewsItem into an HCEvent, or None if it isn't one.
   254	
   255	    Precision-biased: returns None on any ambiguity. Regulatory actions
   256	    (approval / CRL / clearance) are checked before trial readouts — an
   257	    approval PR carries no Phase grammar, so the two families don't collide.
   258	    """
   259	    title = item.title or ""
   260	    if not title.strip():
   261	        return None
   262	
   263	    reg = _classify_regulatory(title, item)
   264	    if reg is not None:
   265	        return reg
   266	
   267	    return _classify_trial(title, item)

exec
/bin/bash -lc "nl -ba src/slack.py | sed -n '1,370p'" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor
 succeeded in 0ms:
     1	"""Slack delivery for sa-monitor halt + resume notifications.
     2	
     3	Posts to the consolidated #street-account channel for ALL sa-monitor alerts
     4	across Phases 1-5. Block Kit format per HEALTH_REPORTING.md §4.1.
     5	
     6	Webhook source-of-truth:
     7	- SLACK_WEBHOOK_STREET_ACCOUNT env var, OR
     8	- Claude Folder/.secrets/slack_webhook_street_account.txt (one line)
     9	"""
    10	from __future__ import annotations
    11	
    12	import json
    13	import logging
    14	import os
    15	import time
    16	from pathlib import Path
    17	from typing import Optional
    18	
    19	import requests
    20	
    21	from .coverage import TickerMeta
    22	from .events.types import (
    23	    CLEARANCE,
    24	    CRL,
    25	    DIR_MET,
    26	    DIR_MISSED,
    27	    FDA_APPROVAL,
    28	    TRIAL_READOUT,
    29	    HCEvent,
    30	)
    31	from .feeds.types import HaltEvent
    32	from .news.types import NewsItem
    33	from .template import (
    34	    _NEWS_SOURCE_LABELS,
    35	    _format_date_short,
    36	    _format_price,
    37	    _format_time_hhmm,
    38	    biotech_triage_cta,
    39	    followup_headline,
    40	    followup_source_label,
    41	    followup_when,
    42	    hc_event_cta_context,
    43	    hc_event_headline,
    44	    hc_event_when,
    45	    is_biotech,
    46	)
    47	
    48	log = logging.getLogger(__name__)
    49	
    50	REPO_ROOT = Path(__file__).resolve().parents[1]
    51	CLAUDE_FOLDER = REPO_ROOT.parent
    52	WEBHOOK_FILE = CLAUDE_FOLDER / ".secrets" / "slack_webhook_street_account.txt"
    53	ENV_VAR = "SLACK_WEBHOOK_STREET_ACCOUNT"
    54	
    55	POST_TIMEOUT_SEC = 10
    56	SLACK_SECTION_TEXT_LIMIT = 3000
    57	
    58	# Backoff for the wake-race: a scheduled task that catches up on wake
    59	# (StartWhenAvailable) can fire before DNS/WiFi is up, so the first POST dies
    60	# with a transient transport error. Retry rides through it. See CONVENTIONS s3.
    61	_RETRY_BACKOFF = (5, 15, 30)  # seconds to wait BEFORE retry attempts 2..N
    62	
    63	
    64	def resolve_webhook_url(env_var: str = ENV_VAR,
    65	                        file_path: Path = WEBHOOK_FILE) -> str:
    66	    env = os.environ.get(env_var)
    67	    if env:
    68	        return env.strip()
    69	    if file_path.exists():
    70	        return file_path.read_text(encoding="utf-8").strip()
    71	    raise SystemExit(
    72	        f"No Slack webhook configured. Set the {env_var} env var or write the "
    73	        f"webhook URL (one line) to {file_path}."
    74	    )
    75	
    76	
    77	def build_halt_blocks(event: HaltEvent, meta: Optional[TickerMeta],
    78	                      *, note_context: Optional[str] = None) -> dict:
    79	    name = (meta.name if meta else "") or event.name or event.symbol
    80	    date_short = _format_date_short(event.halt_date)
    81	    time_hhmm = _format_time_hhmm(event.halt_time)
    82	
    83	    headline = f":no_entry: *SA:* `{event.symbol}` — {name} halted, news pending"
    84	    if event.last_price is not None:
    85	        headline += f"  ·  {_format_price(event.last_price)}"
    86	
    87	    reason_line = (
    88	        f"`{time_hhmm} ET {date_short}`  ·  reason `{event.reason_code} - "
    89	        f"{event.reason_description}`"
    90	    )
    91	    if meta and meta.sector:
    92	        sector_str = meta.sector
    93	        if meta.subsector:
    94	            sector_str += f" / {meta.subsector}"
    95	        reason_line += f"  ·  Sector: {sector_str}"
    96	
    97	    source_line = f"_source: {event.source} ({event.exchange})_"
    98	    lines = [headline, reason_line]
    99	    if note_context:
   100	        lines.append(f"_{note_context}_")
   101	    # Lean halt->triage hand-off (option A): biotech halts carry a copy-pasteable
   102	    # triage nudge. See root biotech_catalyst_architecture_plan.md §3.
   103	    if meta and is_biotech(meta.subsector):
   104	        lines.append(biotech_triage_cta(event.symbol))
   105	    lines.append(source_line)
   106	    text_block = "\n".join(lines)
   107	    fallback = (
   108	        f"SA: {event.symbol} {name} halted, news pending "
   109	        f"({event.reason_code} - {event.reason_description})"
   110	    )
   111	    return {
   112	        "blocks": [
   113	            {
   114	                "type": "section",
   115	                "text": {"type": "mrkdwn", "text": text_block[:SLACK_SECTION_TEXT_LIMIT]},
   116	            }
   117	        ],
   118	        "text": fallback,
   119	    }
   120	
   121	
   122	def build_resume_blocks(event: HaltEvent, meta: Optional[TickerMeta]) -> dict:
   123	    name = (meta.name if meta else "") or event.name or event.symbol
   124	    date_short = _format_date_short(event.halt_date)
   125	    halt_hhmm = _format_time_hhmm(event.halt_time)
   126	    resume_hhmm = _format_time_hhmm(event.resume_trade_time or "")
   127	
   128	    headline = (
   129	        f":white_check_mark: *SA:* `{event.symbol}` — shares to resume trading "
   130	        f"at {resume_hhmm} ET"
   131	    )
   132	    detail = (
   133	        f"`{date_short}`  ·  resume `{resume_hhmm} ET`  ·  originally halted "
   134	        f"`{halt_hhmm} ET`  ·  reason `{event.reason_code} - "
   135	        f"{event.reason_description}`"
   136	    )
   137	    if meta and meta.sector:
   138	        sector_str = meta.sector
   139	        if meta.subsector:
   140	            sector_str += f" / {meta.subsector}"
   141	        detail += f"  ·  Sector: {sector_str}"
   142	
   143	    source_line = f"_source: {event.source} ({event.exchange})_"
   144	    text_block = "\n".join([headline, detail, source_line])
   145	    fallback = (
   146	        f"SA: {event.symbol} resume at {resume_hhmm} ET (originally halted "
   147	        f"{halt_hhmm} ET, {event.reason_code})"
   148	    )
   149	    return {
   150	        "blocks": [
   151	            {
   152	                "type": "section",
   153	                "text": {"type": "mrkdwn", "text": text_block[:SLACK_SECTION_TEXT_LIMIT]},
   154	            }
   155	        ],
   156	        "text": fallback,
   157	    }
   158	
   159	
   160	def build_followup_blocks(halt: HaltEvent, item: NewsItem,
   161	                          meta: Optional[TickerMeta], *,
   162	                          last_price: Optional[float] = None) -> dict:
   163	    """Block Kit for a §7 Follow-up alert — the substantive news that broke on a
   164	    previously-halted covered name. Mirrors build_halt_blocks; references the
   165	    original halt in-body (no Slack threading over a webhook)."""
   166	    symbol = halt.symbol
   167	    name = (meta.name if meta else "") or halt.name or symbol
   168	    time_hhmm, date_short = followup_when(halt, item)
   169	    headline = followup_headline(item)
   170	
   171	    header = f":newspaper: *SA:* `{symbol}` — Follow-up: {headline}"
   172	    price = last_price if last_price is not None else halt.last_price
   173	    if price is not None:
   174	        header += f"  ·  {_format_price(price)}"
   175	
   176	    halt_hhmm = _format_time_hhmm(halt.halt_time)
   177	    detail = f"`{time_hhmm} ET {date_short}`  ·  Follows the {halt_hhmm} ET halt on `{symbol}`"
   178	    if meta and meta.sector:
   179	        sector_str = meta.sector
   180	        if meta.subsector:
   181	            sector_str += f" / {meta.subsector}"
   182	        detail += f"  ·  Sector: {sector_str}"
   183	
   184	    source_line = f"_source: {followup_source_label(item)} press release_"
   185	    text_block = "\n".join([header, detail, source_line])
   186	    fallback = f"SA: {symbol} Follow-up: {headline}"
   187	    return {
   188	        "blocks": [
   189	            {
   190	                "type": "section",
   191	                "text": {"type": "mrkdwn", "text": text_block[:SLACK_SECTION_TEXT_LIMIT]},
   192	            }
   193	        ],
   194	        "text": fallback,
   195	    }
   196	
   197	
   198	def _hc_event_emoji(event: HCEvent) -> str:
   199	    """Event-type emoji per design §5. Trial readouts split on direction so a
   200	    'met' and a 'missed' are visually distinct at a glance."""
   201	    if event.event_type == FDA_APPROVAL:
   202	        return ":pill:"
   203	    if event.event_type == CRL:
   204	        return ":x:"
   205	    if event.event_type == CLEARANCE:
   206	        return ":heavy_check_mark:"
   207	    if event.event_type == TRIAL_READOUT:
   208	        if event.direction == DIR_MISSED:
   209	            return ":small_red_triangle_down:"
   210	        if event.direction == DIR_MET:
   211	            return ":test_tube:"
   212	        return ":microscope:"  # topline, direction TBD
   213	    return ":pill:"
   214	
   215	
   216	def build_hc_event_blocks(event: HCEvent, meta: Optional[TickerMeta]) -> dict:
   217	    """Block Kit for an HC event-wire alert — an FDA action or clinical readout
   218	    on a covered name (design §5, templates §15/§16/§19). Single-section mrkdwn,
   219	    mirroring build_followup_blocks. Headline is the issuer's verbatim title;
   220	    the fact-dense efficacy body is a deferred LLM enhancement."""
   221	    symbol = event.symbol
   222	    headline = hc_event_headline(event.headline)
   223	    time_hhmm, date_short = hc_event_when(event.published_at)
   224	
   225	    header = f"{_hc_event_emoji(event)} *SA:* `{symbol}` — {headline}"
   226	
   227	    when = f"`{time_hhmm} ET {date_short}`" if time_hhmm else ""
   228	    detail_bits = [when] if when else []
   229	    if meta and meta.sector:
   230	        sector_str = meta.sector
   231	        if meta.subsector:
   232	            sector_str += f" / {meta.subsector}"
   233	        detail_bits.append(f"Sector: {sector_str}")
   234	    detail = "  ·  ".join(detail_bits)
   235	
   236	    label = _NEWS_SOURCE_LABELS.get(event.source, event.source)
   237	    source_line = f"_source: {label} press release"
   238	    if event.url:
   239	        source_line += f" · {event.url}"
   240	    source_line += "_"
   241	
   242	    lines = [header]
   243	    if detail:
   244	        lines.append(detail)
   245	    if meta and is_biotech(meta.subsector):
   246	        lines.append(biotech_triage_cta(symbol, context=hc_event_cta_context(event)))
   247	    lines.append(source_line)
   248	    text_block = "\n".join(lines)
   249	    fallback = f"SA: {symbol} — {headline}"
   250	    return {
   251	        "blocks": [
   252	            {
   253	                "type": "section",
   254	                "text": {"type": "mrkdwn", "text": text_block[:SLACK_SECTION_TEXT_LIMIT]},
   255	            }
   256	        ],
   257	        "text": fallback,
   258	    }
   259	
   260	
   261	def build_dm_blocks(message: str, *, level: str = "warning") -> dict:
   262	    emoji = {"ok": ":white_check_mark:", "warning": ":warning:", "error": ":x:"}.get(
   263	        level, ":warning:"
   264	    )
   265	    text = f"{emoji} *sa-monitor*: {message}"
   266	    return {
   267	        "blocks": [{"type": "section", "text": {"type": "mrkdwn", "text": text}}],
   268	        "text": f"sa-monitor: {message}",
   269	    }
   270	
   271	
   272	def post_payload(payload: dict, *, webhook_url: Optional[str] = None,
   273	                 timeout: int = POST_TIMEOUT_SEC) -> requests.Response:
   274	    url = webhook_url or resolve_webhook_url()
   275	    headers = {"Content-Type": "application/json"}
   276	    log.debug("slack: POST (url redacted)")
   277	    body = json.dumps(payload)
   278	    # Retry only transient transport errors (DNS-not-ready on wake, etc.); a
   279	    # successful-but-bad-status response is left to raise_for_status as before.
   280	    attempts = len(_RETRY_BACKOFF) + 1
   281	    last_exc = None
   282	    for i in range(attempts):
   283	        try:
   284	            resp = requests.post(url, headers=headers, data=body, timeout=timeout)
   285	            break
   286	        except requests.exceptions.RequestException as exc:
   287	            last_exc = exc
   288	            if i == attempts - 1:
   289	                raise
   290	            delay = _RETRY_BACKOFF[i]
   291	            log.warning("slack: POST attempt %d/%d failed (%s); retrying in %ds",
   292	                        i + 1, attempts, exc, delay)
   293	            if not os.environ.get("PYTEST_CURRENT_TEST"):
   294	                time.sleep(delay)
   295	    resp.raise_for_status()
   296	    return resp
   297	
   298	
   299	def post_halt(event: HaltEvent, meta: Optional[TickerMeta], *,
   300	              webhook_url: Optional[str] = None,
   301	              dry_run: bool = False,
   302	              note_context: Optional[str] = None) -> Optional[dict]:
   303	    payload = build_halt_blocks(event, meta, note_context=note_context)
   304	    if dry_run:
   305	        log.info("slack[dry-run] halt payload for %s:\n%s", event.symbol,
   306	                 json.dumps(payload, indent=2))
   307	        return payload
   308	    post_payload(payload, webhook_url=webhook_url)
   309	    return payload
   310	
   311	
   312	def post_resume(event: HaltEvent, meta: Optional[TickerMeta], *,
   313	                webhook_url: Optional[str] = None,
   314	                dry_run: bool = False) -> Optional[dict]:
   315	    payload = build_resume_blocks(event, meta)
   316	    if dry_run:
   317	        log.info("slack[dry-run] resume payload for %s:\n%s", event.symbol,
   318	                 json.dumps(payload, indent=2))
   319	        return payload
   320	    post_payload(payload, webhook_url=webhook_url)
   321	    return payload
   322	
   323	
   324	def post_followup(halt: HaltEvent, item: NewsItem, meta: Optional[TickerMeta], *,
   325	                  webhook_url: Optional[str] = None,
   326	                  dry_run: bool = False,
   327	                  last_price: Optional[float] = None) -> Optional[dict]:
   328	    payload = build_followup_blocks(halt, item, meta, last_price=last_price)
   329	    if dry_run:
   330	        log.info("slack[dry-run] followup payload for %s:\n%s", halt.symbol,
   331	                 json.dumps(payload, indent=2))
   332	        return payload
   333	    post_payload(payload, webhook_url=webhook_url)
   334	    return payload
   335	
   336	
   337	def post_hc_event(event: HCEvent, meta: Optional[TickerMeta], *,
   338	                  webhook_url: Optional[str] = None,
   339	                  dry_run: bool = False) -> Optional[dict]:
   340	    payload = build_hc_event_blocks(event, meta)
   341	    if dry_run:
   342	        log.info("slack[dry-run] hc_event payload for %s:\n%s", event.symbol,
   343	                 json.dumps(payload, indent=2))
   344	        return payload
   345	    post_payload(payload, webhook_url=webhook_url)
   346	    return payload
   347	
   348	
   349	def post_dm(message: str, *, level: str = "warning",
   350	            webhook_url: Optional[str] = None,
   351	            dry_run: bool = False) -> Optional[dict]:
   352	    payload = build_dm_blocks(message, level=level)
   353	    if dry_run:
   354	        log.info("slack[dry-run] dm payload:\n%s", json.dumps(payload, indent=2))
   355	        return payload
   356	    post_payload(payload, webhook_url=webhook_url)
   357	    return payload

exec
/bin/bash -lc "nl -ba src/halt_monitor.py | sed -n '330,640p'" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor
 succeeded in 0ms:
   330	                log.error("slack followup post failed for %s: %s", halt.symbol, exc)
   331	
   332	        # slack_mode == "off": stdout emit is the delivery — mark so we don't
   333	        # reprint every poll. live: mark only on a successful post.
   334	        if posted:
   335	            tracker.followed_up.add(hid)
   336	            stats.followups_emitted += 1
   337	            log.info("followup emitted for %s: %s", halt.symbol, item.title)
   338	            if log_path:
   339	                _append_log(log_path, "followup", halt, meta, emitted=True,
   340	                            note_context=f"Follow-up: {item.title}")
   341	
   342	
   343	def _emit_hc_events(
   344	    news_items: list[NewsItem],
   345	    universe: Universe,
   346	    tracker: HaltTracker,
   347	    log_path: Optional[Path],
   348	    *,
   349	    stats: RunStats,
   350	    slack_mode: str = "off",
   351	    slack_webhook_url: Optional[str] = None,
   352	) -> None:
   353	    """Detect + emit HC event-wire alerts (FDA actions + clinical readouts).
   354	
   355	    v1 CONSUMES the PR-wire NewsItems already fetched this poll: classify each,
   356	    and — for items whose issuer-embedded tickers are in the covered universe —
   357	    post an SA-faithful alert (design of record §4).
   358	
   359	    Attribution is HIGH-confidence by construction: the ticker was written into
   360	    the issuer's own release and extracted by the news parsers, so there is no
   361	    name-matching / misattribution risk (the openFDA sponsor→ticker resolver is
   362	    deferred to session 2).
   363	
   364	    Dedup key: f"{news_id}|{symbol}|{event_type}" in tracker.hc_events_emitted —
   365	    one alert per (press release, covered ticker, event type), ever. Post-then-
   366	    mark, identical gating to _emit_followups:
   367	      off      → stdout emit is the delivery → mark
   368	      live     → mark only on a successful post
   369	      dry-run  → render only, never post, never mark (re-testable)
   370	    """
   371	    for item in news_items:
   372	        ev = classify(item)
   373	        if ev is None:
   374	            continue
   375	        # v1 universe filter = issuer-extracted tickers ∩ covered Universe.
   376	        # (session 2 adds the sponsor→ticker resolver fallback here.)
   377	        covered = [t for t in item.tickers if universe.get(t)]
   378	        if not covered:
   379	            continue
   380	        for symbol in covered:
   381	            dedup_key = f"{item.news_id}|{symbol}|{ev.event_type}"
   382	            if dedup_key in tracker.hc_events_emitted:
   383	                continue  # one alert per (PR, ticker, type), ever
   384	            meta = universe.get(symbol)
   385	            event = replace(ev, symbol=symbol)
   386	
   387	            rendered = render_hc_event(
   388	                event, sector=(meta.sector if meta else ""),
   389	                subsector=(meta.subsector if meta else ""),
   390	            )
   391	            print()
   392	            print("=" * 60)
   393	            print(rendered)
   394	            print("=" * 60)
   395	            sys.stdout.flush()
   396	
   397	            # dry-run: render only — never post, never mark (re-testable).
   398	            if slack_mode == "dry-run":
   399	                try:
   400	                    slack.post_hc_event(event, meta,
   401	                                        webhook_url=slack_webhook_url, dry_run=True)
   402	                except Exception as exc:
   403	                    log.error("hc-event dry-run render failed for %s: %s",
   404	                              symbol, exc)
   405	                continue
   406	
   407	            posted = True
   408	            if slack_mode == "live":
   409	                try:
   410	                    slack.post_hc_event(event, meta,
   411	                                        webhook_url=slack_webhook_url, dry_run=False)
   412	                    stats.slack_posts_succeeded += 1
   413	                except Exception as exc:
   414	                    posted = False
   415	                    stats.slack_posts_failed += 1
   416	                    log.error("slack hc-event post failed for %s: %s", symbol, exc)
   417	
   418	            # off: stdout emit is the delivery → mark. live: mark only on post ok.
   419	            if posted:
   420	                tracker.hc_events_emitted.add(dedup_key)
   421	                stats.hc_events_emitted += 1
   422	                log.info("hc-event emitted for %s [%s/%s]: %s",
   423	                         symbol, event.event_type, event.direction, event.headline)
   424	                if log_path:
   425	                    _append_hc_log(log_path, event, meta)
   426	
   427	
   428	def _append_hc_log(log_path: Path, event, meta) -> None:
   429	    """Structured JSONL audit record for a delivered HC event (mirrors
   430	    _append_log; HCEvent has a different shape than HaltEvent so it gets its
   431	    own writer rather than being coerced)."""
   432	    log_path.parent.mkdir(parents=True, exist_ok=True)
   433	    record = {
   434	        "ts_utc": datetime.now(timezone.utc).isoformat(timespec="seconds"),
   435	        "kind": "hc_event",
   436	        "emitted": True,
   437	        "symbol": event.symbol,
   438	        "event_type": event.event_type,
   439	        "direction": event.direction,
   440	        "phase": event.phase,
   441	        "headline": event.headline,
   442	        "source": event.source,
   443	        "url": event.url,
   444	        "published_at": event.published_at,
   445	        "confidence": event.confidence,
   446	        "sector": meta.sector if meta else None,
   447	        "subsector": meta.subsector if meta else None,
   448	    }
   449	    with log_path.open("a", encoding="utf-8") as f:
   450	        f.write(json.dumps(record, ensure_ascii=False) + "\n")
   451	
   452	
   453	def _append_log(log_path: Path, kind: str, event: HaltEvent, meta,
   454	                *, emitted: bool, note_context: Optional[str] = None) -> None:
   455	    log_path.parent.mkdir(parents=True, exist_ok=True)
   456	    record = {
   457	        "ts_utc": datetime.now(timezone.utc).isoformat(timespec="seconds"),
   458	        "kind": kind,
   459	        "emitted": emitted,
   460	        "symbol": event.symbol,
   461	        "exchange": event.exchange,
   462	        "halt_date": event.halt_date,
   463	        "halt_time": event.halt_time,
   464	        "reason_code": event.reason_code,
   465	        "reason_description": event.reason_description,
   466	        "resume_trade_time": event.resume_trade_time,
   467	        "source": event.source,
   468	        "sector": meta.sector if meta else None,
   469	        "subsector": meta.subsector if meta else None,
   470	        "note_context": note_context,
   471	    }
   472	    with log_path.open("a", encoding="utf-8") as f:
   473	        f.write(json.dumps(record, ensure_ascii=False) + "\n")
   474	
   475	
   476	def _post_health_heartbeat(stats: RunStats, slack_mode: str,
   477	                            ended_with_error: bool = False) -> None:
   478	    """End-of-run heartbeat to #street-account per HEALTH_REPORTING.md.
   479	    Only fires in slack live mode."""
   480	    if slack_mode != "live":
   481	        return
   482	    status = "error" if ended_with_error else "ok"
   483	    if stats.fetch_errors > 0 and not ended_with_error:
   484	        status = "warning"
   485	
   486	    base_parts = [
   487	        f"halt-monitor run summary: polls={stats.polls_completed}",
   488	        f"halts={stats.halts_emitted}",
   489	        f"resumes={stats.resumes_emitted}",
   490	        f"followups={stats.followups_emitted}",
   491	        f"hc_events={stats.hc_events_emitted}",
   492	        f"slack_ok/fail={stats.slack_posts_succeeded}/{stats.slack_posts_failed}",
   493	        f"fetch_errors={stats.fetch_errors}",
   494	    ]
   495	    enrich_part = ""
   496	    if (stats.earnings_calendar_loaded or stats.analyst_days_calendar_loaded
   497	            or stats.news_polls_completed):
   498	        enrich_bits = [f"enriched={stats.halts_enriched_with_note}"]
   499	        if stats.halts_enriched_with_cross_ref:
   500	            enrich_bits.append(f"cross_ref={stats.halts_enriched_with_cross_ref}")
   501	        sources = []
   502	        if stats.earnings_calendar_loaded:
   503	            sources.append(
   504	                f"earnings={stats.earnings_calendar_loaded}@{stats.earnings_calendar_generated_at or '?'}"
   505	            )
   506	        if stats.analyst_days_calendar_loaded:
   507	            sources.append(
   508	                f"analyst_days={stats.analyst_days_calendar_loaded}@{stats.analyst_days_calendar_generated_at or '?'}"
   509	            )
   510	        if stats.news_polls_completed:
   511	            sources.append(
   512	                f"news_cache={stats.news_cache_size} ({stats.news_polls_completed} polls)"
   513	            )
   514	        enrich_part = " ".join(enrich_bits) + " (" + ", ".join(sources) + ")"
   515	    summary = ", ".join(base_parts) + (" — " + enrich_part if enrich_part else "")
   516	    try:
   517	        slack.post_dm(summary, level=status if status in {"ok","warning","error"} else "warning")
   518	    except Exception as exc:
   519	        log.error("failed to post health heartbeat: %s", exc)
   520	
   521	
   522	def run(
   523	    *,
   524	    once: bool = False,
   525	    duration_sec: Optional[int] = None,
   526	    interval_sec: int = 5,
   527	    log_path: Optional[Path] = None,
   528	    include_non_emit: bool = False,
   529	    universe_path: Optional[Path] = None,
   530	    slack_mode: str = "off",
   531	    slack_webhook_url: Optional[str] = None,
   532	    persist_state: bool = True,
   533	    earnings_calendar_path: Optional[Path] = None,
   534	    analyst_days_calendar_path: Optional[Path] = None,
   535	    enable_news_cross_ref: bool = False,
   536	    news_window_minutes: int = 60,
   537	    hc_events_enabled: bool = False,
   538	) -> RunStats:
   539	    universe = Universe(universe_path) if universe_path else Universe()
   540	    log.info("loaded universe: %d tickers (%s)", len(universe), universe.filter_rule)
   541	    log.info("slack_mode=%s persist_state=%s", slack_mode, persist_state)
   542	
   543	    earnings_calendar = (
   544	        EarningsCalendar(earnings_calendar_path) if earnings_calendar_path else None
   545	    )
   546	    analyst_days_calendar = (
   547	        AnalystDayCalendar(analyst_days_calendar_path) if analyst_days_calendar_path else None
   548	    )
   549	    if earnings_calendar is not None:
   550	        log.info("earnings calendar: %d events (generated_at=%s)",
   551	                 earnings_calendar.loaded_count, earnings_calendar.generated_at)
   552	    if analyst_days_calendar is not None:
   553	        log.info("analyst-days calendar: %d events (generated_at=%s)",
   554	                 analyst_days_calendar.loaded_count, analyst_days_calendar.generated_at)
   555	
   556	    news_cache: Optional[NewsCache] = (
   557	        NewsCache(window_minutes=news_window_minutes) if enable_news_cross_ref else None
   558	    )
   559	    if news_cache is not None:
   560	        log.info("news cross-ref enabled (window=%dm, sources=PRN/BW/GNW)", news_window_minutes)
   561	    if hc_events_enabled:
   562	        log.info("HC event-wire enabled (FDA actions + clinical readouts → #street-account)")
   563	
   564	    if persist_state:
   565	        state.cleanup_old()
   566	        tracker = state.load()
   567	    else:
   568	        tracker = HaltTracker()
   569	
   570	    stats = RunStats(started_at=datetime.now(timezone.utc).isoformat(timespec="seconds"))
   571	    stats.state_loads = 1 if persist_state else 0
   572	    if earnings_calendar is not None:
   573	        stats.earnings_calendar_loaded = earnings_calendar.loaded_count
   574	        stats.earnings_calendar_generated_at = earnings_calendar.generated_at
   575	    if analyst_days_calendar is not None:
   576	        stats.analyst_days_calendar_loaded = analyst_days_calendar.loaded_count
   577	        stats.analyst_days_calendar_generated_at = analyst_days_calendar.generated_at
   578	    health = {name: FeedHealth(**stats.feed_health[name])
   579	              for name in stats.feed_health}
   580	
   581	    signal.signal(signal.SIGINT, _handle_signal)
   582	    signal.signal(signal.SIGTERM, _handle_signal)
   583	
   584	    deadline = (time.monotonic() + duration_sec) if duration_sec else None
   585	    ended_with_error = False
   586	
   587	    try:
   588	        while not _should_stop:
   589	            t0 = time.monotonic()
   590	
   591	            nasdaq_events = _safe_fetch("nasdaq_rss", nasdaq.fetch, stats, health, slack_mode)
   592	            nyse_events = _safe_fetch("nyse_csv", nyse.fetch, stats, health, slack_mode)
   593	            stats.nasdaq_events_seen = max(stats.nasdaq_events_seen, len(nasdaq_events))
   594	            stats.nyse_events_seen = max(stats.nyse_events_seen, len(nyse_events))
   595	
   596	            # News-feed poll (slower cadence than halt feeds — every Nth tick).
   597	            # Run BEFORE halt emit so a halt fired this tick can cross-ref news
   598	            # already in the cache from this poll. Fetched when EITHER the
   599	            # cross-ref cache OR the HC event-wire is enabled (both consume the
   600	            # same freshly-fetched news_items).
   601	            if ((news_cache is not None or hc_events_enabled)
   602	                    and stats.polls_completed % NEWS_POLL_EVERY == 0):
   603	                news_items: list = []
   604	                for label, mod in [("news_prnewswire", prnewswire),
   605	                                    ("news_businesswire", bw),
   606	                                    ("news_globenewswire", gnw)]:
   607	                    news_items.extend(_safe_fetch(label, mod.fetch, stats, health, slack_mode))
   608	                if news_cache is not None:
   609	                    added = news_cache.ingest(news_items)
   610	                    stats.news_polls_completed += 1
   611	                    stats.news_items_ingested += added
   612	                    # tickers_indexed reflects items currently in the lookback
   613	                    # window (i.e. cross-refable). news_items_ingested is the
   614	                    # cumulative count for dedup auditing.
   615	                    stats.news_cache_size = news_cache.tickers_indexed
   616	                # HC event-wire: classify the freshly-fetched PR-wire items and
   617	                # emit alerts for covered names (additive; does not touch the
   618	                # halt/resume/follow-up paths above).
   619	                if hc_events_enabled:
   620	                    _emit_hc_events(news_items, universe, tracker, log_path,
   621	                                    stats=stats, slack_mode=slack_mode,
   622	                                    slack_webhook_url=slack_webhook_url)
   623	
   624	            for kind, event in tracker.ingest(nasdaq_events + nyse_events):
   625	                _emit(kind, event, universe, log_path,
   626	                      include_non_emit=include_non_emit, stats=stats,
   627	                      slack_mode=slack_mode, slack_webhook_url=slack_webhook_url,
   628	                      earnings_calendar=earnings_calendar,
   629	                      analyst_days_calendar=analyst_days_calendar,
   630	                      news_cache=news_cache, tracker=tracker)
   631	
   632	            # §7 Follow-up detection: a PR that crossed AFTER a previously-
   633	            # posted covered halt fired. Runs every poll (cheap — iterates
   634	            # seen_halts) so a follow-up fires promptly once its PR lands in
   635	            # the news cache. Obeys the same slack_mode gating as halt/resume.
   636	            _emit_followups(tracker, universe, news_cache, log_path,
   637	                            stats=stats, slack_mode=slack_mode,
   638	                            slack_webhook_url=slack_webhook_url)
   639	
   640	            if persist_state:

exec
/bin/bash -lc "nl -ba src/template.py | sed -n '238,310p'" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor
 succeeded in 0ms:
   238	
   239	
   240	# Human label for an HC event type, used in the biotech triage CTA so it reads
   241	# "Biotech readout — triage this name?" rather than the halt path's "Biotech halt".
   242	_HC_CTA_CONTEXT = {
   243	    "trial_readout": "readout",
   244	    "fda_approval": "approval",
   245	    "crl": "CRL",
   246	    "clearance": "clearance",
   247	}
   248	
   249	
   250	def hc_event_cta_context(event) -> str:
   251	    """CTA context word for an HCEvent (falls back to a generic 'event')."""
   252	    return _HC_CTA_CONTEXT.get(event.event_type, "event")
   253	
   254	
   255	def render_hc_event(event, *, sector: str = "", subsector: str = "") -> str:
   256	    """Render an HC event-wire alert to plain text (stdout / --slack off).
   257	
   258	    event is an events.types.HCEvent. Mirrors render_followup's shape:
   259	
   260	        HH:MM ET M/DD/YY [StreetAccount] TICKER {verbatim issuer headline}
   261	        Sector: {sector} / {subsector}
   262	        [biotech triage CTA if biotech]
   263	        Source: {Wire} press release · {url}
   264	    """
   265	    time_hhmm, date_short = hc_event_when(event.published_at)
   266	    headline = hc_event_headline(event.headline)
   267	
   268	    when = f"{time_hhmm} ET {date_short} ".lstrip() if time_hhmm else ""
   269	    header = f"{when}[StreetAccount] {event.symbol} {headline}"
   270	
   271	    parts = [header]
   272	    if sector:
   273	        sector_line = f"Sector: {sector}"
   274	        if subsector:
   275	            sector_line += f" / {subsector}"
   276	        parts.append(sector_line)
   277	    if is_biotech(subsector):
   278	        parts.append(biotech_triage_cta(event.symbol, context=hc_event_cta_context(event)))
   279	    label = _NEWS_SOURCE_LABELS.get(event.source, event.source)
   280	    source_line = f"Source: {label} press release"
   281	    if event.url:
   282	        source_line += f" · {event.url}"
   283	    parts.append(source_line)
   284	    return "\n".join(parts)
   285	
   286	
   287	def render_resume(event: HaltEvent, *, sector: str = "", subsector: str = "",
   288	                  name_override: str = "") -> str:
   289	    """Render a resume notification — sa-monitor variant.
   290	
   291	    Phase 1 v0 template (per template-library.md §6):
   292	
   293	        SA: TICKER [shares to resume trading at HH:MM ET]
   294	        HH:MM ET M/DD/YY [TICKER] resume scheduled at HH:MM ET, originally halted at HH:MM ET
   295	        Sector: {sector} / {subsector}
   296	
   297	    Halt duration is omitted in v0 — it's computed in D6 with full timestamp
   298	    parsing once we know how the exchange feeds publish resume timestamps.
   299	    """
   300	    name = _ticker_display_name(event, name_override)
   301	    date_short = _format_date_short(event.halt_date)
   302	    halt_time_hhmm = _format_time_hhmm(event.halt_time)
   303	    resume_time_hhmm = _format_time_hhmm(event.resume_trade_time or "")
   304	
   305	    headline = f"SA: {event.symbol} [shares to resume trading at {resume_time_hhmm} ET]"
   306	
   307	    body_line = (
   308	        f"{resume_time_hhmm} ET {date_short} [{event.symbol}] resume scheduled "
   309	        f"at {resume_time_hhmm} ET, originally halted at {halt_time_hhmm} ET "
   310	        f"({event.reason_code} - {event.reason_description})"

exec
/bin/bash -lc "nl -ba src/halt_monitor.py | sed -n '640,735p'" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor
 succeeded in 0ms:
   640	            if persist_state:
   641	                try:
   642	                    state.save(tracker)
   643	                    stats.state_saves += 1
   644	                except Exception as exc:
   645	                    log.error("state save failed: %s", exc)
   646	
   647	            stats.polls_completed += 1
   648	            if stats.polls_completed % 12 == 0:
   649	                log.info("poll %d: tracked=%d emit=%d/%d errors=%d",
   650	                         stats.polls_completed, len(tracker),
   651	                         stats.halts_emitted, stats.resumes_emitted, stats.fetch_errors)
   652	
   653	            if once:
   654	                break
   655	            if deadline and time.monotonic() >= deadline:
   656	                log.info("duration reached; stopping")
   657	                break
   658	
   659	            elapsed = time.monotonic() - t0
   660	            time.sleep(max(0.0, interval_sec - elapsed))
   661	    except Exception as exc:
   662	        ended_with_error = True
   663	        log.exception("run aborted: %s", exc)
   664	        if slack_mode == "live":
   665	            try:
   666	                slack.post_dm(f"halt-monitor crashed: `{exc}`", level="error")
   667	            except Exception:
   668	                pass
   669	        raise
   670	    finally:
   671	        # Persist health snapshot back into stats for the final report
   672	        for name, h in health.items():
   673	            stats.feed_health[name] = asdict(h)
   674	        log.info("run ended: polls=%d halts=%d resumes=%d errors=%d",
   675	                 stats.polls_completed, stats.halts_emitted,
   676	                 stats.resumes_emitted, stats.fetch_errors)
   677	        _post_health_heartbeat(stats, slack_mode, ended_with_error=ended_with_error)
   678	
   679	    return stats
   680	
   681	
   682	def main(argv: Optional[list[str]] = None) -> int:
   683	    parser = argparse.ArgumentParser(description="sa-monitor halt feed runner")
   684	    parser.add_argument("--once", action="store_true")
   685	    parser.add_argument("--duration", type=int, default=None)
   686	    parser.add_argument("--interval", type=int, default=5)
   687	    parser.add_argument(
   688	        "--log", type=Path,
   689	        default=Path("logs") / f"halt_monitor_{datetime.now().strftime('%Y%m%d_%H%M%S')}.jsonl",
   690	    )
   691	    parser.add_argument("--include-non-emit", action="store_true")
   692	    parser.add_argument("--universe", type=Path, default=None)
   693	    parser.add_argument("--slack", choices=["off", "dry-run", "live"], default="off")
   694	    parser.add_argument("--no-persist", action="store_true",
   695	                        help="disable dedup-state persistence (default: enabled)")
   696	    parser.add_argument("--earnings-calendar", type=Path, default=None,
   697	                        help="path to earnings-agent upcoming_events.json (Phase 2 enrichment)")
   698	    parser.add_argument("--analyst-days-calendar", type=Path, default=None,
   699	                        help="path to analyst-days upcoming_events.json (Phase 2 enrichment)")
   700	    parser.add_argument("--news-cross-ref", action="store_true",
   701	                        help="enable PRN/BW/GNW news cross-ref enrichment (Phase 2 slice 2B)")
   702	    parser.add_argument("--news-window-minutes", type=int, default=60,
   703	                        help="lookback window for news cross-ref (default 60min)")
   704	    parser.add_argument("--hc-events", action="store_true",
   705	                        help="enable the HC event-wire lane (FDA approvals/CRLs + "
   706	                             "clinical-trial readouts on covered names → #street-account)")
   707	    args = parser.parse_args(argv)
   708	
   709	    stats = run(
   710	        once=args.once, duration_sec=args.duration, interval_sec=args.interval,
   711	        log_path=args.log, include_non_emit=args.include_non_emit,
   712	        universe_path=args.universe, slack_mode=args.slack,
   713	        persist_state=not args.no_persist,
   714	        earnings_calendar_path=args.earnings_calendar,
   715	        analyst_days_calendar_path=args.analyst_days_calendar,
   716	        enable_news_cross_ref=args.news_cross_ref,
   717	        news_window_minutes=args.news_window_minutes,
   718	        hc_events_enabled=args.hc_events,
   719	    )
   720	    print(json.dumps(asdict(stats), indent=2))
   721	    return 0
   722	
   723	
   724	if __name__ == "__main__":
   725	    sys.exit(main())

exec
/bin/bash -lc "nl -ba src/state.py | sed -n '55,135p'" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor
 succeeded in 0ms:
    55	    state_dir.mkdir(parents=True, exist_ok=True)
    56	    day_key = day_key or _today_et_key()
    57	    path = _state_file_for(day_key, state_dir)
    58	
    59	    payload = {
    60	        "schema_version": 1,
    61	        "trading_day_et": day_key,
    62	        "saved_at_utc": dt.datetime.now(dt.timezone.utc).isoformat(timespec="seconds"),
    63	        "halts": [list(hid) for hid in tracker.seen_halts.keys()],
    64	        "resumes_emitted": [list(hid) for hid in tracker.resumes_emitted],
    65	        "emitted_halts": [list(hid) for hid in tracker.emitted_halts],
    66	        "followed_up": [list(hid) for hid in tracker.followed_up],
    67	        "hc_events_emitted": sorted(tracker.hc_events_emitted),
    68	    }
    69	
    70	    # Atomic write: tmp file → rename
    71	    fd, tmp_path = tempfile.mkstemp(dir=str(state_dir), prefix=".tmp_", suffix=".json")
    72	    try:
    73	        with os.fdopen(fd, "w", encoding="utf-8") as f:
    74	            json.dump(payload, f, indent=2)
    75	        os.replace(tmp_path, path)
    76	    except Exception:
    77	        if os.path.exists(tmp_path):
    78	            os.unlink(tmp_path)
    79	        raise
    80	    return path
    81	
    82	
    83	def load(*, state_dir: Path = DEFAULT_STATE_DIR, day_key: str = "") -> HaltTracker:
    84	    """Rehydrate a tracker from today's state file (if any)."""
    85	    day_key = day_key or _today_et_key()
    86	    path = _state_file_for(day_key, state_dir)
    87	    tracker = HaltTracker()
    88	    if not path.exists():
    89	        log.info("state: no prior file at %s; starting fresh", path)
    90	        return tracker
    91	    try:
    92	        payload = json.loads(path.read_text(encoding="utf-8"))
    93	    except (OSError, json.JSONDecodeError) as exc:
    94	        log.warning("state: failed to read %s (%s); starting fresh", path, exc)
    95	        return tracker
    96	
    97	    if payload.get("schema_version") != 1:
    98	        log.warning(
    99	            "state: unexpected schema_version %r in %s; starting fresh",
   100	            payload.get("schema_version"), path,
   101	        )
   102	        return tracker
   103	
   104	    halts = [tuple(h) for h in payload.get("halts", [])]
   105	    resumes = {tuple(r) for r in payload.get("resumes_emitted", [])}
   106	    # Additive fields (introduced after schema v1 shipped); older state files
   107	    # simply lack them and rehydrate to empty sets.
   108	    emitted_halts = {tuple(e) for e in payload.get("emitted_halts", [])}
   109	    followed_up = {tuple(f) for f in payload.get("followed_up", [])}
   110	    # HC event-wire delivery-dedup — string keys, not HaltId tuples.
   111	    hc_events_emitted = set(payload.get("hc_events_emitted", []))
   112	
   113	    # Pre-fill seen_halts with placeholder HaltEvents — we only need the
   114	    # halt_ids to be populated so dedup works; the full event history isn't
   115	    # required for the runner's correctness.
   116	    for hid in halts:
   117	        symbol, halt_date, halt_time = hid
   118	        tracker.seen_halts[(symbol, halt_date, halt_time)] = HaltEvent(
   119	            symbol=symbol,
   120	            exchange="",
   121	            halt_date=halt_date,
   122	            halt_time=halt_time,
   123	            reason_code="",
   124	            reason_description="",
   125	            source="restored_from_state",
   126	        )
   127	    tracker.resumes_emitted = resumes
   128	    tracker.emitted_halts = emitted_halts
   129	    tracker.followed_up = followed_up
   130	    tracker.hc_events_emitted = hc_events_emitted
   131	    log.info(
   132	        "state: rehydrated from %s (%d halts, %d resumes, %d emitted, "
   133	        "%d followed_up, %d hc_events)",
   134	        path, len(halts), len(resumes), len(emitted_halts), len(followed_up),
   135	        len(hc_events_emitted),

exec
/bin/bash -lc "nl -ba src/dedup.py | sed -n '1,110p'" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor
 succeeded in 0ms:
     1	"""Halt-event deduplication.
     2	
     3	Both the Nasdaq RSS and NYSE CSV feeds report the same halt event (since the
     4	exchange tape consolidates both). Polling at 5s means each event re-appears
     5	in N consecutive polls until the feed rolls it off. We dedupe by halt-id
     6	`(symbol, halt_date, halt_time)` so the runner only fires one Slack message
     7	per real event, and one resume message per resume.
     8	
     9	State lives in memory for the duration of a single market-session run. A
    10	small persistence layer (writing seen-ids to disk between polls) is deferred
    11	to D6 — the end-to-end runner — so the script survives restart-mid-session.
    12	"""
    13	from __future__ import annotations
    14	
    15	from dataclasses import dataclass, field
    16	from typing import Iterator
    17	
    18	from .feeds.types import HaltEvent
    19	
    20	# halt_id -> the most-recently-seen HaltEvent for that id (so resume info
    21	# accumulates as the feed publishes it).
    22	HaltId = tuple[str, str, str]
    23	
    24	
    25	@dataclass
    26	class HaltTracker:
    27	    """Tracks halt + resume events across feed polls.
    28	
    29	    Use:
    30	        tracker = HaltTracker()
    31	        for event in tracker.ingest(events_from_this_poll):
    32	            ... # only new halts and newly-published resumes come out
    33	    """
    34	
    35	    seen_halts: dict[HaltId, HaltEvent] = field(default_factory=dict)
    36	    resumes_emitted: set[HaltId] = field(default_factory=set)
    37	    # halt_ids we actually DELIVERED a halt alert for (passed the non-emit-code
    38	    # filter AND — in live mode — posted successfully). seen_halts records mere
    39	    # OBSERVATIONS; this records deliveries. A §7 Follow-up may only fire for a
    40	    # halt we delivered. Persisted so a restart preserves the gate.
    41	    emitted_halts: set[HaltId] = field(default_factory=set)
    42	    # halt_ids for which a §7 Follow-up alert has already fired — one per halt,
    43	    # ever. Persisted alongside seen_halts so a mid-session restart never
    44	    # re-emits a follow-up.
    45	    followed_up: set[HaltId] = field(default_factory=set)
    46	    # Delivery-dedup for the HC event-wire lane. Keys are strings of the form
    47	    # f"{news_id}|{symbol}|{event_type}" — one alert per (press release,
    48	    # covered ticker, event type), ever. Persisted like followed_up so a
    49	    # mid-session restart never re-emits an HC event. (Mirrors emitted_halts /
    50	    # followed_up exactly; unlike them, keyed by string not HaltId.)
    51	    hc_events_emitted: set[str] = field(default_factory=set)
    52	
    53	    def ingest(self, events: list[HaltEvent]) -> Iterator[tuple[str, HaltEvent]]:
    54	        """Diff `events` against state and yield (kind, event) for new ones.
    55	
    56	        kind ∈ {"halt", "resume"}.
    57	
    58	        - halt: first time we see this halt_id
    59	        - resume: previously-seen halt now has a resume_trade_time populated
    60	          AND we haven't emitted a resume for this halt_id yet
    61	        """
    62	        for event in events:
    63	            hid = event.halt_id
    64	            previous = self.seen_halts.get(hid)
    65	
    66	            if previous is None:
    67	                # First sighting — emit halt
    68	                self.seen_halts[hid] = event
    69	                yield ("halt", event)
    70	                # If the very first sighting already includes a resume time
    71	                # (rare — usually catch-up from a feed roll), also emit resume
    72	                if event.is_resumed and hid not in self.resumes_emitted:
    73	                    self.resumes_emitted.add(hid)
    74	                    yield ("resume", event)
    75	                continue
    76	
    77	            # Already seen — update stored event (resume info may have
    78	            # populated since the prior poll) and check whether resume
    79	            # is freshly-known.
    80	            self.seen_halts[hid] = event
    81	            if event.is_resumed and hid not in self.resumes_emitted:
    82	                self.resumes_emitted.add(hid)
    83	                yield ("resume", event)
    84	
    85	    def __len__(self) -> int:
    86	        return len(self.seen_halts)
    87	
    88	    def reset(self) -> None:
    89	        """Clear state — used at session boundary or for tests."""
    90	        self.seen_halts.clear()
    91	        self.resumes_emitted.clear()
    92	        self.emitted_halts.clear()
    93	        self.followed_up.clear()
    94	        self.hc_events_emitted.clear()

codex
The round-one gates are present in the intended order: CRL before regulatory suppression, then shared denial/commentary/pending suppression for approval and clearance, and trial suppression only inside the trial path. I’m going to execute the focused suite next, then probe additional headline variants directly against `classify()`.
exec
/bin/bash -lc 'PYTHONDONTWRITEBYTECODE=1 pytest -q -p no:cacheprovider tests/test_hc_events.py' in /mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor
 exited 1 in 262ms:
Traceback (most recent call last):
  File "/home/jroyp/.local/bin/pytest", line 6, in <module>
    sys.exit(console_main())
             ^^^^^^^^^^^^^^
  File "/home/jroyp/.local/lib/python3.12/site-packages/_pytest/config/__init__.py", line 223, in console_main
    code = main()
           ^^^^^^
  File "/home/jroyp/.local/lib/python3.12/site-packages/_pytest/config/__init__.py", line 193, in main
    config = _prepareconfig(new_args, plugins)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/jroyp/.local/lib/python3.12/site-packages/_pytest/config/__init__.py", line 361, in _prepareconfig
    config: Config = pluginmanager.hook.pytest_cmdline_parse(
                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/jroyp/.local/lib/python3.12/site-packages/pluggy/_hooks.py", line 512, in __call__
    return self._hookexec(self.name, self._hookimpls.copy(), kwargs, firstresult)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/jroyp/.local/lib/python3.12/site-packages/pluggy/_manager.py", line 120, in _hookexec
    return self._inner_hookexec(hook_name, methods, kwargs, firstresult)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/jroyp/.local/lib/python3.12/site-packages/pluggy/_callers.py", line 167, in _multicall
    raise exception
  File "/home/jroyp/.local/lib/python3.12/site-packages/pluggy/_callers.py", line 139, in _multicall
    teardown.throw(exception)
  File "/home/jroyp/.local/lib/python3.12/site-packages/_pytest/helpconfig.py", line 124, in pytest_cmdline_parse
    config = yield
             ^^^^^
  File "/home/jroyp/.local/lib/python3.12/site-packages/pluggy/_callers.py", line 121, in _multicall
    res = hook_impl.function(*args)
          ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/jroyp/.local/lib/python3.12/site-packages/_pytest/config/__init__.py", line 1192, in pytest_cmdline_parse
    self.parse(args)
  File "/home/jroyp/.local/lib/python3.12/site-packages/_pytest/config/__init__.py", line 1562, in parse
    self.hook.pytest_load_initial_conftests(
  File "/home/jroyp/.local/lib/python3.12/site-packages/pluggy/_hooks.py", line 512, in __call__
    return self._hookexec(self.name, self._hookimpls.copy(), kwargs, firstresult)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/jroyp/.local/lib/python3.12/site-packages/pluggy/_manager.py", line 120, in _hookexec
    return self._inner_hookexec(hook_name, methods, kwargs, firstresult)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/jroyp/.local/lib/python3.12/site-packages/pluggy/_callers.py", line 167, in _multicall
    raise exception
  File "/home/jroyp/.local/lib/python3.12/site-packages/pluggy/_callers.py", line 139, in _multicall
    teardown.throw(exception)
  File "/home/jroyp/.local/lib/python3.12/site-packages/_pytest/warnings.py", line 128, in pytest_load_initial_conftests
    return (yield)
            ^^^^^
  File "/home/jroyp/.local/lib/python3.12/site-packages/pluggy/_callers.py", line 116, in _multicall
    next(function_gen)  # first yield
    ^^^^^^^^^^^^^^^^^^
  File "/home/jroyp/.local/lib/python3.12/site-packages/_pytest/capture.py", line 170, in pytest_load_initial_conftests
    capman.start_global_capturing()
  File "/home/jroyp/.local/lib/python3.12/site-packages/_pytest/capture.py", line 773, in start_global_capturing
    self._global_capturing = _get_multicapture(self._method)
                             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/jroyp/.local/lib/python3.12/site-packages/_pytest/capture.py", line 714, in _get_multicapture
    return MultiCapture(in_=FDCapture(0), out=FDCapture(1), err=FDCapture(2))
                                              ^^^^^^^^^^^^
  File "/home/jroyp/.local/lib/python3.12/site-packages/_pytest/capture.py", line 493, in __init__
    TemporaryFile(buffering=0),
    ^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/usr/lib/python3.12/tempfile.py", line 770, in TemporaryFile
    prefix, suffix, dir, output_type = _sanitize_params(prefix, suffix, dir)
                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/usr/lib/python3.12/tempfile.py", line 265, in _sanitize_params
    dir = gettempdir()
          ^^^^^^^^^^^^
  File "/usr/lib/python3.12/tempfile.py", line 454, in gettempdir
    return _os.fsdecode(_gettempdir())
                        ^^^^^^^^^^^^^
  File "/usr/lib/python3.12/tempfile.py", line 447, in _gettempdir
    tempdir = _get_default_tempdir()
              ^^^^^^^^^^^^^^^^^^^^^^
  File "/usr/lib/python3.12/tempfile.py", line 362, in _get_default_tempdir
    raise FileNotFoundError(_errno.ENOENT,
FileNotFoundError: [Errno 2] No usable temporary directory found in ['/tmp', '/var/tmp', '/usr/tmp', '/mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor']

codex
Pytest cannot start in this sandbox because Python cannot create any temporary file under `/tmp`, `/var/tmp`, or the workspace. I’ll still run direct import-level classifier probes with bytecode disabled; those don’t need temp files and are enough to validate the regex behavior.
exec
/bin/bash -lc "PYTHONDONTWRITEBYTECODE=1 python - <<'PY'
from src.events.classify import classify
from src.news.types import NewsItem

def item(title):
    return NewsItem(source='prnewswire', title=title, body='', url='u', published_at='2026-05-05T11:05:00+00:00', tickers=('VRDN',))

heads = [
    'Acme expects topline Phase 3 data in Q4 2026',
    'Acme anticipates topline Phase 3 data in the second half of 2026',
    'Acme announces Phase 3 topline data due in Q4 2026',
    'Acme announces upcoming topline Phase 3 data in Q4 2026',
    'Acme expects FDA clearance for its cardiac monitor in Q4 2026',
    'Acme says FDA clearance remains pending for its cardiac monitor',
    'Acme targets FDA clearance for its cardiac monitor in 2027',
    'Acme announces pending FDA approval of acmezumab',
    'Acme highlights FDA approval of RivalDrug by a competitor',
    'Acme notes FDA approval of RivalDrug by a competitor',
    'FDA approves Acmezumab following Priority Review',
]
for h in heads:
    ev = classify(item(h))
    print(h)
    print(' ->', None if ev is None else (ev.event_type, ev.direction, ev.phase))
PY" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor
 exited 127 in 0ms:
/bin/bash: line 1: python: command not found

codex
`python` is not on PATH here, so I’m switching to `python3` for the same import-level probes.
exec
/bin/bash -lc "PYTHONDONTWRITEBYTECODE=1 python3 - <<'PY'
from src.events.classify import classify
from src.news.types import NewsItem

def item(title):
    return NewsItem(source='prnewswire', title=title, body='', url='u', published_at='2026-05-05T11:05:00+00:00', tickers=('VRDN',))

heads = [
    'Acme expects topline Phase 3 data in Q4 2026',
    'Acme anticipates topline Phase 3 data in the second half of 2026',
    'Acme announces Phase 3 topline data due in Q4 2026',
    'Acme announces upcoming topline Phase 3 data in Q4 2026',
    'Acme expects FDA clearance for its cardiac monitor in Q4 2026',
    'Acme says FDA clearance remains pending for its cardiac monitor',
    'Acme targets FDA clearance for its cardiac monitor in 2027',
    'Acme announces pending FDA approval of acmezumab',
    'Acme highlights FDA approval of RivalDrug by a competitor',
    'Acme notes FDA approval of RivalDrug by a competitor',
    'FDA approves Acmezumab following Priority Review',
]
for h in heads:
    ev = classify(item(h))
    print(h)
    print(' ->', None if ev is None else (ev.event_type, ev.direction, ev.phase))
PY" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor
 succeeded in 0ms:
Acme expects topline Phase 3 data in Q4 2026
 -> ('trial_readout', 'n/a', '3')
Acme anticipates topline Phase 3 data in the second half of 2026
 -> ('trial_readout', 'n/a', '3')
Acme announces Phase 3 topline data due in Q4 2026
 -> ('trial_readout', 'n/a', '3')
Acme announces upcoming topline Phase 3 data in Q4 2026
 -> ('trial_readout', 'n/a', '3')
Acme expects FDA clearance for its cardiac monitor in Q4 2026
 -> ('clearance', 'n/a', '')
Acme says FDA clearance remains pending for its cardiac monitor
 -> ('clearance', 'n/a', '')
Acme targets FDA clearance for its cardiac monitor in 2027
 -> ('clearance', 'n/a', '')
Acme announces pending FDA approval of acmezumab
 -> ('fda_approval', 'n/a', '')
Acme highlights FDA approval of RivalDrug by a competitor
 -> ('fda_approval', 'n/a', '')
Acme notes FDA approval of RivalDrug by a competitor
 -> ('fda_approval', 'n/a', '')
FDA approves Acmezumab following Priority Review
 -> None

codex
The direct probes found several remaining misfires and one real-event suppression. I’m going to cross-check the originally fixed repros and the anchored positives so I can separate “round 1 fixed correctly” from the remaining gaps.
exec
/bin/bash -lc "PYTHONDONTWRITEBYTECODE=1 python3 - <<'PY'
from src.events.classify import classify
from src.news.types import NewsItem

def item(title):
    return NewsItem(source='prnewswire', title=title, body='', url='u', published_at='2026-05-05T11:05:00+00:00', tickers=('VRDN',))

fixed_repros = [
    'Acme did not receive FDA clearance for its cardiac monitor',
    'Acme did not receive FDA approval for acmezumab',
    'Acme comments on FDA approval of RivalDrug by a competitor',
    'Acme announces topline Phase 3 data expected in Q4 2026',
    'CRL Announces First-Quarter 2026 Financial Results',
]
real_events = [
    'Viridian reports Phase 3 REVEAL-2 trial of elegrobart in chronic thyroid eye disease met its primary endpoint',
    'Corcept reports Phase 2 DAZALS study of dazucorilant does not meet primary endpoint',
    'FDA approves Veppanu for ESR1M, ER+/HER2- advanced breast cancer',
    'Regenco receives Complete Response Letter from FDA for regzumab',
    'Deviceco receives FDA clearance for its AI-powered cardiac monitor',
]
print('fixed_repros')
for h in fixed_repros:
    ev = classify(item(h))
    print(None if ev is None else (ev.event_type, ev.direction, ev.phase), ' :: ', h)
print('real_events')
for h in real_events:
    ev = classify(item(h))
    print(None if ev is None else (ev.event_type, ev.direction, ev.phase), ' :: ', h)
PY" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor
 succeeded in 0ms:
fixed_repros
None  ::  Acme did not receive FDA clearance for its cardiac monitor
None  ::  Acme did not receive FDA approval for acmezumab
None  ::  Acme comments on FDA approval of RivalDrug by a competitor
None  ::  Acme announces topline Phase 3 data expected in Q4 2026
None  ::  CRL Announces First-Quarter 2026 Financial Results
real_events
('trial_readout', 'met', '3')  ::  Viridian reports Phase 3 REVEAL-2 trial of elegrobart in chronic thyroid eye disease met its primary endpoint
('trial_readout', 'missed', '2')  ::  Corcept reports Phase 2 DAZALS study of dazucorilant does not meet primary endpoint
('fda_approval', 'n/a', '')  ::  FDA approves Veppanu for ESR1M, ER+/HER2- advanced breast cancer
('crl', 'n/a', '')  ::  Regenco receives Complete Response Letter from FDA for regzumab
('clearance', 'n/a', '')  ::  Deviceco receives FDA clearance for its AI-powered cardiac monitor

codex
The original five repros now return `None`, and the core anchored real events still classify. I’m checking the CTA path separately now because that is outside `classify()` and can regress independently in stdout vs Slack.
exec
/bin/bash -lc "PYTHONDONTWRITEBYTECODE=1 python3 - <<'PY'
from dataclasses import replace
from src.template import biotech_triage_cta, render_hc_event
from src.slack import build_hc_event_blocks
from src.events.classify import classify
from src.news.types import NewsItem
from src.coverage import TickerMeta

item = NewsItem(source='prnewswire', title='Viridian reports Phase 3 REVEAL-2 trial met its primary endpoint', body='', url='u', published_at='2026-05-05T11:05:00+00:00', tickers=('VRDN',))
ev = replace(classify(item), symbol='VRDN')
meta = TickerMeta(symbol='VRDN', name='VRDN Inc', sector='Biopharma', subsector='Biotech')
print('halt_cta:', biotech_triage_cta('VRDN'))
print('hc_render_contains:', 'Biotech readout' in render_hc_event(ev, sector=meta.sector, subsector=meta.subsector), 'Biotech halt' in render_hc_event(ev, sector=meta.sector, subsector=meta.subsector))
text = build_hc_event_blocks(ev, meta)['blocks'][0]['text']['text']
print('hc_slack_contains:', 'Biotech readout' in text, 'Biotech halt' in text)
PY" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor
 succeeded in 141ms:
halt_cta: :dna: Biotech halt — triage this name?  ->  `triage VRDN`
hc_render_contains: True False
hc_slack_contains: True False

codex
**Findings**

- **High:** Future readout language still slips through the trial classifier. [src/events/classify.py](</mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor/src/events/classify.py:79>) only catches `expects to`, and [lines 85-87](</mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor/src/events/classify.py:85>) require `expected/anticipated` or `readout` in narrow forms.  
  Repro: `Acme expects topline Phase 3 data in Q4 2026` -> `('trial_readout', 'n/a', '3')`; expected `None`. Same issue with `Acme announces Phase 3 topline data due in Q4 2026` and `upcoming topline Phase 3 data`.

- **High:** Clearance still matches pending/target/expected language because [src/events/classify.py:192](</mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor/src/events/classify.py:192>) matches bare `FDA clearance`, while the suppression gate lacks `pending`, `expects`, `targets`, etc.  
  Repro: `Acme expects FDA clearance for its cardiac monitor in Q4 2026` -> `('clearance', 'n/a', '')`; expected `None`. Also: `Acme says FDA clearance remains pending...` -> clearance.

- **High:** Commentary suppression is still incomplete. [src/events/classify.py:168](</mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor/src/events/classify.py:168>) catches `comments/congratulates/statement`, but misses common commentary verbs like `highlights` and `notes`, while [line 125](</mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor/src/events/classify.py:125>) accepts `FDA approval of`.  
  Repro: `Acme highlights FDA approval of RivalDrug by a competitor` -> `('fda_approval', 'n/a', '')`; expected `None`.

- **High:** The pending-milestone gate suppresses real approval headlines that mention priority review. [src/events/classify.py:140](</mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor/src/events/classify.py:140>) treats any `priority review` as suppressive before approval matching at [line 223](</mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor/src/events/classify.py:223>).  
  Repro: `FDA approves Acmezumab following Priority Review` -> `None`; expected `fda_approval`.

**Verification**

The original five classifier repros now return `None`, and the anchored real events I checked still fire. CTA wording also looks correct: halt path still says `Biotech halt`, while HC render/Slack blocks say `Biotech readout`.

I could not run `pytest` in this read-only sandbox because pytest failed before collection with `No usable temporary directory found`. Direct `python3` import-level probes were successful.
tokens used
79,907
**Findings**

- **High:** Future readout language still slips through the trial classifier. [src/events/classify.py](</mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor/src/events/classify.py:79>) only catches `expects to`, and [lines 85-87](</mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor/src/events/classify.py:85>) require `expected/anticipated` or `readout` in narrow forms.  
  Repro: `Acme expects topline Phase 3 data in Q4 2026` -> `('trial_readout', 'n/a', '3')`; expected `None`. Same issue with `Acme announces Phase 3 topline data due in Q4 2026` and `upcoming topline Phase 3 data`.

- **High:** Clearance still matches pending/target/expected language because [src/events/classify.py:192](</mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor/src/events/classify.py:192>) matches bare `FDA clearance`, while the suppression gate lacks `pending`, `expects`, `targets`, etc.  
  Repro: `Acme expects FDA clearance for its cardiac monitor in Q4 2026` -> `('clearance', 'n/a', '')`; expected `None`. Also: `Acme says FDA clearance remains pending...` -> clearance.

- **High:** Commentary suppression is still incomplete. [src/events/classify.py:168](</mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor/src/events/classify.py:168>) catches `comments/congratulates/statement`, but misses common commentary verbs like `highlights` and `notes`, while [line 125](</mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor/src/events/classify.py:125>) accepts `FDA approval of`.  
  Repro: `Acme highlights FDA approval of RivalDrug by a competitor` -> `('fda_approval', 'n/a', '')`; expected `None`.

- **High:** The pending-milestone gate suppresses real approval headlines that mention priority review. [src/events/classify.py:140](</mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor/src/events/classify.py:140>) treats any `priority review` as suppressive before approval matching at [line 223](</mnt/c/Users/jroyp/Dropbox/Claude Folder/sa-monitor/src/events/classify.py:223>).  
  Repro: `FDA approves Acmezumab following Priority Review` -> `None`; expected `fda_approval`.

**Verification**

The original five classifier repros now return `None`, and the anchored real events I checked still fire. CTA wording also looks correct: halt path still says `Biotech halt`, while HC render/Slack blocks say `Biotech readout`.

I could not run `pytest` in this read-only sandbox because pytest failed before collection with `No usable temporary directory found`. Direct `python3` import-level probes were successful.
